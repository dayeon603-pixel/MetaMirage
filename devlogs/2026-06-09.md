# MetaMirage Devlog — 2026-06-09

## [11:00 UTC] Session — R97

### State at Session Start
- MetaMirage is post-deadline (April 17, 2026 Kaggle submission deadline). Core research findings confirmed and locked:
  - Global sign-flip: r = −0.94 (TDR vs accuracy, n=7 models)
  - Forced abstention family: r = −0.92, 95% CI excludes zero, LOO-stable
  - Confidence inversion family: r = +0.97, 95% CI excludes zero, LOO-stable
  - Both effects LOO-stable across all n−1 leave-one-out folds
- Repo state: kaggle_submission.ipynb, dashboard.html, BLOG_POST.md, TWEET_THREAD.md all finalized. SUBMISSION_CHECKLIST.md records submission workflow.
- Active parallel work: SPS R97 (suite→5278) and NeuroVoice R97 (suite→6981) running in this session.

### Work Completed
- Session orientation: confirmed all 5 repos have clean author history (Dayeon only, zero Claude-authored commits).
- Confirmed Phase 0B: SPS all 8 failure files have **Date:** and **Status:** fields; NeuroVoice all 6 failure files similarly complete.
- Confirmed metaMirage post-deadline steady state: no new code work required; repository content complete.

### Results / Observations
- MetaMirage submission artifacts: README.md (17 KB), BLOG_POST.md (5.6 KB), dashboard.html (65 KB), kaggle_submission.ipynb (142 KB) all present and finalized.
- Sign-flip finding stands: global r = −0.94 confirmed by bootstrap CI and LOO analysis across both forced_abstention and confidence_inversion family subsets.
- SPS R97: `tests/test_kaggle_adversarial_sps_source_noarch.py` added (+30 tests, suite 5,248 → 5,278). Kaggle notebook source-locked across all 7 cells.
- NeuroVoice R97: `tests/test_decoder_source_5_noarch.py` added (+30 tests, suite 6,951 → 6,981). decoder.py 5th source pass; file now at saturation.

### Hypotheses
- No new hypotheses for metaMirage this session. Research findings are locked and published.

### Next Steps
- metaMirage: No further code work required. Monitor for any post-submission reviewer questions.
- Primary focus remains SPS and NeuroVoice coverage expansion (SPS→5308, NeuroVoice→7011 targets for R98).

## [11:45 UTC] Session — R98

### State at Session Start
- MetaMirage remains in post-deadline steady state. No new research work required.
- Active parallel work: SPS R98 (suite→5308) and NeuroVoice R98 (suite→7011) running in this session.

### Work Completed
- SPS R98: `tests/test_sps_stats_behavioral_noarch.py` pushed (+30, suite 5,278 → 5,308). First behavioral integration tests for sps/stats.py — all 7 exported functions (bootstrap_ci, loo_spectral_gap, loo_batch_spectral_gap, spectral_gap_report, delta_method_rsps_ci, permutation_test_gap_diff, compare_architecture_cis) validated with live round-trips using stdlib-only synthetic data.
- NeuroVoice R98: `tests/test_pretrain_source_noarch.py` pushed (+30, suite 6,981 → 7,011). First source pass for src/training/pretrain.py — set_seed/run_pretrain/data-split/DataLoader/models/optimizer/scheduler/training-loop/_masked_mse/_evaluate all source-locked.

### Results / Observations
- SPS suite: 5,278 → 5,308 (+30). All 7 sps/stats.py exported functions now have behavioral test coverage.
- NeuroVoice suite: 6,981 → 7,011 (+30). src/training/pretrain.py first source pass complete.
- Combined noarch suite: 5,308 + 7,011 = 12,319 tests.
- Key behavioral confirmations (SPS stats): delta_method_rsps_ci correctly produces CI lo>1 for large-gap/small-variance case — directly validates the sign-flip statistical inference machinery.

### Hypotheses
- No new metaMirage hypotheses this session.

### Next Steps
- metaMirage: Monitor for post-submission queries. No code work required.
- SPS R99: sps/transformations.py 2nd source pass or sps/metrics.py behavioral pass (suite→5,338).
- NeuroVoice R99: pretrain.py 2nd source pass (checkpoint guards/best_val update/encoder.cpu) or finetune.py first source pass (suite→7,041).

## [12:30 UTC] Session — R99

### State at Session Start
- MetaMirage remains in post-deadline steady state. No new research work required.
- Active parallel work: SPS R99 (suite→5,338) and NeuroVoice R99 (suite→7,041) running in this session.

### Work Completed
- SPS R99: `tests/test_metrics_source_2_noarch.py` pushed (+30, suite 5,308 → 5,338). 2nd source pass for sps/metrics.py — SPSReport residual fields (spectral_gap_result/gap_ci/rsps_ci), summary() body residuals (header/indistinguishable branch/rSPS+gap appends/join return), estimate_arbitrary_sps() construction kwargs, full_sps_analysis() SPSReport kwargs, _get_embedding_layer()/_find_layers() body source-locked.
- NeuroVoice R99: `tests/test_pretrain_source_2_noarch.py` pushed (+30, suite 7,011 → 7,041). 2nd source pass for src/training/pretrain.py — setup/model-construction residuals (checkpoint_dir.mkdir/dev=torch.device/encoder/mae_decoder/params/AdamW/steps_per_epoch/total_epochs), backward-pass sequence (zero_grad/backward/step/scheduler/increment/guards), checkpoint expressions (best check+update/"best_encoder.pt"/"final_encoder.pt"/return encoder.cpu()), _masked_mse channel mean (diff.mean(dim=-1)), _evaluate accumulation.

### Results / Observations
- SPS suite: 5,308 → 5,338 (+30). sps/metrics.py at source-expression saturation (2 passes).
- NeuroVoice suite: 7,011 → 7,041 (+30). src/training/pretrain.py at source-expression saturation (2 passes).
- Combined noarch suite: 5,338 + 7,041 = 12,379 tests.

### Hypotheses
- No new metaMirage hypotheses. Research findings remain locked and published.

### Next Steps
- metaMirage: Monitor for post-submission queries. No code work required.
- SPS R100: sps/core.py 2nd source pass or sps/jacobian.py residuals (+30, suite→5,368).
- NeuroVoice R100: src/training/finetune.py 1st source pass (+30, suite→7,071).

## [13:10 UTC] Session — R100

### State at Session Start
- MetaMirage remains in post-deadline steady state. No new research work required.
- Active parallel work: SPS R100 (suite→5,368) and NeuroVoice R100 (suite→7,071) running in this session.

### Work Completed
- SPS R100: `tests/test_sps_core_source_2_noarch.py` pushed (+30, suite 5,338 → 5,368). 2nd source pass for sps/core.py — SSE.estimate() transform-sample + ratio block (tx/cx sample call, `if not valid.any(): continue`, ftx forward, `torch.where` ratio with `cx.clamp(min=1e-12)` and `zeros_like`), SSE._forward() dim==3 branch (guard + plain CLS slice + 2D return), build_sps_estimator() SSE constructor wiring (all 5 kwargs + return SPSEstimator), SPSEstimator._get_embeddings() callable path (if callable/no_grad/return get_emb()(input_ids)) and guard conditionals (sub/emb_layer/we not-None checks, return we(input_ids), raise AttributeError).
- NeuroVoice R100: `tests/test_finetune_source_noarch.py` pushed (+30, suite 7,041 → 7,071). First source pass for src/training/finetune.py — module constants (_F0_IDX=6, _VOICING_IDX=8), setup (checkpoint_dir.mkdir, dev=torch.device, pipeline construction, pretrain_checkpoint guard, AdamW param-group structure with encoder/decoder separate LRs, weight_decay=0.01), loader+scheduler (total_steps formula, CosineAnnealingLR with T_max/eta_min=1e-7, mse_criterion), loop inits, per-batch forward (neural/articulatory batch keys, pred=pipeline, T=min temporal align, mse loss), voicing loss block (voicing_pred aligned form, > 0.5 threshold, BCE logits, weighted total_loss), warmup (global_step guard, scale formula, g["lr"] update), epoch_loss normalize, pipeline.save("best_pipeline.pt"), return from_checkpoint.cpu().

### Results / Observations
- SPS suite: 5,338 → 5,368 (+30). sps/core.py now source-locked across 6 dedicated passes (R66/R68/R79/R81/R82/R100). All structural expressions covered.
- NeuroVoice suite: 7,041 → 7,071 (+30). src/training/finetune.py first source pass complete. All setup, loop, voicing, warmup, and checkpoint expressions locked.
- Combined noarch suite: 5,368 + 7,071 = 12,439 tests.

### Hypotheses
- sps/core.py is now comprehensively source-locked. Remaining uncovered territory is minimal (primarily the return dict key assertions already covered by R66).
- finetune.py has substantial 1st-pass coverage. 2nd pass will target _validate() body and early-stopping residuals.

### Next Steps
- metaMirage: Monitor for post-submission queries. No code work required.
- SPS R101: sps/jacobian.py first source pass or sps/core.py behavioral integration tests (+30, suite→5,398).
- NeuroVoice R101: src/training/finetune.py 2nd source pass — _validate() body, early-stopping, epoch logging (+30, suite→7,101).

## [14:11 UTC] Session — R101

### State at Session Start
- MetaMirage remains in post-deadline steady state. No new research work required.
- Active parallel work: SPS R101 and NeuroVoice R101 running in this session.

### Work Completed
- SPS R101: `tests/test_transformations_source_2_noarch.py` pushed (+30, suite 5,368 → 5,398). 2nd source pass for sps/transformations.py — EPF.sample() full body (device/B/idx/v/alpha/alpha_exp/perturbed/magnitudes), semantic_directions branch, _synonym_directions residuals (all_directions list annotation, syn_ids.clone, synonym_map.get, noise_scale, delta+noise, d_norm normalize+view_as, append unsqueeze, cat return), _random_orthogonal_directions Gram-Schmidt residuals (proj formula, norm.clamp), AdversarialEmbeddingFamily.sample() (late-import, detach+requires_grad, enable_grad context, v_star adversarial call + renormalize, perturbed formula), build_wordnet_synonym_map body (vocab_size default, dict annotation, decode+strip, isalpha/len guard).
- NeuroVoice R101: `tests/test_finetune_source_2_noarch.py` pushed (+30, suite 7,071 → 7,101). 2nd source pass for src/training/finetune.py — _validate() complete body (@torch.no_grad decorator, pipeline.eval, total/n init, device-keyed batch access, tuple-assign temporal alignment, voicing loss single-line form, total accumulate, n+1, return total/max(n,1)), _validate() signature (voicing_weight param name, mse_criterion: nn.Module, -> float), run_finetune() residuals (epoch range 1..max_epochs+1, pipeline.train, epoch_loss init, clip_grad_norm, optimiser/scheduler.step, epoch_loss accumulate, global_step+1, _validate call with all 5 args, Fine-tuning complete logger).

### Results / Observations
- MetaMirage post-deadline state unchanged. No new findings.
- SPS suite: 5,368 → 5,398 (+30). sps/transformations.py at source-expression saturation (2 passes).
- NeuroVoice suite: 7,071 → 7,101 (+30). src/training/finetune.py at source-expression saturation (2 passes).
- Combined noarch suite: 5,398 + 7,101 = 12,499 tests.

### Hypotheses
- No new metaMirage hypotheses this session. Research findings remain locked.

### Next Steps
- metaMirage: Monitor for post-submission queries. No code work required.
- SPS R102: sps/stats.py 2nd source pass or sps/jacobian.py 2nd source pass (+30, suite→5,428).
- NeuroVoice R102: src/data/dataset.py 2nd source pass or src/models/pipeline.py 2nd source pass (+30, suite→7,131).

## [15:05 UTC] Session — R102

### State at Session Start
- MetaMirage remains in post-deadline steady state. No new research work required.
- Active parallel work: SPS R102 and NeuroVoice R102 running in this session.

### Work Completed
- SPS R102: `tests/test_cross_arch_adversarial_7_noarch.py` pushed (+30, suite 5,398 → 5,428). 7th source pass for experiments/cross_arch_adversarial.py — prepare_batches body (batches list/chunking loop/tokenizer kwargs/enc dict access/embedding_layer forward/batches.append), run_architecture model-load block (AutoTokenizer/AutoModel.from_pretrained, model.eval+to, get_input_embeddings, skip_wordnet guard, vocab_size=10_000/max_synonyms_per_token=5, logger.warning WordNet fallback), run_architecture SDP/adv construction (_sdpa_kernel_adv import+context, except block, AdversarialEmbeddingConfig/Family kwargs), collect_spectral_gaps SDP branch (_sdpa_kernel import, logger.debug failure).
- NeuroVoice R102: `tests/test_dataset_source_noarch.py` pushed (+30, suite 7,101 → 7,131). First source pass for src/data/dataset.py — NeuralSpeechDataset (all 4 init field assignments, _windows typed list, for-path loop, _load_file, logger.info fstring, h5py.File, pid/sid nested loops, grp access, eeg/art/events reads, onset/offset loop, seg_eeg slice, sr_neural read, __len__/__getitem__), PretrainDataset (mask_ratio, _windows, logger.info, "eeg" guard, preprocessor.epoch, _windows.extend, n_masked/randperm/zeros bool/mask[indices]), CollateFn (keys=batch[0].keys()).

### Results / Observations
- MetaMirage post-deadline state unchanged. No new findings.
- SPS suite: 5,398 → 5,428 (+30). cross_arch_adversarial.py at 7 source passes (216 tests).
- NeuroVoice suite: 7,101 → 7,131 (+30). src/data/dataset.py first source pass complete.
- Combined noarch suite: 5,428 + 7,131 = 12,559 tests.

### Hypotheses
- No new metaMirage hypotheses this session. Research findings remain locked.

### Next Steps
- metaMirage: Monitor for post-submission queries. No code work required.
- SPS R103: cross_arch_adversarial.py 8th source pass residuals or sps/core.py behavioral integration tests (+30, suite→5,458).
- NeuroVoice R103: src/data/dataset.py 2nd source pass — sr_art/art_onset/art_offset, windowing loop, win_art guard, _windows.append, torch.from_numpy, CollateFn stack (+30, suite→7,161).

## [17:00 UTC] Session — R103

### State at Session Start
- MetaMirage: post-deadline steady-state. Research locked.
- SPS suite: 5,428 tests. NeuroVoice suite: 7,131 tests. Combined: 12,559.

### Work Completed
- SPS R103: `tests/test_jacobian_37_noarch.py` (+30, suite 5,428 → 5,458). sps/jacobian.py 2nd source pass — SpectralGapResult.__repr__ (def, → str annotation, all three f-string components with :.4f formatting, count :.4f} == 3), adversarial_worst_direction gather block (worst_dirs = directions[torch.arange(B, device=x.device), worst_k], return worst_dirs, gather comment, docstring: worst-case semantic direction / constructive dual / AdversarialEmbeddingFamily / argmax_{v in A_x}), full_spectral_norm shape+device+probe loop (B/seq_len/hidden = x.shape, device = x.device, for _ in range(n_probe), v = normalize_directions(v), return max_norm, n_probe: int = 8 / n_iter: int = 16 defaults, jvp call with v not v_k).
- NeuroVoice R103: `tests/test_dataset_source_2_noarch.py` (+30, suite 7,131 → 7,161). src/data/dataset.py 2nd source pass — NeuralSpeechDataset windowing block (sr_art = int(grp.attrs.get("sr_articulatory", 100)), art_onset/art_offset int formulas, seg_art slice, windowing range() loop, win_eeg/art_start/win_art slices, win_art guard, _windows.append+astype), __getitem__ return keys ("neural"/from_numpy(eeg), "articulatory"/from_numpy(art)), PretrainDataset __getitem__ (from_numpy(self._windows[idx]), C/W = x.shape, mask[mask_indices] = True, "neural": x / "mask": mask), CollateFn body (out dict init, for-k, tensors list-comp, bool dtype check, torch.stack, return out), module-level (__all__, preprocessing import, signature defaults 250/50/50/125).

### Results / Observations
- MetaMirage post-deadline state unchanged. No new findings.
- SPS suite: 5,428 → 5,458 (+30). sps/jacobian.py at source-expression saturation after 2 passes.
- NeuroVoice suite: 7,131 → 7,161 (+30). src/data/dataset.py 2nd pass complete.
- Combined noarch suite: 5,458 + 7,161 = 12,619 tests.

### Hypotheses
- No new metaMirage hypotheses this session. Research findings remain locked.

### Next Steps
- metaMirage: Monitor for post-submission queries. No code work required.
- SPS R104: sps/jacobian.py behavioral tests (mock JVP) or sps/core.py behavioral integration tests (+30, suite→5,488).
- NeuroVoice R104: src/data/dataset.py behavioral integration (mock h5py) or src/data/articulatory.py source pass (+30, suite→7,191).
