# metaMirage Devlog — 2026-06-07

## [00:00 UTC] Session — satellite R58

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_core_rel_noarch.py` (+30 noarch tests, suite→4108). RelativeSPSEstimator._collect_sensitivities body: config=estimator.config, all_sens: list[torch.Tensor] = [], for batch in batches, input_ids/attention_mask/embeddings to(config.device), "embeddings" in batch guard, estimator._get_embeddings(input_ids) fallback, all_sens.append(sens.cpu()), if not all_sens guard + return [], return torch.cat(all_sens).tolist(). estimate() body: late import from sps.stats (bootstrap_ci, delta_method_rsps_ci, loo_spectral_gap), t_result=self.t_estimator.estimate(iter(batches)), sps_t/mean_sens_t/std_sens_t/n_samples_t dict extractions, arb_result=self.arb_estimator.estimate(iter(batches)), sps_arb/mean_sens_arb/n_samples_arb dict extractions, emb_mean_s=mean_sens_t+arb_mean_s=mean_sens_arb kwargs for delta_method_rsps_ci, rsps_ci=(rsps_point,rsps_lo,rsps_hi), rsps_ci_excludes_one=rsps_lo>1.0 or rsps_hi<1.0, t_sens_list=self._collect_sensitivities(self.t_estimator,batches), sens_ci_t=bootstrap_ci(t_sens_list,seed=self.config.seed), loo_t=loo_spectral_gap(t_sens_list), "RelativeSPS: sps_t=%.4f..." logger format, return RelativeSPSReport with rsps_ci/sens_ci_t/loo_t kwargs.
- **NeuroVoice**: `tests/test_train_vocoder_8_noarch.py` (+30 noarch tests, suite→5811). run_vocoder_training body setup: _set_seed(train_config.seed), checkpoint_dir.mkdir(parents=True,exist_ok=True), dev=torch.device(device), n_val=max(1,len(articulatory)//10), art_train+art_val articulatory[n_val:]+[:n_val].to(dev), aud_train+aud_val audio[n_val:]+[:n_val].to(dev). Models+opts: TensorDataset(art_train,aud_train), DataLoader(dataset,batch_size=train_config.batch_size,shuffle=True,drop_last=True), HiFiGANVocoder(vocoder_config).to(dev), VocoderDiscriminator().to(dev), AdamW(generator.parameters(),lr_g,betas), AdamW(discriminator.parameters(),lr_d,betas), ExponentialLR(opt_g,gamma=lr_decay), ExponentialLR(opt_d,gamma=lr_decay). Loop init: logger.info startup with train/val counts+device+max_steps, global_step=0, best_val_mel=float("inf"), seg=train_config.segment_size, while global_step<train_config.max_steps, generator.train()+discriminator.train().

## [14:00 UTC] Session — satellite R59

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_metrics_full_analysis_noarch.py` (+30 noarch tests, suite→4138). full_sps_analysis body: config=config or SPSConfig(), build_sps_estimator(model,transform_family,config), estimator.estimate(iter(batches)), report=SPSReport(, if compute_spectral_gap:, logger.info("Computing spectral gap ..."), batch=batches[0], emb=batch.get("embeddings"), if emb is None: raise ValueError(. estimate_arbitrary_sps body: from sps.transformations import EmbeddingPerturbationConfig, arb_config=EmbeddingPerturbationConfig(n_directions=config.m_transforms,use_synonym_directions=False), embedding_layer=_get_embedding_layer(model), EmbeddingPerturbationFamily(embedding_layer,synonym_map=None,config=arb_config), build_sps_estimator, result=estimator.estimate(data_iterator), return result if return_full else result["sps"]. _get_embedding_layer body: get_emb=getattr(model,"get_input_embeddings",None), if callable(get_emb): return get_emb(), for attr in ("roberta","bert","transformer","model"), sub/emb/we getattr chain, if we is not None: return we, raise AttributeError.
- **NeuroVoice**: `tests/test_train_vocoder_9_noarch.py` (+30 noarch tests, suite→5841). run_vocoder_training inner loop: segment crop block (for loop header, max_steps guard+break, T_audio=aud_batch.shape[-1], if T_audio>seg, random.randint, aud_batch/art_batch slice assignments, art_start/art_len int expressions). Discriminator step (fake_audio=generator(art_batch), opt_d.zero_grad, real_scores/_+fake_scores/_ from discriminator, _discriminator_loss, loss_d.backward, clip_grad_norm_ on discriminator.parameters(), opt_d.step, _/real_fmaps+fake_scores_g/fake_fmaps second pass). Generator step (opt_g.zero_grad, _generator_adv_loss, _feature_matching_loss, _mel_spectrogram×2, F.l1_loss, lambda_fm+lambda_mel weighted sum, loss_g.backward, clip_grad_norm_ on generator.parameters()).

## [04:04 UTC] Session — satellite R60

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_metrics_spectral_gap_subblock_noarch.py` (+30 noarch tests, suite→4168). full_sps_analysis spectral-gap subblock: emb.to(config.device)/input_ids/attention_mask batch-to-device, directions=transform_family.semantic_directions(emb,input_ids), gap_result=spectral_gap(_fn,emb,directions), report.spectral_gap_mean=gap_result.mean_gap, report.spectral_gap_result=gap_result, per_sample_gaps=gap_result.normalized_gap.tolist(), report.gap_ci=bootstrap_ci(per_sample_gaps), out=model(inputs_embeds=inputs_embeds,attention_mask=attention_mask). compute_relative subblock: if compute_relative:, logger.info("Computing relative SPS..."), arb_full=estimate_arbitrary_sps(model,iter(batches),config,return_full=True), report.relative_sps=relative_sps(report.sps,arb_full["sps"]), report.rsps_ci=delta_method_rsps_ci(, emb_mean_s/emb_std_s/emb_n/arb_mean_s/arb_std_s kwargs. compute_layerwise subblock: arb_n=arb_full["n_samples"], if compute_layerwise:, logger.info("Computing layerwise SPS profile ..."), analyzer=LayerwiseSPSAnalyzer(model,transform_family,config), report.layerwise_profile=analyzer.compute_profile_from_batches(batches), return report, _fn internals (hasattr/return last_hidden_state/return out dim3), sps=core_result["sps"].
- **NeuroVoice**: `tests/test_train_vocoder_10_noarch.py` (+30 noarch tests, suite→5871). run_vocoder_training final blocks: opt_g.step()/scheduler_g.step()/scheduler_d.step()/global_step+=1/if %1000==0/logger.info progress f-strings. val_every branch: _validate_vocoder call/val mel log/val_mel<best_val_mel guard/best_val_mel update/torch.save/generator_state_dict+step keys/best_vocoder.pt path/↳ New best log. checkpoint_every branch: vocoder_step_{global_step}.pt path. Final: training-complete log/torch.load with map_location+weights_only/generator.load_state_dict/return generator.cpu(). _validate_vocoder body: generator.eval()/fake=generator(art_val)/mel_real aud_val/return float(loss).

## [05:05 UTC] Session — satellite R61

No new R&D this session (satellite-only).
- **SPS**: `tests/test_layerwise_compute_profile_noarch.py` (+30 noarch tests, suite→4198). compute_profile_from_batches body: profile:list[float]=[], for layer_idx/layer_module in enumerate(self._layers), logger.info computing-layer format, sps_l=self._compute_sps_at_layer(batches,layer_module,layer_idx), profile.append(sps_l), logger.info result format, return profile. _compute_sps_at_layer hook fn: captured dict init, hook sig, isinstance(output,tuple) path/captured["hidden"]=output[0][:,0,:].detach(), elif isinstance tensor path/dim()==3 path/else path, handle=target_layer.register_forward_hook(hook). _compute_sps_at_layer layer_fn+proxy: layer_fn sig, _=self.model(inputs_embeds=embeddings,...), assert captured["hidden"] is not None, return captured["hidden"], class _LayerProxy(nn.Module), return layer_fn(inputs_embeds,attention_mask), proxy=_LayerProxy(), divergence_fn=get_divergence_fn(self.config.divergence), sens_est=StructuredSensitivityEstimator(, result=estimator.estimate(iter(batches))/handle.remove()/return result["sps"].
- **NeuroVoice**: `tests/test_encoder_forward_noarch.py` (+30 noarch tests, suite→5901). _TCNBlock.forward body: out=self.conv(x), causal trim out[...,:x.shape[-1]], rearrange bdt→btd, self.norm(out), rearrange btd→bdt, self.act(out), self.dropout(out), return out+x. NeuralEncoder.forward body: z=self.input_proj(x), for block in self.tcn:/z=block(z), rearrange bdt→btd, attn_out/_=self.channel_attn(z,z,z), z=self.attn_norm(z+attn_out), out:torch.Tensor=self.output_norm(self.output_proj(z)), return out. MaskedAutoencoderDecoder.forward body: B/T/D=latent.shape, mask_tokens=self.mask_token.expand(B,T,D), x=torch.where(mask.unsqueeze(-1),mask_tokens,latent), x=self.proj(x)/transformer(x), recon:torch.Tensor=self.head(x), return recon.

## [06:10 UTC] Session — satellite R62

No new metaMirage R&D this session (satellite-only).
- **SPS**: `tests/test_layerwise_init_noarch.py` (+30 noarch tests, suite→4228). LayerwiseSPSAnalyzer.__init__ body: self.model=model, self.transform_family=transform_family, self.config=config, self._layers=self._find_layers(model), if not self._layers guard, raise ValueError. compute_profile body: raise NotImplementedError. _find_layers body: roberta/bert for loop, sub/encoder/layers getattr chain, return list(layers), transformer=getattr GPT-2 block, h=getattr/return list(h), for attr in ("encoder","layers","blocks") generic block, isinstance(sub,nn.ModuleList). conftest.py patched with pydantic+numpy stubs for noarch environments.
- **NeuroVoice**: `tests/test_pretrain_3_noarch.py` (+30 noarch tests, suite→5931). run_pretrain body setup: set_seed(seed), checkpoint_dir.mkdir, dev=torch.device, n_val/n_train, random_split tuple unpack, DataLoader train/val with num_workers/pin_memory/drop_last. Models block: NeuralEncoder+MAE decoder construction, params concat, AdamW, steps_per_epoch, total_epochs. _masked_mse body: diff squared, mean channels, masked_loss formula, return. _evaluate body: eval calls, total_loss/n_batches init, loss accumulate, return mean.

## [07:00 UTC] Session — satellite R63

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_stats_source_noarch.py` (+30 noarch tests, suite→4258). bootstrap_ci body: n=len(values), if n==0:/return 0.0,0.0,0.0, rng=random.Random(seed), boot_means=sorted(sum(rng.choices(values,k=n))/n for _ in range(n_boot)), lo_idx=int((alpha/2)*n_boot), hi_idx=int((1-alpha/2)*n_boot), round(sum(values)/n,6)+round(boot_means[lo_idx],6) return tuple. loo_spectral_gap body: n=len(per_sample_gaps), if n<3:/return {"stable":False,"n":n}, total=sum(per_sample_gaps), loo_means=[(total-g)/(n-1) for g in per_sample_gaps], "mean":round(total/n,6)/"loo_min"/"loo_max"/"loo_range"/"stable" return dict. delta_method_rsps_ci body: if abs(alpha-0.05)<1e-9:/z=1.96/elif abs(alpha-0.01)<1e-9:/z=2.576/z=1.645, log_rsps=arb_mean_s-emb_mean_s, var_log=(emb_std_s**2)/max(emb_n,1)+(arb_std_s**2)/max(arb_n,1), se_log=math.sqrt(var_log), rsps_point=math.exp(log_rsps), rsps_lo=math.exp(log_rsps-z*se_log).
- **NeuroVoice**: `tests/test_pretrain_4_noarch.py` (+30 noarch tests, suite→5961). run_pretrain training-loop body: scheduler=OneCycleLR(max_lr=lr,total_steps=max_steps,pct_start=warmup_steps/max_steps,anneal_strategy="cos"), global_step=0, best_val_loss=float("inf"), startup logger.info, while global_step<max_steps:/encoder.train()/mae_decoder.train(), for batch in train_loader:/if global_step>=max_steps:break, neural:torch.Tensor=batch["neural"].to(dev)/mask:torch.Tensor=batch["mask"].to(dev), latent=encoder(neural)/recon=mae_decoder(latent,mask), target=neural.permute(0,2,1), loss=_masked_mse(recon,target,mask), optimiser.zero_grad()/loss.backward()/clip_grad_norm_(params,grad_clip)/optimiser.step(), scheduler.step(), global_step+=1, if%500==0:logger.info train-loss, if%checkpoint_every==0:_evaluate+val-log+best_val_guard+torch.save/best_encoder.pt, final torch.save/final_encoder.pt, return encoder.cpu(). numpy 2.4.6 also installed into pytest tool venv (conftest.py fix).

## [08:12 UTC] Session — satellite R64

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_loo_batch_spectral_gap_noarch.py` (+30 noarch tests, suite→4288). loo_batch_spectral_gap body: n=len(per_batch_means), if n<3 guard, return {"stable":False,"n_batches":n}, total=sum, loo_means list-comp, grand_mean=total/n, worst_idx=max(range(n),key=lambda i:abs(loo_means[i]-grand_mean)), return dict with "grand_mean":round(grand_mean,6)+"worst_batch":worst_idx. spectral_gap_report body: mean/ci_lo/ci_hi=bootstrap_ci(n_boot/seed/alpha kwargs), loo=loo_spectral_gap(per_sample_gaps), loo_stable=loo.get("stable",False), loo_range=loo.get("loo_range",0.0), return dict with "ci_width":round(ci_hi-ci_lo,6)+"loo_stable"+"n":len(per_sample_gaps). compare_architecture_cis body: reports:dict[str,dict]={}, for arch,gaps in arch_gaps.items(), reports[arch]=spectral_gap_report(gaps,n_boot=n_boot,seed=seed), ranking=sorted(reports.keys(),key=lambda a:reports[a]["mean"],reverse=True), reports["ranking"]=ranking, return reports.
- **NeuroVoice**: `tests/test_eval_metrics_2_noarch.py` (+30 noarch tests, suite→5991). compute_metrics body: if predicted.shape!=target.shape guard+raise ValueError, N,A=predicted.shape, assert A==ARTICULATORY_DIM, rmse_per_dim=[]+corr_per_dim=[], for i in range(A), p=predicted[:,i], rmse_per_dim.append(float(np.sqrt(np.mean((p-t)**2)))), r,_=stats.pearsonr(p,t), corr_per_dim.append(float(r) if np.isfinite(r) else 0.0), mean_rmse/mean_corr float(np.mean) calls, voiced_pred=(predicted[:,_VOICING_IDX]>voicing_threshold).astype(int), voicing_accuracy=float(np.mean(voiced_pred==voiced_target)), voiced_mask=voiced_target.astype(bool), n_voiced=int(voiced_mask.sum()), f0_rmse=float("nan") else branch, logger.warning("Fewer than 2 voiced frames"). summary() body: lines=[...] init, n={self.n_frames}/mean_rmse:.4f/mean_corr:.4f/voicing_accuracy:.3f/f0_rmse_voiced:.2f/f0_corr_voiced:.4f f-strings, for i,name in enumerate(ARTICULATORY_NAMES), lines.append, return "\n".join(lines). Also fixed test_eval_metrics_noarch.py numpy stub (30 tests recovered from error state).

## [09:15 UTC] Session — satellite R65

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_permutation_test_body_noarch.py` (+30 noarch tests, suite→4318). permutation_test_gap_diff body: rng=random.Random(seed), na/nb=len(gaps_a)/len(gaps_b), if na==0 or nb==0 guard, pooled=gaps_a+gaps_b, n_total=na+nb, mean_a/mean_b/obs_diff, extreme=0, for _ in range(n_perm), rng.shuffle(pooled), perm_a=pooled[:na]/perm_b=pooled[na:], perm_diff=sum(perm_a)/na-sum(perm_b)/nb, two-sided/greater/less branches with abs/>=/<= comparisons, extreme+=1, p_value=(extreme+1)/(n_perm+1), return dict. Module-level: from __future__ import annotations, import math/random, __all__ contains permutation_test_gap_diff.
- **NeuroVoice**: `tests/test_alignment_loss_forward_noarch.py` (+30 noarch tests, suite→6021). SONARAlignmentLoss.forward body: cosine_sim=F.cosine_similarity(predicted,target_sonar,dim=1), cosine_loss=(1.0-cosine_sim).mean(), losses dict init, if language_ids is not None guard, lid_logits=self._lid_head(predicted.detach()), n_classes=lid_logits.shape[1], uniform=torch.full_like(lid_logits,1.0/n_classes), F.kl_div(F.log_softmax(lid_logits,dim=1),uniform,reduction="batchmean"), losses["lid_suppression"]=-lid_suppression*self._aux_weight, losses["total"]=cosine_loss+losses["lid_suppression"], return losses. Module: from __future__ import annotations, torch/nn/F imports, class SONARAlignmentLoss(nn.Module), super().__init__(), self._aux_weight/self._lid_head assignments.

## [10:09 UTC] Session — satellite R66

No new metaMirage R\&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_sps_estimator_body_source_noarch.py` (+30 noarch tests, suite→4348). SPSEstimator.estimate() body: set_seed(self.config.seed), all_sens: list[torch.Tensor] = [], for batch in data_iterator, input_ids/attention_mask .to(self.config.device), "embeddings" in batch guard, _get_embeddings fallback, sens_estimator.estimate(embeddings=…/input_ids=…/attention_mask=…/m=self.config.m_transforms) kwargs, all_sens.append(sens.cpu()), if not all_sens guard, RuntimeError("data_iterator yielded no batches."), torch.cat(all_sens), mean_s=.mean().item(), import math, math.exp(-mean_s), from sps.stats import bootstrap_ci/loo_spectral_gap, sens_list=.tolist(), bootstrap_ci(seed=self.config.seed)/loo_spectral_gap calls, logger.info format string, return dict keys (sps/mean_sensitivity/std_sensitivity/n_samples/sens_ci/loo).
- **NeuroVoice**: `tests/test_confidence_estimate_body_noarch.py` (+30 noarch tests, suite→6051). ConfidenceEstimator.estimate() body: n=self._fallback_passes if low_latency else self._n_passes, if n==1 guard, self._model.eval(), embedding=self._model(features).cpu().numpy().flatten(), return embedding,0.5 (unknown confidence), self._model.train(), embeddings=[], for _ in range(n), emb=self._model(features).cpu().numpy().flatten(), embeddings.append(emb), np.stack(embeddings), mean_emb=embeddings_arr.mean(axis=0), variance=embeddings_arr.var(axis=0).mean(), confidence=float(np.exp(-variance*10)), max(0.0,min(1.0,confidence)), return mean_emb,confidence. Module structure: from __future__ import annotations, import dataclasses/logging/numpy/torch, sonar_projector import, @torch.no_grad(), @dataclasses.dataclass(slots=True), class SemanticVector/ConfidenceEstimator declarations.

## [11:08 UTC] Session — satellite R67

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_structured_sensitivity_forward_body_noarch.py` (+30 noarch tests, suite→4378). StructuredSensitivityEstimator._forward body: out = self.model(, inputs_embeds=embeddings kwarg, attention_mask=attention_mask kwarg, # Support both BaseModelOutput comment, if hasattr(out, "last_hidden_state"):, return out.last_hidden_state[:, 0, :], # (B, h) — CLS token, if isinstance(out, torch.Tensor):, if out.dim() == 3:, raise TypeError(f"Unexpected model output type: {type(out)}"). estimate() inner-loop: fx = self._forward(embeddings, attention_mask), max_ratios = torch.zeros(B, device=self.device), for _ in range(m):, tx, cx = self.transform_family.sample(, embeddings, input_ids, self.epsilon, valid = cx > 1e-12, if not valid.any():, ftx = self._forward(tx, attention_mask), div = self.divergence_fn(fx, ftx), ratio = torch.where(. build_sps_estimator() factory: config = config or SPSConfig(), divergence_fn = get_divergence_fn(config.divergence), sens_estimator = StructuredSensitivityEstimator(, epsilon=config.epsilon, device=config.device, return SPSEstimator(sensitivity_estimator=sens_estimator, config=config).
- **NeuroVoice**: `tests/test_articulatory_extract_noarch.py` (+30 noarch tests, suite→6081). ArticulatoryInverter.extract() body: sr = self.sample_rate, hop = self.hop_length, n_fft = self.n_fft, f0/voiced_flag/voiced_prob = librosa.pyin( with fmin/fmax C2-C7/sr/hop_length/fill_na=0.0 kwargs, f0_delta = np.diff(f0, prepend=f0[0]).astype(np.float32). Spectral: stft = librosa.stft(audio, n_fft, hop), magnitude = np.abs(stft), centroid/flatness/rms/log_rms/freqs calls, low_energy/spectral_tilt/nasal_energy band calculations. Features list + align loop: n_frames = f0.shape[0], features = [12-element list], for feat in features/f = feat[:n_frames]/np.pad(edge), art = np.stack(aligned, axis=1)/art = self._normalise(art)/return art.

## [12:00 UTC] Session — satellite R68

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_sps_core_init_and_summary_noarch.py` (+30 noarch tests, suite→4408). StructuredSensitivityEstimator.__init__ body: self.model=model, self.transform_family=transform_family, self.divergence_fn=divergence_fn, self.epsilon=epsilon, self.device=device, param annotations (model:nn.Module, transform_family:TransformationFamily, divergence_fn:DivergenceFn, epsilon:float, device:str="cpu"). SPSEstimator.__init__ body: self.sens_estimator=sensitivity_estimator, self.config=config, sensitivity_estimator:StructuredSensitivityEstimator annotation, config:SPSConfig annotation. SPSConfig.validate_device body: try:, torch.device(v), except RuntimeError as e:, raise ValueError(f"Invalid torch device '{v}': {e}") from e, return v, @classmethod decorator. AdversarialSPSReport.summary() body: near-adversarial/moderate/LARGE gap_label strings, adv_gap_ratio>0.95/>0.80 guards, SPS_emb/SPS_adv/Adversarial-gap-ratio/N-Samples line entries, "\n".join(lines) return.
- **NeuroVoice**: `tests/test_emg_features_source_noarch.py` (+30 noarch tests, suite→6111). EMGFeatureExtractor.__init__ body: mode:str="raw"/n_fft:int=64 defaults, if mode not in ("raw","handcrafted"): guard, msg=f"Unknown mode: {mode}..." error, raise ValueError(msg), self._mode=mode, self._n_fft=n_fft. extract() body: if self._mode=="raw": dispatch guard, return segment, return self._handcrafted_features(segment). _handcrafted_features temporal block: features_per_ch init, for ch in range(segment.shape[0]):, x=segment[ch], n=len(x), rms/mav/zcr/wl/ssc formulas. Spectral block: fft_mag/freqs/power/total_power, mean_freq/cumulative/med_idx/median_freq, return np.concatenate(features_per_ch).

## [13:05 UTC] Session — satellite R69

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_relative_sps_report_summary_and_inits_noarch.py` (+30 noarch tests, suite→4438). RelativeSPSReport.summary() body: rsps_point > 1.05/> 0.95 guards, three interp strings, sig_label ternary, "Relative SPS Analysis" header, "rSPS point estimate"/"rSPS 95% CI (delta-method)" lines, "\n".join(lines) return. AdversarialSPSEstimator.__init__: self.emb_estimator/adv_estimator/config=config or emb_estimator.config, param annotations, class+factory declarations, factory return+default. RelativeSPSEstimator.__init__: self.t_estimator/arb_estimator/config=config or t_estimator.config, param annotations, class+factory declarations, factory return+t_estimator build.
- **NeuroVoice**: `tests/test_emg_stream_source_noarch.py` (+30 noarch tests, suite→6141). EMGStream.__init__: n_channels=4/sample_rate=1000/buffer_ms=200 defaults, self._n_channels/._sr/._buffer_size formula/._buffer np.zeros/._write_pos=0/._seq=0. receive_packet: decode call, drop guard, seq update, min(n_samples, buffer_size-write_pos), circular buffer write slice, write_pos increment, full-buffer copy. _decode_packet: len(raw)<6 guard, struct.unpack("<HHH"), expected_data_bytes=n_ch*n_samp*2, np.frombuffer int16, float32/32768.0, reshape, EMGPacket return. reset: buffer.fill(0)/write_pos=0/seq=0.

## [14:08 UTC] Session — satellite R70

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_sps_utils_source_noarch.py` (+30 noarch tests, suite→4468). set_seed body: random.seed/np.random.seed/torch.manual_seed/torch.cuda.manual_seed_all/torch.backends.cudnn.deterministic=True/logger.debug format. cosine_divergence body: a_n=F.normalize(a,dim=-1)/b_n=F.normalize(b,dim=-1)/return 1.0-(a_n*b_n).sum(dim=-1).clamp(-1.0,1.0). l2_divergence body: return torch.norm(a-b,dim=-1). get_divergence_fn body: match name/case "cosine": return cosine_divergence/case "l2": return l2_divergence/case _: raise ValueError. normalize_directions body: flat=v.flatten(start_dim=1)/norm=flat.norm(dim=1,keepdim=True).clamp(min=eps)/return (flat/norm).view_as(v). batch_dot body: return (a.flatten(start_dim=1)*b.flatten(start_dim=1)).sum(dim=1). DivergenceType/DivergenceFn type aliases.
- **NeuroVoice**: `tests/test_vocabulary_tiers_source_noarch.py` (+30 noarch tests, suite→6171). VocabularyTier.vocab_size_cap body: caps dict with TIER_1/TIER_2/TIER_3 keys, TIER_3:None entry, return caps[self]. uses_hard_trie_constraint: return self==VocabularyTier.TIER_1. uses_soft_trie_bias: return self==VocabularyTier.TIER_2. UserVocabularyProfile.record_utterance body: total_utterances+=1, _compute_tier call, graduation guard, logger.info, tier assignment, return True/False. add_custom_word body: word.lower().strip(), not-in guard, append. expand_vocabulary_sonar body: shape validation guards, query_norm/cand_norm linalg formulas, similarities=query_norm@cand_norm.T, np.argpartition top-k, cap at TIER_2_VOCAB_SIZE. _compute_tier branch conditions.

## [15:09 UTC] Session — satellite R71

No new R&D this session (satellite-only).
- **SPS**: `tests/test_metrics_sps_report_source_noarch.py` (+30 noarch tests, suite→4498). SPSReport.summary() body source-expression pass: `"=" * 60` separator, `"  Structured Perturbation Stability (SPS) Report"` header, `f"  SPS_eps(f)          : {self.sps:.4f}"` f-string, `f"  Mean Sensitivity    : {self.mean_sensitivity:.4f}"`, `f"  Std  Sensitivity    : {self.std_sensitivity:.4f}"`, `f"  N Samples           : {self.n_samples}"`, `"> 1 (semantic > arbitrary stability)"` / `"= 1 (indistinguishable)"` / `"< 1 (FAILURE: higher semantic sensitivity)"` interpretation strings, `return "\n".join(lines)` return. rSPS/gap/layerwise branches: `f"  rSPS (Def 8)        : ..."`, `_, lo, hi = self.rsps_ci`, `f"  rSPS 95% CI (δ-meth): ..."`, `f"  Spectral Gap gamma-bar : ..."`, `_, lo, hi = self.gap_ci`, `f"  Gap 95% CI (boot)   : ..."`, `"  ".join(...)` for profile_str, `f"L{i}:{v:.3f}"`, `f"  Layerwise Profile   : {profile_str}"`, `lines.append("=" * 60)` closing. relative_sps() body: `if arbitrary_sps < eps:` guard, error messages, `eps: float = 1e-12` default, `return semantic_sps / arbitrary_sps`. SPSReport field annotations: sps/mean_sensitivity/std_sensitivity/n_samples/relative_sps.
- **NeuroVoice**: `tests/test_phoneme_articulatory_source_noarch.py` (+30 noarch tests, suite→6201). phoneme_articulatory.py source pass: module docstring title, articulatory dim names (jaw_aperture/lip_rounding/tongue_height/tongue_backness/spectral_tilt), references (Ladefoged & Maddieson 1996, Anumanchipalli 2019), from __future__ import annotations, ARTICULATORY_DIM = 12. ArticulatoryVector.__post_init__ source: shape guard `if self.features.shape != (ARTICULATORY_DIM,):`, f-string error messages, `not (np.all(self.features >= 0.0) and np.all(self.features <= 1.0))` range guard, `bad = np.where(...)[0].tolist()`, @dataclass(frozen=True), def __post_init__ declaration, repr f-string. PhonemeArticulatoryMapper methods: self._normalize = normalize, if normalize:, self._normed = self._compute_normalized(), mat = self.feature_matrix(), mean/std.clip(min=1e-8), return (mat - mean) / std, idx = list(self._inventory.keys()).index(ipa), return self._normed[idx], return av.features.copy(), shared list comprehension.
