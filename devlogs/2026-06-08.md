# metaMirage Devlog — 2026-06-08

## [03:04 UTC] Session — satellite R74

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_adversarial_sps_results_analysis_noarch.py` (+30 noarch tests, suite→4588). load_results body: with open(path) as f:/return json.load(f). format_results_table body: lines=[]/header f-string/"-"*82 separator/for name,r in results_dict.get("models",{}).items()/ci=r.get("bootstrap_ci",[None,None])/loo_sym="✓" if r.get("loo_stable",False) else "✗"/return "\n".join(lines). verify_hypotheses body: models=results_dict.get("models",{})/gap_base/gap_large/gap_deb get chains/gap_ratio=gap_base/max(gap_large,1e-12)/h1=bool(h1_lo<=gap_ratio<=h1_hi)/h2=bool(gap_deb<gap_base)/h3=bool(all(m.get("loo_stable",False) for m in models.values()))/n_pass=sum([h1,h2,h3])/return dict. build_summary body: hyps=verify_hypotheses call/"H1":hyps["h1"]/"H2":hyps["h2"]/"H3":hyps["h3"]/"n_pass":hyps["n_pass"]/"gap_ratio_roberta_base_large":float(hyps["gap_ratio"])/run_config guard+assign.
- **NeuroVoice**: `tests/test_code_switch_source_noarch.py` (+30 noarch tests, suite→6291). CodeSwitchResult dataclass: @dataclasses.dataclass(slots=True)/is_code_switched:bool/segments:list[tuple[str,int,int]]/confidence:float. Constants: MIN_WINDOW_WORDS:int=3/CONFIDENCE_DELTA_THRESHOLD:float=0.4. CodeSwitchDetector.__init__: self._model_path=model_path/self._user_priors=user_priors or {}/self._window_size=window_size/self._model=None. detect() short path: if self._model is None/raise RuntimeError/words=text.split()/len(words)<self._window_size guard/predict_language call/CodeSwitchResult(is_code_switched=False,...). detect() sliding window: window_langs:list[tuple[str,float]]=[]/for i in range(len(words)-self._window_size+1)/window_text=" ".join/predict_language_with_conf/window_langs.append/delta=abs formula/prior=self._user_priors.get/adjusted_threshold=CONFIDENCE_DELTA_THRESHOLD*(1-prior*0.5)/char_pos=len(" ".join(words[:i]))+1/return labels[0].replace("__label__","").
