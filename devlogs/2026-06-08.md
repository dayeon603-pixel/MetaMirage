# metaMirage Devlog — 2026-06-08

## [03:04 UTC] Session — satellite R74

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_adversarial_sps_results_analysis_noarch.py` (+30 noarch tests, suite→4588). load_results body: with open(path) as f:/return json.load(f). format_results_table body: lines=[]/header f-string/"-"*82 separator/for name,r in results_dict.get("models",{}).items()/ci=r.get("bootstrap_ci",[None,None])/loo_sym="✓" if r.get("loo_stable",False) else "✗"/return "\n".join(lines). verify_hypotheses body: models=results_dict.get("models",{})/gap_base/gap_large/gap_deb get chains/gap_ratio=gap_base/max(gap_large,1e-12)/h1=bool(h1_lo<=gap_ratio<=h1_hi)/h2=bool(gap_deb<gap_base)/h3=bool(all(m.get("loo_stable",False) for m in models.values()))/n_pass=sum([h1,h2,h3])/return dict. build_summary body: hyps=verify_hypotheses call/"H1":hyps["h1"]/"H2":hyps["h2"]/"H3":hyps["h3"]/"n_pass":hyps["n_pass"]/"gap_ratio_roberta_base_large":float(hyps["gap_ratio"])/run_config guard+assign.
- **NeuroVoice**: `tests/test_code_switch_source_noarch.py` (+30 noarch tests, suite→6291). CodeSwitchResult dataclass: @dataclasses.dataclass(slots=True)/is_code_switched:bool/segments:list[tuple[str,int,int]]/confidence:float. Constants: MIN_WINDOW_WORDS:int=3/CONFIDENCE_DELTA_THRESHOLD:float=0.4. CodeSwitchDetector.__init__: self._model_path=model_path/self._user_priors=user_priors or {}/self._window_size=window_size/self._model=None. detect() short path: if self._model is None/raise RuntimeError/words=text.split()/len(words)<self._window_size guard/predict_language call/CodeSwitchResult(is_code_switched=False,...). detect() sliding window: window_langs:list[tuple[str,float]]=[]/for i in range(len(words)-self._window_size+1)/window_text=" ".join/predict_language_with_conf/window_langs.append/delta=abs formula/prior=self._user_priors.get/adjusted_threshold=CONFIDENCE_DELTA_THRESHOLD*(1-prior*0.5)/char_pos=len(" ".join(words[:i]))+1/return labels[0].replace("__label__","").

## [04:04 UTC] Session — satellite R75

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_cross_arch_adversarial_noarch.py` (+30 noarch tests, suite→4618). Module constants: TEST_SENTENCES:list[str]=[]/ARCHITECTURES=[/"roberta-base"/"microsoft/deberta-v3-base". prepare_batches: batches:list[dict]=[]/for i in range(0,len(sentences),batch_size)/chunk=sentences[i:i+batch_size]/enc=tokenizer(/embeddings=embedding_layer(input_ids)/batches.append({...}). collect_spectral_gaps: gaps:list[float]=[]/from torch.nn.attention import SDPBackend,sdpa_kernel as _sdpa_kernel/with _sdpa_kernel(SDPBackend.MATH):/model(inputs_embeds=emb,attention_mask=mask).last_hidden_state[:,0,:]/for batch in batches/dirs=t_emb.semantic_directions(emb,ids)/result=spectral_gap(_make_fn(mask),emb,dirs,n_probe_full=16)/gaps.extend(result.normalized_gap.tolist()). ArchResult(NamedTuple)/per_sample_gaps:list[float]. validate_hypotheses: by_name={r.name:r for r in results}/rb=by_name.get("roberta-base")/ratio=rb.report.adv_gap_ratio/h1_pass=0.82<=ratio<=0.95/rb_ratio/deb_ratio/h2_pass=deb_ratio<rb_ratio. serialise_results: "models":{/"spectral_gap":arch_result.gap_report["mean"]/"sps_emb":arch_result.report.sps_emb.
- **NeuroVoice**: `tests/test_decoder_heads_source_noarch.py` (+30 noarch tests, suite→6321). GeneratedOutput dataclass: @dataclasses.dataclass(slots=True)/target_language:str/audio_bytes:bytes|None/tts_latency_ms:int/generation_confidence:float. DecoderHead.__init__: self.language=language/self._model_path=model_path/self._session:ort.InferenceSession|None=None. load(): import onnxruntime as ort/ort.InferenceSession(/providers=["CPUExecutionProvider"]/size_mb=self._model_path.stat().st_size/(1024*1024)/logger.info loaded. is_loaded: return self._session is not None. generate(): if self._session is None/msg=f"Decoder head [{self.language}] not loaded."/raise RuntimeError(msg)/input_data=semantic_vector.reshape(1,-1)/result=self._session.run(None,{"semantic_input":input_data})/return str(result[0]). DecoderHeadManager.__init__: self._lang_packs_dir=lang_packs_dir/self._max_loaded=max_loaded/self._heads:dict[str,DecoderHead]={}/self._load_order:list[str]=[]. get_head(): if language in self._heads and self._heads[language].is_loaded/self._load_order.remove(language)/self._load_order.append(language)/model_path=self._lang_packs_dir/language/"decoder_head.onnx"/while len(self._load_order)>=self._max_loaded/evict_lang=self._load_order.pop(0).
