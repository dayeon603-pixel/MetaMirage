# metaMirage Devlog — 2026-06-08

## [03:04 UTC] Session — satellite R74

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_adversarial_sps_results_analysis_noarch.py` (+30 noarch tests, suite→4588). load_results body: with open(path) as f:/return json.load(f). format_results_table body: lines=[]/header f-string/"-"*82 separator/for name,r in results_dict.get("models",{}).items()/ci=r.get("bootstrap_ci",[None,None])/loo_sym="✓" if r.get("loo_stable",False) else "✗"/return "\n".join(lines). verify_hypotheses body: models=results_dict.get("models",{})/gap_base/gap_large/gap_deb get chains/gap_ratio=gap_base/max(gap_large,1e-12)/h1=bool(h1_lo<=gap_ratio<=h1_hi)/h2=bool(gap_deb<gap_base)/h3=bool(all(m.get("loo_stable",False) for m in models.values()))/n_pass=sum([h1,h2,h3])/return dict. build_summary body: hyps=verify_hypotheses call/"H1":hyps["h1"]/"H2":hyps["h2"]/"H3":hyps["h3"]/"n_pass":hyps["n_pass"]/"gap_ratio_roberta_base_large":float(hyps["gap_ratio"])/run_config guard+assign.
- **NeuroVoice**: `tests/test_code_switch_source_noarch.py` (+30 noarch tests, suite→6291). CodeSwitchResult dataclass: @dataclasses.dataclass(slots=True)/is_code_switched:bool/segments:list[tuple[str,int,int]]/confidence:float. Constants: MIN_WINDOW_WORDS:int=3/CONFIDENCE_DELTA_THRESHOLD:float=0.4. CodeSwitchDetector.__init__: self._model_path=model_path/self._user_priors=user_priors or {}/self._window_size=window_size/self._model=None. detect() short path: if self._model is None/raise RuntimeError/words=text.split()/len(words)<self._window_size guard/predict_language call/CodeSwitchResult(is_code_switched=False,...). detect() sliding window: window_langs:list[tuple[str,float]]=[]/for i in range(len(words)-self._window_size+1)/window_text=" ".join/predict_language_with_conf/window_langs.append/delta=abs formula/prior=self._user_priors.get/adjusted_threshold=CONFIDENCE_DELTA_THRESHOLD*(1-prior*0.5)/char_pos=len(" ".join(words[:i]))+1/return labels[0].replace("__label__","").

## [04:04 UTC] Session — satellite R75

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_cross_arch_adversarial_noarch.py` (+30 noarch tests, suite→4618). Module constants: TEST_SENTENCES:list[str]=[]/ARCHITECTURES=[/"roberta-base"/"microsoft/deberta-v3-base". prepare_batches: batches:list[dict]=[]/for i in range(0,len(sentences),batch_size)/chunk=sentences[i:i+batch_size]/enc=tokenizer(/embeddings=embedding_layer(input_ids)/batches.append({...}). collect_spectral_gaps: gaps:list[float]=[]/from torch.nn.attention import SDPBackend,sdpa_kernel as _sdpa_kernel/with _sdpa_kernel(SDPBackend.MATH):/model(inputs_embeds=emb,attention_mask=mask).last_hidden_state[:,0,:]/for batch in batches/dirs=t_emb.semantic_directions(emb,ids)/result=spectral_gap(_make_fn(mask),emb,dirs,n_probe_full=16)/gaps.extend(result.normalized_gap.tolist()). ArchResult(NamedTuple)/per_sample_gaps:list[float]. validate_hypotheses: by_name={r.name:r for r in results}/rb=by_name.get("roberta-base")/ratio=rb.report.adv_gap_ratio/h1_pass=0.82<=ratio<=0.95/rb_ratio/deb_ratio/h2_pass=deb_ratio<rb_ratio. serialise_results: "models":{/"spectral_gap":arch_result.gap_report["mean"]/"sps_emb":arch_result.report.sps_emb.
- **NeuroVoice**: `tests/test_decoder_heads_source_noarch.py` (+30 noarch tests, suite→6321). GeneratedOutput dataclass: @dataclasses.dataclass(slots=True)/target_language:str/audio_bytes:bytes|None/tts_latency_ms:int/generation_confidence:float. DecoderHead.__init__: self.language=language/self._model_path=model_path/self._session:ort.InferenceSession|None=None. load(): import onnxruntime as ort/ort.InferenceSession(/providers=["CPUExecutionProvider"]/size_mb=self._model_path.stat().st_size/(1024*1024)/logger.info loaded. is_loaded: return self._session is not None. generate(): if self._session is None/msg=f"Decoder head [{self.language}] not loaded."/raise RuntimeError(msg)/input_data=semantic_vector.reshape(1,-1)/result=self._session.run(None,{"semantic_input":input_data})/return str(result[0]). DecoderHeadManager.__init__: self._lang_packs_dir=lang_packs_dir/self._max_loaded=max_loaded/self._heads:dict[str,DecoderHead]={}/self._load_order:list[str]=[]. get_head(): if language in self._heads and self._heads[language].is_loaded/self._load_order.remove(language)/self._load_order.append(language)/model_path=self._lang_packs_dir/language/"decoder_head.onnx"/while len(self._load_order)>=self._max_loaded/evict_lang=self._load_order.pop(0).

## [05:07 UTC] Session — satellite R76

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_estimate_sps_source_noarch.py` (+30 noarch tests, suite→4648). TEST_SENTENCES: list[str] = [ constant/prepare_batches body: batches: list[dict[str, torch.Tensor]] = []/for i in range(0, len(sentences), batch_size):/chunk = sentences[i : i + batch_size]/enc = tokenizer(/input_ids = enc["input_ids"].to(device)/attention_mask = enc["attention_mask"].to(device)/embeddings = embedding_layer(input_ids)/return batches. compute_all_spectral_gaps body: per_sample_gaps: list[float] = []/SDPBackend sdpa_kernel import/with _sdpa_kernel(SDPBackend.MATH):/return out.last_hidden_state[:, 0, :]/directions = t_emb.semantic_directions(emb, ids)/result = spectral_gap(fn, emb, directions, n_probe_full=16)/per_sample_gaps.extend(result.normalized_gap.tolist())/col_w dict/header join/sep join. parse_args+main: --epsilon default 0.1/--n-boot default 1000/model_name = "roberta-base"/arb_sps = arb_result["sps"]/rsps = relative_sps(core["sps"], arb_sps)/adv_ratio formula/rsps_ci unpack/excludes_one logic/gap_ci unpack/bar block expression.
- **NeuroVoice**: `tests/test_lang_pack_manager_source_noarch.py` (+30 noarch tests, suite→6351). LangPackManifest.__init__: self.lang_code: str = data["lang_code"]/self.bcp47: str = data["bcp47"]/self.version: str = data["version"]/self.decoder_checksum: str = data["decoder_checksum"]/self.tts_checksum: str = data["tts_checksum"]/self.quality_tier: int = data.get("quality_tier", 2)/to_dict keys: "lang_code": self.lang_code,/"bcp47": self.bcp47,/"quality_tier": self.quality_tier,/logger = logging.getLogger(__name__). LangPackManager: self._packs_dir = packs_dir/self._cdn_base_url = cdn_base_url/installed = []/for lang_dir in self._packs_dir.iterdir():/if lang_dir.is_dir() and (lang_dir / "manifest.json").exists():/installed.append(lang_dir.name)/return sorted(installed)/manifest_path = self._packs_dir / lang_code / "manifest.json"/return LangPackManifest(json.load(f)). verify_integrity+install+sha256: manifest = self.get_manifest(lang_code)/decoder_path build/tts_path build/decoder_hash = self._file_sha256(decoder_path)/decoder_hash == manifest.decoder_checksum/shutil.copy2 decoder/manifest_data["decoder_checksum"] assignment/json.dump(manifest_data, f, indent=2)/h = hashlib.sha256()/for chunk in iter(lambda: f.read(8192), b""):.

## [06:10 UTC] Session — satellite R77

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_jacobian_source_noarch.py` (+30 noarch tests, suite→4678). SpectralGapResult: @dataclass(slots=True)/restricted_norm:torch.Tensor/full_spectral_norm:torch.Tensor/normalized_gap:torch.Tensor/mean_restricted_norm:float=field(init=False)/mean_full_norm:float=field(init=False)/mean_gap:float=field(init=False)/__post_init__: self.mean_restricted_norm=self.restricted_norm.mean().item()/self.mean_full_norm=self.full_spectral_norm.mean().item()/self.mean_gap=self.normalized_gap.mean().item(). directional_derivative_norm: v_magnitude=v.flatten(start_dim=1).norm(dim=1)/v_unit=normalize_directions(v)/_,jvp_val=torch.autograd.functional.jvp(scalar_fn,x,v_unit,create_graph=False)/norms=jvp_val.flatten(start_dim=1).norm(dim=1)/return torch.where(v_magnitude>eps,norms,torch.zeros_like(norms)). restricted_operator_norm: B,K,seq_len,hidden=directions.shape/norms_per_dir:list[torch.Tensor]=[]/stacked=torch.stack(norms_per_dir,dim=1)/return stacked.max(dim=1).values. adversarial_worst_direction: worst_k=stacked.argmax(dim=1). full_spectral_norm: max_norm=torch.zeros(B,device=device)/v=torch.randn(B,seq_len,hidden,device=device)/max_norm=torch.maximum(max_norm,jvp_norm). spectral_gap: x=x.detach().requires_grad_(True)/rest_norm=restricted_operator_norm/full_norm=full_spectral_norm/normalized=(1.0-rest_norm/full_norm.clamp(min=eps)).clamp(0.0,1.0). verify_spectral_gap_bound: lower=math.exp(-(1.0-mean_gap)*lipschitz_constant)/"improvement_factor":lower/max(theorem3,1e-12). ForwardFn=Callable[[torch.Tensor],torch.Tensor].
- **NeuroVoice**: `tests/test_tone_conditioner_source_noarch.py` (+30 noarch tests, suite→6381). Module: from .phoneme_articulatory import ToneContourSegment,ARTICULATORY_DIM/__all__=["ToneConditioner"]/class ToneConditioner:/self.art_dim=art_dim. expand_segment: if n_frames<=0:/return np.zeros((0,self.art_dim),dtype=np.float32)/vecs=segment.vectors()/durs=segment.durations()/n_seg=len(vecs)/out=np.empty((n_frames,self.art_dim),dtype=np.float32)/frame_start=0/for i,(vec,dur) in enumerate(zip(vecs,durs)):/if i==n_seg-1:/frame_end=n_frames/else: frame_end=frame_start+round(dur*n_frames)/frame_end=min(frame_end,n_frames)/if frame_end>frame_start:/out[frame_start:frame_end]=vec.features.astype(np.float32)/frame_start=frame_end/return out. build_conditioning: if len(segments)!=len(syllable_spans):/out=np.zeros((T_art,self.art_dim),dtype=np.float32)/for seg,(start,end) in zip/if start<0 or end>T_art or start>=end:/if seg is None:/n_frames=end-start/out[start:end]=self.expand_segment(seg,n_frames). to_tensor: import torch/t=torch.from_numpy(np.ascontiguousarray(cond,dtype=np.float32))/t=t.unsqueeze(0).

## [07:15 UTC] Session — satellite R78

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_transformations_source_noarch.py` (+30 noarch tests, suite→4708). EmbeddingPerturbationConfig field annotations: n_directions:int=8/projection_noise_scale:float=0.02/use_synonym_directions:bool=True. EmbeddingPerturbationFamily init: self.config=config or EmbeddingPerturbationConfig(). sample body: K=directions.size(1)/idx=torch.randint(0,K,(B,),device=device)/v=directions[torch.arange(B,device=device),idx]/alpha_exp=alpha.view(B,1,1)/perturbed=embeddings+alpha_exp*v. semantic_directions: K=self.config.n_directions/if self.config.use_synonym_directions and self.synonym_map:/return self._synonym_directions/return self._random_orthogonal_directions. _synonym_directions: delta=syn_embeddings-embeddings/noise_scale=self.config.projection_noise_scale/delta=delta+noise_scale*torch.randn_like(delta)/return torch.cat(all_directions,dim=1). _random_orthogonal_directions: flat_dim=seq_len*hidden/raw=torch.randn(B,K,flat_dim,device=device)/ortho=torch.zeros_like(raw). SynonymSubstitutionFamily init: if not synonym_map. sample: perturbed_ids=input_ids.clone()/n_substituted=0/positions=torch.randperm(seq_len).tolist()/valid=magnitudes<=epsilon/magnitudes=magnitudes*valid.float(). AdversarialEmbeddingFamily init: self.forward_fn=forward_fn/self._t_emb=EmbeddingPerturbationFamily. sample: from sps.jacobian import adversarial_worst_direction/v_star=normalize_directions(v_star.detach().flatten(start_dim=1)).view_as(embeddings).
- **NeuroVoice**: `tests/test_vocabulary_inventory_source_noarch.py` (+30 noarch tests, suite→6411). Constants: SONAR_DIM:int=1024/ALIGNMENT_SIMILARITY_THRESHOLD:float=0.75/PENDING_MARKER:str="pending". ConceptEntry fields: concept_id:int/concept_name:str/translations:dict[str,list[str]]/sonar_vector:NDArray[np.float32]|None=field(default=None,repr=False). get_primary: forms=self.translations.get(language)/primary=forms[0]/if primary==PENDING_MARKER. from_dict: required-field guard/translations get/"translations":{}/isinstance check/sonar_raw get/np.array(sonar_raw,dtype=np.float32)/shape!=(SONAR_DIM,) guard/int(data["concept_id"])/dict comp/bool(data.get("nsm_prime",False)). to_dict: result["sonar_vector"]=self.sonar_vector.tolist(). VocabularyInventory: self._vocab_path:Path/self._by_category:dict[str,list[int]]={}/self._reverse_index_built:bool=False/open(encoding="utf-8")/setdefault(entry.category,[]).append/lang_index=setdefault(lang,{})/lang_index[form.lower()]=concept.concept_id/self._reverse_index_built=True/cid=lang_index.get(text.lower())/report["overall"]=total_covered/total_concepts.

## [08:06 UTC] Session — satellite R79

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_core_relative_sps_source_noarch.py` (+30 noarch tests, suite→4738). RelativeSPSReport.summary body: "T MORE stable than T_arb (semantic invariance present)"/"T ≈ T_arb (no significant semantic structure benefit)"/"T LESS stable than T_arb"/rsps_point>1.05/rsps_point>0.95/sig_label conditional/header "Relative SPS Analysis (rSPS=SPS_T/SPS_arb)"/SPS_T line/rSPS point estimate line/rSPS 95% CI delta-method line. RelativeSPSEstimator.__init__: self.t_estimator=t_estimator/self.arb_estimator=arb_estimator/self.config=config or t_estimator.config/t_estimator:SPSEstimator/arb_estimator:SPSEstimator. _collect_sensitivities: all_sens:list[torch.Tensor]=[]/input_ids=batch["input_ids"].to(config.device)/"embeddings" in batch guard/all_sens.append(sens.cpu())/return torch.cat(all_sens).tolist(). estimate: from sps.stats import bootstrap_ci,delta_method_rsps_ci,loo_spectral_gap/t_result=self.t_estimator.estimate(iter(batches))/sps_t=t_result["sps"]/arb_result=self.arb_estimator.estimate(iter(batches))/sps_arb=arb_result["sps"]/rsps_point,rsps_lo,rsps_hi=delta_method_rsps_ci(/emb_mean_s=mean_sens_t/rsps_ci=(rsps_point,rsps_lo,rsps_hi)/rsps_ci_excludes_one=rsps_lo>1.0 or rsps_hi<1.0/t_sens_list=self._collect_sensitivities(self.t_estimator,batches).
- **NeuroVoice**: `tests/test_lang_registry_source_noarch.py` (+30 noarch tests, suite→6441). QualityTier: class QualityTier(IntEnum)/VALIDATED=1/AUTO=2/COMMUNITY=3. LanguageInfo: @dataclasses.dataclass(slots=True,frozen=True)/code:str/bcp47:str/tier:QualityTier/has_impaired_data:bool/seamless_s2s:bool=False. LANGUAGE_REGISTRY: dict[str,LanguageInfo]={/"ko":LanguageInfo(/code="ko",bcp47="ko-KR",name="Korean"/tier=QualityTier.VALIDATED/has_impaired_data=True/family="African"/decoder_size_mb:float/tts_size_mb:float/seamless_s2t:bool=True/sonar_supported:bool=True. Accessors: get_mvp_languages/get_languages_by_tier/get_languages_by_family/get_seamless_s2s_languages list comp bodies/total=0.0/return sorted(LANGUAGE_REGISTRY.keys())/return sorted({lang.family for lang in LANGUAGE_REGISTRY.values()}).

## [09:06 UTC] Session — satellite R80

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_sps_init_source_noarch.py` (+30 noarch tests, suite→4768). sps/__init__.py source-expression pass: from sps.core import block/from sps.jacobian import block/from sps.metrics import block/from sps.transformations import block (AdversarialEmbeddingConfig/SynonymSubstitutionConfig/TransformationFamily)/from sps.stats import block (permutation_test_gap_diff/compare_architecture_cis/delta_method_rsps_ci)/from sps.utils import block (cosine_divergence/get_divergence_fn/normalize_directions)/__all__ list entries (# core/# stats section comments/"AdversarialSPSEstimator"/"permutation_test_gap_diff").
- **NeuroVoice**: `tests/test_emg_calibration_source_noarch.py` (+30 noarch tests, suite→6471). src/emg/calibration.py source-expression pass: MIN_REPS_PER_WORD=5/RECOMMENDED_REPS=10/CalibrationSample (4 fields)/CalibrationProgress (target_vocabulary/samples_per_word/min_reps/recommended_reps fields, is_minimum_met count>=self.min_reps, is_recommended_met count>=self.recommended_reps, total_samples sum, next_word incomplete dict comp + min return, status_message literals)/EMGCalibrator.__init__ (5 self assignments + mkdir)/add_sample (word guard/sample_path f-string/np.save)/_save_meta ("total_samples" key)/get_training_data (emg_data list comp).

## [10:08 UTC] Session — satellite R81

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_sps_core_spsconfig_sse_estimate_source_noarch.py` (+30 noarch tests, suite→4798). SPSConfig source-expression pass: class SPSConfig(BaseModel)/from pydantic import BaseModel,Field,field_validator/epsilon:float=Field(0.1,gt=0.0,description="Perturbation radius epsilon.")/n_data_samples:int=Field(512,ge=1)/m_transforms:int=Field(32,ge=1,description="Transformations sampled per input.")/divergence:DivergenceType="cosine"/seed:int=42/batch_size:int=Field(32,ge=1)/@field_validator("device")/device:str="cpu". SSE.estimate() body: embeddings/input_ids/attention_mask .to(self.device)/B=embeddings.size(0)/fx=self._forward/max_ratios=torch.zeros/for _ in range(m):/valid=cx>1e-12/div=self.divergence_fn(fx,ftx)/max_ratios=torch.maximum. _forward body: hasattr last_hidden_state/return [:, 0, :]/isinstance Tensor/raise TypeError. _get_embeddings body: getattr get_input_embeddings/attr loop ("roberta","bert","transformer","model")/getattr embeddings sub/getattr word_embeddings. build_sps_estimator: config or SPSConfig()/get_divergence_fn(config.divergence).
- **NeuroVoice**: `tests/test_beam_decoder_source_noarch.py` (+30 noarch tests, suite→6501). src/emg/beam_decoder.py source-expression pass: CTC_BLANK_ID:int=0/DEFAULT_BEAM_WIDTH:int=50/DEFAULT_LM_WEIGHT:float=0.3/NEG_INF:float=-1e30. VocabularyTrie: self._words={w.lower() for w in words}/self._marisa=None/self._dict_trie:dict[str,object]={}/node.setdefault(ch,{})/node["$"]=True/return word.lower() in self._words. BeamHypothesis: @dataclass(slots=True)/token_ids:list[int]/ctc_score:float/combined_score:float/confidence:float/n_frames:int. _BeamState: p_blank:float=NEG_INF/p_nonblank:float=NEG_INF/return _log_add(self.p_blank,self.p_nonblank). _log_add: return a+math.log1p(math.exp(b-a)). MultilingualBeamDecoder.__init__: self._id_to_char/self._beam_width/self._lm_weight/self._language_models or {}. decode: lm=self._language_models.get/blank_lp=frame_log_probs[CTC_BLANK_ID]/new_p_blank=_log_add(state.p_blank,state.p_nonblank)+blank_lp/confidence=float(np.exp(combined-best_combined)) if rank>0 else 1.0. _prefix_to_text: chars:list[str]=[]. _is_word_boundary: return last_char==" ".

## [11:09 UTC] Session — satellite R82

No new metaMirage R&D this session (satellite-only; deadline April 16, 2026 passed).
- **SPS**: `tests/test_adversarial_estimator_and_factories_source_noarch.py` (+30 noarch tests, suite→4828). AdversarialSPSEstimator.__init__ body: self.emb_estimator=emb_estimator/self.adv_estimator=adv_estimator/self.config=config or emb_estimator.config/emb_estimator:SPSEstimator/adv_estimator:SPSEstimator param annotations. _collect_sensitivities body: config=estimator.config/all_sens:list[torch.Tensor]=[]/'"embeddings" in batch' guard/estimator._get_embeddings(input_ids) fallback/m=config.m_transforms kwarg. estimate() body: from sps.stats import bootstrap_ci,loo_spectral_gap/emb_result=self.emb_estimator.estimate(iter(batches))/sps_emb=emb_result["sps"]/adv_result=self.adv_estimator.estimate(iter(batches))/sps_adv=adv_result["sps"]/adv_gap_ratio=round(sps_adv/max(sps_emb,1e-12),6)/_collect_sensitivities emb+adv calls/emb_sens_ci=bootstrap_ci(emb_sens_list)/loo_emb=loo_spectral_gap(emb_sens_list). build_adversarial_sps_estimator body: config=config or SPSConfig()/emb_estimator=build_sps_estimator(model,emb_family,config)/adv_estimator=build_sps_estimator(model,adv_family,config)/return AdversarialSPSEstimator(emb_estimator,adv_estimator,config). build_relative_sps_estimator body: config=config or SPSConfig()/t_estimator=build_sps_estimator(model,t_family,config)/arb_estimator=build_sps_estimator(model,arb_family,config)/t_family:TransformationFamily/arb_family:TransformationFamily/return RelativeSPSEstimator(t_estimator,arb_estimator,config).
- **NeuroVoice**: `tests/test_emg_encoder_source_noarch.py` (+30 noarch tests, suite→6531). Module constants: ARTICULATORY_VOCAB_SIZE:int=64/CTC_BLANK_TOKEN_ID:int=0/CNN_STRIDE:int=2/D_MODEL:int=256/SONAR_DIM:int=1024. ArticulatoryVocab: vocab_size:int=ARTICULATORY_VOCAB_SIZE/return self.vocab_size+1 (n_ctc_tokens)/dists=np.linalg.norm(self.centroids-feature_vec,axis=1)/return int(np.argmin(dists))+1/if loaded.shape[0]!=self.vocab_size guard. _CNNFrontend: self.channel_dropout_p=channel_dropout_p/widths=[n_channels,d_model//4,d_model//2,d_model]/if self.training and self.channel_dropout_p>0.0 guard/return self.net(x). ContinuousEMGEncoder.__init__: self.d_model=d_model/self._max_seq_len=max_seq_len/self.register_buffer(/encoder_layer=nn.TransformerEncoderLayer(/pe[:,0::2]=torch.sin(position*div_term)/pe[:,1::2]=torch.cos(position*div_term). forward: cnn_out=self.cnn(emg)/x=cnn_out.transpose(1,2)/if T_sub>self._max_seq_len/x=x+pos_enc[:,:T_sub,:]/out:torch.Tensor=self.output_norm(x). CTCHead: self.linear=nn.Linear(d_model,vocab.n_ctc_tokens)/log_probs:torch.Tensor=F.log_softmax(logits,dim=-1). EMGBaselineClassifier: self._sonar_lookup=sonar_lookup or {}/class_idx=int(self._model.predict(features)[0]). EMGEncoder=ContinuousEMGEncoder alias.
