# metaMirage Devlog — 2026-06-13

## [00:13 UTC] Session — R174 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R174
- `tests/test_sps_core_source_10_noarch.py` (+30, suite 7,558 → 7,588).
- 10th source-expression pass for `src/sps/core.py`:
  `mean_sens_emb = emb_result["mean_sensitivity"]`, `std_sens_emb = emb_result["std_sensitivity"]`,
  `n_samples = emb_result["n_samples"]`, `mean_sens_adv = adv_result["mean_sensitivity"]`,
  `std_sens_adv = adv_result["std_sensitivity"]`,
  `adv_sens_list = self._collect_sensitivities(self.adv_estimator, batches)`,
  `emb_sens_ci = bootstrap_ci(emb_sens_list)`, `adv_sens_ci = bootstrap_ci(adv_sens_list)`,
  `loo_emb = loo_spectral_gap(emb_sens_list)`, `loo_adv = loo_spectral_gap(adv_sens_list)`,
  `"AdversarialSPS: sps_emb=%.4f  sps_adv=%.4f  gap_ratio=%.4f  n=%d"`,
  `adv_gap_ratio=adv_gap_ratio,`, `mean_sens_emb=mean_sens_emb,`, `mean_sens_adv=mean_sens_adv,`,
  `emb_sens_ci=emb_sens_ci,`, `adv_sens_ci=adv_sens_ci,`, `loo_emb=loo_emb,`, `loo_adv=loo_adv,`,
  `all_sens: list[torch.Tensor] = []`, `all_sens.append(sens.cpu())`,
  `sps_t: float`, `sps_arb: float`, `rsps_point: float`, `rsps_ci: tuple`,
  `rsps_ci_excludes_one: bool`, `mean_sens_t: float`, `n_samples_t: int`,
  `n_samples_arb: int`, `sens_ci_t: tuple`, `loo_t: dict`.

### NeuroVoice R174
- `tests/test_emg_features_source_3_noarch.py` (+30, suite 9,261 → 9,291).
- 3rd source-expression pass for `src/emg/features.py`:
  `from __future__ import annotations`, `import logging`, `from typing import TYPE_CHECKING`,
  `import numpy as np`, `if TYPE_CHECKING:`, `from numpy.typing import NDArray`,
  `logger = logging.getLogger(__name__)`, `__init__`/`extract`/`_handcrafted_features` signatures,
  `if mode not in ("raw", "handcrafted"):`, error msg f-string, `raise ValueError(msg)`,
  `self._mode = mode`, `self._n_fft = n_fft`, `if self._mode == "raw":`, `return segment`,
  `return self._handcrafted_features(segment)`, `features_per_ch: list[NDArray[np.float32]] = []`,
  `for ch in range(segment.shape[0]):`, `x = segment[ch]`, `n = len(x)`,
  `rms = np.sqrt(np.mean(x ** 2))`, `mav = np.mean(np.abs(x))`,
  `zcr = np.sum(np.abs(np.diff(np.sign(x)))) / (2 * n)`, `wl = np.sum(np.abs(np.diff(x)))`,
  `fft_mag = np.abs(np.fft.rfft(x, n=self._n_fft))`,
  `freqs = np.fft.rfftfreq(self._n_fft, d=1.0 / 1000)  # 1000 Hz SR`,
  `total_power = np.sum(power) + 1e-10`, `return np.concatenate(features_per_ch)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,588 | +30 |
| NeuroVoice | 9,291 | +30 |
| Combined | 16,879 | +60 |
| metaMirage | post-deadline | — |

---

## [01:21 UTC] Session — R175 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R175
- `tests/test_sps_core_source_11_noarch.py` (+30, suite 7,588 → 7,618).
- 11th source-expression pass for `src/sps/core.py` (RelativeSPSEstimator):
  `from sps.stats import bootstrap_ci, delta_method_rsps_ci, loo_spectral_gap`,
  `t_result = self.t_estimator.estimate(iter(batches))`,
  `mean_sens_t = t_result["mean_sensitivity"]`, `std_sens_t = t_result["std_sensitivity"]`,
  `n_samples_t = t_result["n_samples"]`,
  `arb_result = self.arb_estimator.estimate(iter(batches))`,
  `mean_sens_arb = arb_result["mean_sensitivity"]`,
  `std_sens_arb = arb_result["std_sensitivity"]`,
  `n_samples_arb = arb_result["n_samples"]`,
  `rsps_ci = (rsps_point, rsps_lo, rsps_hi)`,
  `t_sens_list = self._collect_sensitivities(self.t_estimator, batches)`,
  `sens_ci_t = bootstrap_ci(t_sens_list, seed=self.config.seed)`,
  `loo_t = loo_spectral_gap(t_sens_list)`,
  logger.info format strings + args, `rsps_point=rsps_point,`, `rsps_ci=rsps_ci,`,
  `sens_ci_t=sens_ci_t,`, summary() guard branches, interp assignments (all 3),
  sig_label conditional, header/SPS_T/rSPS f-strings, `return "\n".join(lines)`.

### NeuroVoice R175
- `tests/test_emg_encoder_source_11_noarch.py` (+30, suite 9,291 → 9,321).
- 11th source-expression pass for `src/emg/encoder.py`:
  `channel_dropout_p: float = 0.2,`, `conv_dropout: float = 0.1,`,
  `transformer_dropout: float = 0.1,`, `super().__init__()`, `layers += [`,
  `nn.Conv1d(`, `d_model=d_model,`, `channel_dropout_p=channel_dropout_p,`,
  `conv_dropout=conv_dropout,`, `activation="gelu",`,
  `self.transformer = nn.TransformerEncoder(`, `encoder_layer,`, `return out`,
  f-string for subsampled-length error, `@staticmethod`,
  `def _build_sinusoidal_pe(max_len: int, d_model: int) -> torch.Tensor:`,
  `vocab: ArticulatoryVocab | None = None,`, `d_model: int = D_MODEL,`,
  `from __future__ import annotations`, `import torch`, `import torch.nn as nn`,
  `import logging`, `from typing import TYPE_CHECKING`, `D_MODEL: int = 256`,
  `labels: NDArray[np.int64],`, `len(features),`, RuntimeError/logger.warning
  for predict, logger.info for load_centroids, `loaded: NDArray[np.float32] = np.load(...)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,618 | +30 |
| NeuroVoice | 9,321 | +30 |
| Combined | 16,939 | +60 |
| metaMirage | post-deadline | — |

---

## [02:34 UTC] Session — R176 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R176
- `tests/test_sps_core_source_12_noarch.py` (+30, suite 7,618 → 7,648).
- 12th (final) source-expression pass for `src/sps/core.py`:
  `"  Relative SPS Analysis  (rSPS = SPS_T / SPS_arb)",`,
  `f"    mean_sensitivity          : {self.mean_sens_t:.4f} ± {self.std_sens_t:.4f}",`,
  `f"    95% CI (bootstrap)        : [{self.sens_ci_t[1]:.4f}, {self.sens_ci_t[2]:.4f}]",`,
  `f"    LOO stable                : {self.loo_t.get('stable', 'N/A')}",`,
  `f"    n_samples                 : {self.n_samples_t}",`,
  `f"  SPS_arb (arbitrary T_arb)  : {self.sps_arb:.4f}",`,
  `f"    mean_sensitivity          : {self.mean_sens_arb:.4f} ± {self.std_sens_arb:.4f}",`,
  `f"    n_samples                 : {self.n_samples_arb}",`,
  `f"  rSPS 95% CI (delta-method)  : [{self.rsps_ci[1]:.4f}, {self.rsps_ci[2]:.4f}]",`,
  `f"  Significant (CI ≠ 1)        : {sig_label}",`,
  `std_sens_t=std_sens_t,`, `n_samples_t=n_samples_t,`, `mean_sens_arb=mean_sens_arb,`,
  `std_sens_arb=std_sens_arb,`, `n_samples_arb=n_samples_arb,`, `loo_t=loo_t,`,
  `config=self.config,`, t_family/arb_family docstring lines,
  `Fully configured RelativeSPSEstimator.`,
  `t_estimator = build_sps_estimator(model, t_family, config)`,
  `arb_estimator = build_sps_estimator(model, arb_family, config)`,
  `return RelativeSPSEstimator(t_estimator, arb_estimator, config)`,
  example block lines, `"""Re-run sensitivity estimation and collect per-sample values as list."""`,
  `return torch.cat(all_sens).tolist()`, `>>> print(report.summary())`.
  core.py is now exhausted (12 passes × 30 = 360 tests).

### NeuroVoice R176
- `tests/test_emg_features_source_4_noarch.py` (+30, suite 9,321 → 9,351).
- 4th source pass for features.py + 12th source pass for encoder.py (combined file):
  ssc computation (`np.sum(` / inner diff-sign / divisor), `# Spectral features`,
  `power = fft_mag ** 2`, mean_freq / cumulative / med_idx / median_freq assignments,
  `[rms, mav, zcr, wl, ssc, mean_freq, median_freq],`, features_per_ch.append /
  np.array / dtype=np.float32 / `# Temporal features`;
  encoder.py: `vocab_size: int = ARTICULATORY_VOCAB_SIZE`,
  `centroids: NDArray[np.float32] | None = None`,
  `cluster_labels: list[str] = field(default_factory=list)`,
  `ARTICULATORY_VOCAB_SIZE: int = 64`, `CTC_BLANK_TOKEN_ID: int = 0`, `SONAR_DIM: int = 1024`,
  `@property`, `def n_ctc_tokens(self) -> int:`,
  `"""Total tokens passed to CTCLoss: vocab_size + 1 blank."""`,
  `return self.vocab_size + 1`, `CNN_STRIDE: int = 2`, `CNN_LAYERS: int = 3`,
  `N_TRANSFORMER_LAYERS: int = 4`, `N_ATTENTION_HEADS: int = 4`,
  `EMGEncoder = ContinuousEMGEncoder`,
  `# Keep the old class name importable so existing code (pipeline, serving) doesn't break.`.
  features.py and encoder.py are now both exhausted.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,648 | +30 |
| NeuroVoice | 9,351 | +30 |
| Combined | 16,999 | +60 |
| metaMirage | post-deadline | — |

---

## [03:47 UTC] Session — R177 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R177
- `tests/test_sps_stats_source_1_noarch.py` (+30, suite 7,648 → 7,678).
- 1st source-expression pass for `src/sps/stats.py`:
  `n = len(values)`, `if n == 0:`, `return 0.0, 0.0, 0.0`, `rng = random.Random(seed)`,
  `boot_means = sorted(`, `sum(rng.choices(values, k=n)) / n for _ in range(n_boot)`,
  `lo_idx = int((alpha / 2) * n_boot)`, `hi_idx = int((1 - alpha / 2) * n_boot)`,
  `round(sum(values) / n, 6),`, `round(boot_means[lo_idx], 6),`,
  `n = len(per_sample_gaps)`, `if n < 3:`, `return {"stable": False, "n": n}`,
  `total = sum(per_sample_gaps)`,
  `loo_means = [(total - g) / (n - 1) for g in per_sample_gaps]`,
  `"loo_range": round(max(loo_means) - min(loo_means), 6),`,
  `"stable": (max(loo_means) - min(loo_means)) < 0.05,`,
  `if abs(alpha - 0.05) < 1e-9:`, `z = 1.96`, `log_rsps = arb_mean_s - emb_mean_s`,
  `var_log = (emb_std_s ** 2) / max(emb_n, 1) + (arb_std_s ** 2) / max(arb_n, 1)`,
  `se_log = math.sqrt(var_log)`, `rsps_point = math.exp(log_rsps)`,
  `rsps_lo = math.exp(log_rsps - z * se_log)`, `rsps_hi = math.exp(log_rsps + z * se_log)`,
  `return round(rsps_point, 6), round(rsps_lo, 6), round(rsps_hi, 6)`,
  `pooled = gaps_a + gaps_b`,
  `p_value = (extreme + 1) / (n_perm + 1)  # +1 smoothing avoids p=0`,
  `"observed_diff": round(obs_diff, 6),`, `"p_value": round(p_value, 6),`.

### NeuroVoice R177
- `tests/test_emg_preprocessing_source_1_noarch.py` (+30, suite 9,351 → 9,381).
- 1st source-expression pass for `src/emg/preprocessing.py`:
  `SAMPLE_RATE: int = 1000`, `BANDPASS_LOW: float = 20.0`, `BANDPASS_HIGH: float = 499.0`,
  `NOTCH_FREQ: float = 60.0`, `SEGMENT_MS: int = 200`, `BUFFER_SECONDS: float = 10.0`,
  `WINDOW_MS: int = 200`, `HOP_MS: int = 50`, `ONSET_ENERGY_THRESHOLD: float = 3.0`,
  `ONSET_HOLD_MS: int = 100`,
  `@dataclasses.dataclass(slots=True)`,
  `data: NDArray[np.float32]  # shape (n_channels, n_samples)`,
  `n_channels: int`, `sample_rate: int`, `timestamp_ms: int`, `user_id: str`,
  `start_ms: int`, `end_ms: int   # -1 if offset not yet detected`,
  `peak_rms: float`, `channel: int  # channel with maximum energy`,
  `self._sr = sample_rate`, `self._n_channels = n_channels`, `self._notch_freq = notch_freq`,
  `sos = butter(4, [low, high], btype="bandpass", fs=self._sr, output="sos")`,
  `filtered = np.zeros_like(signal)`, `for ch in range(signal.shape[0]):`,
  `filtered[ch] = sosfiltfilt(sos, signal[ch]).astype(np.float32)`,
  `b, a = iirnotch(self._notch_freq, Q=30.0, fs=self._sr)`,
  `filtered[ch] = filtfilt(b, a, signal[ch]).astype(np.float32)`,
  `std = np.maximum(std, 1e-8)  # avoid division by zero`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,678 | +30 |
| NeuroVoice | 9,381 | +30 |
| Combined | 17,059 | +60 |
| metaMirage | post-deadline | — |

---

## [04:10 UTC] Session — R178 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R178
- `tests/test_sps_stats_source_2_noarch.py` (+30, suite 7,678 → 7,708).
- 2nd source-expression pass for `src/sps/stats.py`:
  `n = len(per_batch_means)`, `return {"stable": False, "n_batches": n}`,
  `total = sum(per_batch_means)`,
  `loo_means = [(total - g) / (n - 1) for g in per_batch_means]`,
  `grand_mean = total / n`,
  `worst_idx = max(range(n), key=lambda i: abs(loo_means[i] - grand_mean))`,
  `"n_batches": n,`, `"grand_mean": round(grand_mean, 6),`,
  `"loo_min": round(min(loo_means), 6),`, `"worst_batch": worst_idx,`,
  `mean, ci_lo, ci_hi = bootstrap_ci(per_sample_gaps, n_boot=n_boot, seed=seed, alpha=alpha)`,
  `loo = loo_spectral_gap(per_sample_gaps)`,
  `loo_stable = loo.get("stable", False)`, `loo_range = loo.get("loo_range", 0.0)`,
  `"mean": mean,`, `"ci_lo": ci_lo,`, `"ci_hi": ci_hi,`,
  `"ci_width": round(ci_hi - ci_lo, 6),`, `"loo_stable": loo_stable,`,
  `"n": len(per_sample_gaps),`, `reports: dict[str, dict] = {}`,
  `for arch, gaps in arch_gaps.items():`,
  `reports[arch] = spectral_gap_report(gaps, n_boot=n_boot, seed=seed)`,
  `ranking = sorted(reports.keys(), key=lambda a: reports[a]["mean"], reverse=True)`,
  `reports["ranking"] = ranking  # type: ignore[assignment]`, `return reports`,
  `na, nb = len(gaps_a), len(gaps_b)`, `rng.shuffle(pooled)`,
  `perm_a = pooled[:na]`, `perm_b = pooled[na:]`.

### NeuroVoice R178
- `tests/test_emg_preprocessing_source_2_noarch.py` (+30, suite 9,381 → 9,411).
- 2nd source-expression pass for `src/emg/preprocessing.py`:
  `mean = signal.mean(axis=1, keepdims=True)`, `std = signal.std(axis=1, keepdims=True)`,
  `return ((signal - mean) / std).astype(np.float32)`,
  `filtered = self.bandpass_filter(raw)`, `filtered = self.notch_filter(filtered)`,
  `return self.normalize(filtered)`,
  `window_samples = int(self._sr * window_ms / 1000)`,
  `hop_samples = int(self._sr * hop_ms / 1000)`,
  `segments: list[EMGSegment] = []`,
  `for start in range(0, signal.shape[1] - window_samples + 1, hop_samples):`,
  `end = start + window_samples`, `data=signal[:, start:end],`,
  `n_channels=signal.shape[0],`,
  `timestamp_ms=start_timestamp_ms + (start * 1000 // self._sr),`,
  `user_id=user_id,`, `return segments`,
  `n_channels: int = 8,`, `window_ms: int = WINDOW_MS,`, `hop_ms: int = HOP_MS,`,
  `preprocessor: EMGPreprocessor | None = None,`,
  `self._window_samples = int(sample_rate * window_ms / 1000)`,
  `self._hop_samples = int(sample_rate * hop_ms / 1000)`,
  `self._user_id = user_id`,
  `self._preprocessor = preprocessor or EMGPreprocessor(`,
  `buffer_samples = int(sample_rate * buffer_seconds)`,
  `if buffer_samples < self._window_samples * 2:`,
  `self._buffer = np.zeros((n_channels, buffer_samples), dtype=np.float32)`,
  `self._capacity = buffer_samples`, `self._write_pos: int = 0`,
  `self._total_written: int = 0`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,708 | +30 |
| NeuroVoice | 9,411 | +30 |
| Combined | 17,119 | +60 |
| metaMirage | post-deadline | — |

---

## [05:15 UTC] Session — R179 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R179
- `tests/test_sps_stats_source_3_noarch.py` (+30, suite 7,708 → 7,738).
- 3rd source-expression pass for `src/sps/stats.py`:
  module header (`from __future__ import annotations`, `import math`, `import random`,
  `__all__ = [`, all seven `__all__` entries); bootstrap_ci residual
  (`round(boot_means[hi_idx], 6),`); LOO dict residuals (`"n": n,`,
  `"mean": round(total / n, 6),`, `"loo_max": round(max(loo_means), 6),`);
  delta-method residuals (`elif abs(alpha - 0.01) < 1e-9:`, `z = 2.576`,
  `z = 1.645  # 90%`); permutation-test main body (`n_total = na + nb`,
  `mean_a`, `mean_b`, `obs_diff`, `extreme = 0`, `for _ in range(n_perm):`,
  `perm_diff`, `if alternative == "two-sided":`, `if abs(perm_diff) >= abs(obs_diff):`,
  `extreme += 1`, `elif alternative == "greater":`, `if perm_diff >= obs_diff:`).

### NeuroVoice R179
- `tests/test_emg_preprocessing_source_3_noarch.py` (+30, suite 9,411 → 9,441).
- 3rd source-expression pass for `src/emg/preprocessing.py` (rewrites old R154 file):
  `from __future__ import annotations`, `import dataclasses`, `import logging`,
  `from typing import Callable, Generator, TYPE_CHECKING`, `import numpy as np`,
  `if TYPE_CHECKING:`, `from numpy.typing import NDArray`,
  `logger = logging.getLogger(__name__)`, `OFFSET_SILENCE_MS: int = 300`;
  ContinuousEMGBuffer init residuals (`sample_rate=sample_rate,`,
  `n_channels=n_channels,`, `self._last_yield_pos: int = 0`,
  debug format string, `self._buffer.fill(0.0)`, reset pointer assignments,
  buffered_ms property, write() channel guard);
  write() body (`f"Expected..."`, `n_new`, `remaining`, `src_offset = 0`,
  `while remaining > 0:`, `space`, `to_write`, `_write_pos modulo advance`,
  `src_offset += to_write`).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,738 | +30 |
| NeuroVoice | 9,441 | +30 |
| Combined | 17,179 | +60 |
| metaMirage | post-deadline | — |

---

## [06:18 UTC] Session — R180 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R180
- `tests/test_sps_stats_source_4_noarch.py` (+30, suite 7,738 → 7,768).
- 4th source-expression pass for `src/sps/stats.py`:
  permutation_test_gap_diff final "less" branch + empty-sample guard + return-dict residuals;
  spectral_gap_report `"loo_range": loo_range,`; all function signature params
  (bootstrap_ci, delta_method_rsps_ci, compare_architecture_cis, permutation_test_gap_diff)
  and return-type annotations.

### NeuroVoice R180
- `tests/test_emg_preprocessing_source_6_noarch.py` (+30, suite 9,441 → 9,471).
- 6th source-expression pass for `src/emg/preprocessing.py`:
  ContinuousEMGBuffer._drain_windows ring-buffer extraction logic and drain EMGSegment kwargs;
  detect_speech_onset state machine initial values, enumerate/zip loop header, all three
  branch conditions, inner assignments (region_start_ms/region_frames/peak_rms transitions).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,768 | +30 |
| NeuroVoice | 9,471 | +30 |
| Combined | 17,239 | +60 |
| metaMirage | post-deadline | — |

---

## [13:15 UTC] Session — R181 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R181
- `tests/test_transformations_source_7_noarch.py` (+30, suite 7,768 → 7,798).
- 7th source-expression pass for `src/sps/transformations.py`:
  `Semantic transformation families for SPS computation.`, `EmbeddingPerturbationFamily (T_emb):`,
  `SynonymSubstitutionFamily (T_syn):`, `AdversarialEmbeddingFamily (T_adv):`,
  `T_alpha^v(E) = E + alpha * v,  v in A_x.`, `Kang (2026) Definitions 0.1, 0.2, 3, A5.`,
  `from abc import ABC, abstractmethod`, `from dataclasses import dataclass, field`,
  `from typing import Callable, Optional`, `from sps.utils import normalize_directions`,
  `class TransformationFamily(ABC):`, `Abstract base for an admissible semantic transformation family T.`,
  `Every concrete family must implement \`.sample\``, `@abstractmethod`,
  `embeddings:  Token embeddings of shape (B, seq_len, hidden_dim).`,
  `input_ids:   Token IDs of shape (B, seq_len).`,
  `perturbed:   Perturbed embeddings of shape (B, seq_len, hidden_dim).`,
  `Zero entries indicate that no valid transformation was`,
  `Return the structured semantic direction set A_x for each sample.`,
  `directions:  Unit vectors of shape (B, K, seq_len, hidden_dim),`,
  `substitution_prob: float = 0.15`, `max_substitutions: int = 3`,
  `T_emb: Continuous perturbation along semantic directions in embedding space.`,
  `T_syn: Token-level synonym substitution via a precomputed synonym map.`,
  `This family is discrete and not differentiable.`,
  `Use EmbeddingPerturbationFamily for that purpose.`,
  `T_adv: Worst-case semantic perturbation within A_x.`,
  `class SynonymSubstitutionConfig:`, `class AdversarialEmbeddingConfig:`,
  `where K is the number of admissible directions per sample.`.

### NeuroVoice R181
- `tests/test_emg_preprocessing_source_7_noarch.py` (+30, suite 9,471 → 9,501).
- 7th source-expression pass for `src/emg/preprocessing.py`:
  `if region_frames >= min_active_frames:`, `end_sample = int(ts_ms * sample_rate / 1000)`,
  `start_sample = int(region_start_ms * sample_rate / 1000)`,
  `region_data = signal[:, start_sample:end_sample]`, `peak_channel = int(`,
  `np.argmax(np.sqrt(np.mean(region_data ** 2, axis=1)))`,
  `start_ms=region_start_ms,`, `end_ms=ts_ms,`, `peak_rms=peak_rms,`, `channel=peak_channel,`,
  `if in_speech and region_frames >= min_active_frames:`,
  `end_ms = frame_timestamps_ms[-1] if frame_timestamps_ms else 0`,
  `region_data = signal[:, start_sample:]`, `end_ms=end_ms,`, `onsets.append(`,
  `baseline_rms = float(np.median(rms_arr[:baseline_frames])) + 1e-8`,
  `-> list[SpeechOnset]:`, `return onsets`, `self._total_written += n_new`,
  `return self._drain_windows()`, `segments: list[EMGSegment] = []`, `segments.append(`,
  `timestamp_ms = window_start_abs * 1000 // self._sr`,
  `energy_threshold_multiplier: float = ONSET_ENERGY_THRESHOLD,`,
  `min_duration_ms: int = ONSET_HOLD_MS,`, `baseline_window_ms: int = 500,`,
  `user_id: str = "",`, `buffer_seconds: float = BUFFER_SECONDS,`,
  `return segments`, `while (self._total_written - self._last_yield_pos) >= self._hop_samples:`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,798 | +30 |
| NeuroVoice | 9,501 | +30 |
| Combined | 17,299 | +60 |
| metaMirage | post-deadline | — |

---

## [14:13 UTC] Session — R182 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R182
- `tests/test_transformations_source_8_noarch.py` (+30, suite 7,798 → 7,828).
- 8th source-expression pass for `src/sps/transformations.py`:
  `import logging`, `import torch`, `import torch.nn as nn`,
  `logger = logging.getLogger(__name__)`, `"""Configuration for the T_emb family."""`,
  `n_directions: int = 8`, `"""Number of semantic directions K to sample per input (defines |A_x|)."""`,
  `projection_noise_scale: float = 0.02`,
  `Small isotropic noise added before projection to avoid degenerate directions.`,
  `use_synonym_directions: bool = True`,
  `Semantic directions at input x are defined as the normalized differences`,
  `between each token's embedding and the embedding of a randomly chosen synonym`,
  `(or the nearest neighbor in embedding space when WordNet is unavailable).`,
  `Formally (Definition 0.1, embedding variant):`,
  `where v in A_x is a unit vector in the span of synonym embedding differences,`,
  `and alpha is drawn uniformly from (0, epsilon].`,
  `This family is differentiable w.r.t. the input embeddings, making it the`,
  `appropriate family for Jacobian / spectral gap computation (Theorem 2).`,
  `embedding_layer:  The model's token embedding layer (nn.Embedding).`,
  `synonym_map:      Dict mapping token_id -> list[synonym_token_id].`,
  `self.embedding_layer = embedding_layer`, `self.synonym_map = synonym_map or {}`,
  `self.config = config or EmbeddingPerturbationConfig()`,
  `Sample one T_emb perturbation per batch element.`,
  `Returns a perturbation E' = E + alpha * v where:`,
  `- v is drawn from the semantic direction set A_x (unit vector)`,
  `- alpha ~ Uniform(0, epsilon]`, `epsilon:     Perturbation radius.`,
  `perturbed:   (B, seq_len, hidden_dim).`, `magnitudes:  (B,).`.

### NeuroVoice R182
- `tests/test_emg_preprocessing_source_8_noarch.py` (+30, suite 9,501 → 9,531).
- 8th source-expression pass for `src/emg/preprocessing.py`:
  `# Hz, standard for speech EMG (matches Gaddy benchmark)`, `# Hz, removes motion artifacts`,
  `# Hz, EMG upper bound (499 not 500: Nyquist at 1kHz SR is 500Hz exactly, butter rejects Wn == fs/2)`,
  `# Hz, power line (use 50 for Korea/Europe)`, `# ms, analysis window`,
  `# ring buffer capacity (seconds of EMG)`, `# hop between consecutive windows`,
  `# RMS × baseline_rms triggers onset`, `# min speech duration to confirm onset`,
  `# silence duration before speech ends`, `# Quality factor = 30 gives narrow notch`,
  `# Write pointer (absolute sample index into ring)`,
  `# How many total samples have been written (monotonically increasing)`,
  `# Absolute sample index of the last window we yielded`,
  `# Write into ring buffer (wrap around if needed)`,
  `# Check we have a full window available`, `# Extract from ring buffer`, `# Wrap around`,
  `# Compute per-frame multi-channel RMS`, `# Baseline from first baseline_window_ms`,
  `# Detect active regions`, `# Find the channel with the highest peak energy`,
  `# Handle speech that continues to end of signal`,
  `frames: list[float] = []`, `frame_timestamps_ms: list[int] = []`,
  `for start in range(0, n_samples - frame_samples + 1, hop_samples):`,
  `frame = signal[:, start : start + frame_samples]`,
  `rms = float(np.sqrt(np.mean(frame ** 2)))`, `frames.append(rms)`,
  `frame_timestamps_ms.append(int(start * 1000 / sample_rate))`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,828 | +30 |
| NeuroVoice | 9,531 | +30 |
| Combined | 17,359 | +60 |
| metaMirage | post-deadline | — |

---

## [15:28 UTC] Session — R183 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R183
- `tests/test_transformations_source_9_noarch.py` (+30, suite 7,828 → 7,858).
- 9th source-expression pass for `src/sps/transformations.py`:
  EPF.sample() body (`device = embeddings.device`, `B = embeddings.size(0)`,
  `directions = self.semantic_directions(embeddings, input_ids)  # (B, K, seq_len, h)`,
  `K = directions.size(1)`,
  `idx = torch.randint(0, K, (B,), device=device)            # (B,)`,
  `v = directions[torch.arange(B, device=device), idx]        # (B, seq_len, h)`,
  `alpha = torch.empty(B, device=device).uniform_(0.0, epsilon)  # (B,)`,
  `alpha_exp = alpha.view(B, 1, 1)`,
  `perturbed = embeddings + alpha_exp * v                     # (B, seq_len, h)`,
  `magnitudes = (perturbed - embeddings).flatten(start_dim=1).norm(dim=1)  # (B,)`);
  sample() inline comments (K-directions, pick-direction, scale-alpha, c(T,x) definition,
  Frobenius displacement); `return perturbed, magnitudes`;
  semantic_directions() body (`B, seq_len, hidden = embeddings.shape`,
  `K = self.config.n_directions`,
  `if self.config.use_synonym_directions and self.synonym_map:`,
  `return self._synonym_directions(embeddings, input_ids, K)`,
  `return self._random_orthogonal_directions(B, seq_len, hidden, K, device)`);
  AEC field docstrings (`"""Configuration for the T_adv adversarial family."""`,
  `"""Number of semantic directions to search over per input."""`,
  `"""If True, build A_x from synonym differences (same as T_emb).`,
  `If False, use random orthogonal directions (ablation)."""`,
  `"""Noise added before normalization to avoid degenerate directions."""`);
  SSF docstring detail (`The transformation magnitude`,
  `c(T_syn, x) is defined as the L2 distance in embedding space between the`,
  `original and perturbed token sequences.`);
  AEF docstring detail (`For each input x, finds the direction v* in A_x that maximizes ||Jf(x) v*||`).

### NeuroVoice R183
- `tests/test_emg_preprocessing_source_9_noarch.py` (+30, suite 9,531 → 9,561).
- 9th source-expression pass for `src/emg/preprocessing.py`:
  EMGPreprocessor class docstring (`"""Preprocess raw EMG signals for the encoder.`,
  `sample_rate: Input sample rate in Hz.`, `n_channels: Number of EMG channels.`,
  `notch_freq: Power line frequency (60 Hz US, 50 Hz KR/EU).`);
  bandpass_filter() sig (`low: float = BANDPASS_LOW,`, `high: float = BANDPASS_HIGH,`) +
  docstring opener + `low: Lower cutoff frequency (Hz).` + `high: Upper cutoff frequency (Hz).` +
  `Filtered signal, same shape.`;
  segment() sig (`hop_ms: int = 50,`, `start_timestamp_ms: int = 0,`) +
  docstring Args (`window_ms: Window length in ms.`, `hop_ms: Hop length in ms.`,
  `user_id: User identifier.`, `start_timestamp_ms: Timestamp of first sample.`,
  `List of EMGSegment objects.`);
  ContinuousEMGBuffer.__init__ ValueError (`if buffer_samples < self._window_samples * 2:`,
  `f"buffer_seconds too small: need at least {2 * window_ms / 1000:.1f}s."`);
  write() ring-buffer slice assignment (`self._buffer[:, self._write_pos : ...]`,
  `samples[:, src_offset : ...]`, `remaining -= to_write`);
  detect_speech_onset() (`if not frames:`, `return []`,
  `rms_arr = np.array(frames, dtype=np.float32)`,
  `baseline_frames = max(1, int(baseline_window_ms / frame_ms))`,
  `baseline_rms = float(np.median(rms_arr[:baseline_frames])) + 1e-8`,
  `threshold = baseline_rms * energy_threshold_multiplier`,
  `is_active = rms_arr > threshold`,
  `min_active_frames = max(1, min_duration_ms // frame_ms)`).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,858 | +30 |
| NeuroVoice | 9,561 | +30 |
| Combined | 17,419 | +60 |
| metaMirage | post-deadline | — |

---

## [16:42 UTC] Session — R184 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R184
- `tests/test_transformations_source_10_noarch.py` (+30, suite 7,858 → 7,888).
- 10th source-expression pass for `src/sps/transformations.py`:
  `all_directions: list[torch.Tensor] = []`, `for _ in range(K):`,
  `# For each sample and position, pick a random synonym token`,
  `syn_ids = input_ids.clone()`, `for b in range(B):`, `for s in range(seq_len):`,
  `tid = input_ids[b, s].item()`, `candidates = self.synonym_map.get(int(tid), [])`,
  `syn_ids[b, s] = candidates[`, `torch.randint(0, len(candidates), (1,)).item()`,
  `with torch.no_grad():`, `syn_embeddings = self.embedding_layer(syn_ids)     # (B, seq_len, h)`,
  `# Add small noise to avoid degenerate zero directions`,
  `# Normalize per-position, then treat as a (B, seq_len, h) direction`,
  `# Normalize over hidden dim only; full-sequence norm applied below`,
  `d_norm = normalize_directions(delta.flatten(start_dim=1)).view_as(delta)`,
  `all_directions.append(d_norm.unsqueeze(1))             # (B, 1, seq_len, h)`,
  `# Gram-Schmidt orthogonalization per sample`, `for k in range(K):`,
  `v = raw[:, k, :]                                       # (B, flat_dim)`,
  `for j in range(k):`, `u = ortho[:, j, :]`,
  `proj = (v * u).sum(dim=-1, keepdim=True) * u`, `v = v - proj`,
  `norm = v.norm(dim=-1, keepdim=True).clamp(min=1e-12)`, `ortho[:, k, :] = v / norm`,
  `return ortho.view(B, K, seq_len, hidden)`,
  `(the directional derivative norm), then perturbs along v*.  This provides an`,
  `adversarial upper bound on sensitivity: SPS^(T_adv)(f) <= SPS^(T_arb)(f),`,
  `The ratio SPS^(T_adv) / SPS^(T_emb) quantifies how close the random synonym`.

### NeuroVoice R184
- `tests/test_emg_preprocessing_source_10_noarch.py` (+30, suite 9,561 → 9,591).
- 10th source-expression pass for `src/emg/preprocessing.py`:
  `# Check we have a full window available`, `window_start_abs = self._last_yield_pos`,
  `window_end_abs = window_start_abs + self._window_samples`,
  `if window_end_abs > self._total_written:`, `break  # not enough samples yet for a full window`,
  `# Extract from ring buffer`, `window_end_ring = window_start_ring + self._window_samples`,
  `if window_end_ring <= self._capacity:`, `try:`,
  `processed = self._preprocessor.process(raw_window)`,
  `Apply bandpass filter to EMG signal.`, `signal: Raw EMG, shape (n_channels, n_samples).`,
  `Remove power line interference (50/60 Hz notch).`, `Notch-filtered signal.`,
  `Per-channel z-score normalization.`, `signal: Filtered EMG, shape (n_channels, n_samples).`,
  `Normalized signal (zero mean, unit variance per channel).`,
  `Full preprocessing chain: bandpass → notch → normalize.`,
  `raw: Raw EMG, shape (n_channels, n_samples).`, `Split preprocessed signal into overlapping windows.`,
  `Accumulates raw EMG samples from the BLE stream and yields overlapping`,
  `windows suitable for the ContinuousEMGEncoder. Preprocessing (bandpass,`,
  `notch, normalize) is applied to each yielded window independently.`,
  `Ring buffer capacity: BUFFER_SECONDS × sample_rate samples`,
  `Yield windows whenever enough new samples have accumulated (hop_ms worth)`,
  `Windows overlap by (window_ms - hop_ms) ms`, `signal: Preprocessed EMG, shape (n_channels, n_samples).`,
  `Preprocessed signal, same shape.`, `n_available = min(self._total_written, self._capacity)`,
  `return int(n_available * 1000 / self._sr)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,888 | +30 |
| NeuroVoice | 9,591 | +30 |
| Combined | 17,479 | +60 |
| metaMirage | post-deadline | — |

---

## [17:13 UTC] Session — R185 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R185
- `tests/test_transformations_source_11_noarch.py` (+30, suite 7,888 → 7,918).
- 11th source-expression pass for `src/sps/transformations.py`:
  `but SPS^(T_adv)(f) >= SPS^(T_emb)(f) (in expectation, since T_adv is the`,
  `worst case over the same direction set that T_emb samples uniformly from).`,
  `direction sampling in T_emb is to the worst case — a ratio near 1 means random`,
  `directions are already near-adversarial; a large ratio means the benchmark`,
  `understates worst-case semantic vulnerability.`,
  `forward_fn:       f: (B, seq, h) -> (B, d). Used for JVP computation.`,
  `embedding_layer:  The model's token embedding layer.`,
  `synonym_map:      Dict[token_id, List[synonym_token_id]]. Can be empty.`,
  `config:           AdversarialEmbeddingConfig.`,
  `Perturb along the worst-case semantic direction v* = argmax_v ||Jf(x) v||.`,
  `delta = syn_embeddings - embeddings                    # (B, seq_len, h)`,
  `noise_scale = self.config.projection_noise_scale`,
  `delta = delta + noise_scale * torch.randn_like(delta)`,
  `return torch.cat(all_directions, dim=1)                    # (B, K, seq_len, h)`,
  `flat_dim = seq_len * hidden`, `raw = torch.randn(B, K, flat_dim, device=device)`,
  `ortho = torch.zeros_like(raw)`,
  `Ablation: draw K random unit directions in embedding space.`,
  `Uses Gram-Schmidt to ensure orthogonality when K <= hidden.`,
  `magnitudes:  (B,) — perturbation magnitudes c(T_adv, x).`,
  `from nltk.corpus import wordnet as wn`, `except ImportError as e:`,
  `nltk is required for WordNet synonym maps. `,
  `Install with: pip install nltk && python -m nltk.downloader wordnet`,
  `vocab_size = vocab_size or tokenizer.vocab_size`,
  `synonym_map: dict[int, list[int]] = {}`, `for token_id in range(vocab_size):`,
  `word = tokenizer.decode([token_id]).strip()`,
  `if not word.isalpha() or len(word) < 2:`, `synonym_tokens: list[int] = []`.

### NeuroVoice R185
- `tests/test_emg_preprocessing_source_11_noarch.py` (+30, suite 9,591 → 9,621).
- 11th source-expression pass for `src/emg/preprocessing.py`:
  `from scipy.signal import butter, sosfiltfilt`, `from scipy.signal import iirnotch, filtfilt`,
  `return filtered`, `signal: EMG signal, shape (n_channels, n_samples).`,
  `signal: NDArray[np.float32],`, `sample_rate: int = SAMPLE_RATE,`,
  `n_channels, n_samples = signal.shape`, `frame_ms = 10`,
  `frame_samples = int(sample_rate * frame_ms / 1000)`, `hop_samples = frame_samples // 2`,
  `"""Detect speech onset and offset events using RMS energy.`, `Algorithm:`,
  `1. Compute per-frame RMS in overlapping 10ms windows.`,
  `2. Estimate baseline RMS from the first baseline_window_ms of signal.`,
  `3. Mark frames where RMS > baseline × energy_threshold_multiplier as active.`,
  `4. Merge contiguous active regions; discard those shorter than min_duration_ms.`,
  `This is intentionally simple. More sophisticated approaches (e.g. learned`,
  `onset detectors) can be plugged in by replacing this function.`,
  `energy_threshold_multiplier: How many multiples of baseline RMS`,
  `baseline_window_ms: Window from which to estimate resting baseline RMS.`,
  `triggers an onset detection.`,
  `min_duration_ms: Minimum duration (ms) for a region to be reported.`,
  `List of SpeechOnset events, sorted by start_ms.`, `onsets: list[SpeechOnset] = []`,
  `for i, (active, ts_ms, rms) in enumerate(`,
  `zip(is_active.tolist(), frame_timestamps_ms, rms_arr.tolist())`,
  `"detect_speech_onset: %d events found (threshold=%.4f, baseline=%.4f)",`,
  `len(onsets), threshold, baseline_rms,`,
  `logger.debug("ContinuousEMGBuffer reset for user '%s'", self._user_id)`,
  `"""Approximate milliseconds of data currently in the buffer."""`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,918 | +30 |
| NeuroVoice | 9,621 | +30 |
| Combined | 17,539 | +60 |
| metaMirage | post-deadline | — |

---

## [18:17 UTC] Session — R186 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R186
- `tests/test_transformations_source_12_noarch.py` (+30, suite 7,918 → 7,948).
- 12th source-expression pass for `src/sps/transformations.py`:
  `# Reuse T_emb direction logic via delegation`, `n_directions=self.config.n_directions,`,
  `use_synonym_directions=self.config.use_synonym_directions,`,
  `projection_noise_scale=self.config.projection_noise_scale,`, `config=emb_cfg,`,
  `# Compute A_x directions: (B, K, seq, h)`, `# Find worst-case direction per sample: (B, seq, h)`,
  `v_star = adversarial_worst_direction(self.forward_fn, x, directions)`,
  `# Re-normalize (JVP preserves direction but not unit norm guarantee)`,
  `perturbed = embeddings + alpha.view(B, 1, 1) * v_star`, `if not synsets:`,
  `for synset in synsets:`, `for lemma in synset.lemmas():`, `if syn_word in seen:`,
  `seen.add(syn_word)`, `syn_id = enc[0]`, `if syn_id != token_id:`,
  `synonym_tokens.append(syn_id)`, `if len(synonym_tokens) >= max_synonyms_per_token:`,
  `return synonym_map`, `# Abstract base`, `# T_emb: Continuous embedding-space perturbation`,
  `# T_syn: Discrete synonym substitution`,
  `# T_adv: Adversarial semantic perturbation (worst-case within A_x)`,
  `# Synonym map construction (WordNet via NLTK)`, `n_substituted = 0`,
  `positions = torch.randperm(seq_len).tolist()`, `for pos in positions:`,
  `logger.info(`, `len(synonym_map),`.

### NeuroVoice R186
- `tests/test_tier1_template_source_2_noarch.py` (+30, suite 9,621 → 9,651).
- 2nd source-expression pass for `src/emg/tier1_template.py`:
  `DEFAULT_N_FEATURES_PER_CHANNEL: int = 5  # RMS, MAV, ZCR, SSC, WL`,
  `DEFAULT_K: int = 3                # k-NN neighbourhood size`,
  `DEFAULT_REPS_PER_COMMAND: int = 3  # 3 repetitions during calibration`,
  `DEFAULT_VOCAB_SIZE: int = 50       # 50 commands for day-1 MVP`,
  `CONFIDENCE_MED_THRESHOLD: float = 0.50   # display but do not speak`,
  `CONFIDENCE_LOW_THRESHOLD: float = 0.30   # show "I didn't catch that"`,
  `confidence: float`, `top_3_alternatives: list[tuple[str, float]]`,
  `confidence_level: str`, `return self.confidence < CONFIDENCE_LOW_THRESHOLD`,
  `self._feature_dim = feature_dim`, `self._normalize = normalize_features`,
  `self._labels: list[str] = []`, `self._norm_std: NDArray[np.float32] | None = None`,
  `self._invalidate_cache()`, `logger.debug(`, `counts: dict[str, int] = {}`,
  `counts[t.label] = counts.get(t.label, 0) + 1`, `return counts`,
  `return min(counts.values()) >= min_templates`,
  `msg = "No calibration templates stored. Run calibration first."`,
  `raise RuntimeError(msg)`, `self._rebuild_cache_if_needed()`,
  `assert self._feature_matrix is not None  # mypy`,
  `distances = _euclidean_distances(query.astype(np.float32), self._feature_matrix)`,
  `label_distances: dict[str, list[float]] = {}`, `sorted_idx = np.argsort(distances)`,
  `label_scores.sort(key=lambda x: x[1])  # ascending by distance`,
  `best_label, best_dist = label_scores[0]`,
  `confidence = _distance_to_confidence(distances, self._k)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,948 | +30 |
| NeuroVoice | 9,651 | +30 |
| Combined | 17,599 | +60 |
| metaMirage | post-deadline | — |

---

## [09:45 UTC] Session — R187 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R187
- `tests/test_sps_core_source_13_noarch.py` (+30, suite 7,948 → 7,978).
- 13th source-expression pass for `src/sps/core.py`:
  RelativeSPSReport docstring Interpretation section (rSPS > 1 / ≈ 1 / < 1 lines,
  log-Wald formula, Definition 8 references), AdversarialSPSReport ratio bullet points
  (Ratio near 1.0 / 0.0, T_adv maximises sentence, inequality line),
  RelativeSPSEstimator.estimate() section-header comments (T pass / T_arb pass /
  rSPS delta-method CI / Per-sample CI and LOO), SPSConfig.validate_device body
  (@field_validator, @classmethod, try/except/raise ValueError, return v),
  RelEstimator class docstring math (log(rSPS) = mean_s_arb − mean_s_T,
  Var(log rSPS) ≈ Var_T / n_T + Var_arb / n_arb, Kang (2026) Definition 8, §4.3.).

### NeuroVoice R187
- `tests/test_tier1_template_source_3_noarch.py` (+30, suite 9,651 → 9,681).
- 3rd source-expression pass for `src/emg/tier1_template.py`:
  predict() kNN loop body (enumerate over sorted_idx, lbl = self._labels[idx],
  label_distances.setdefault append, label_scores init, for lbl/dlist items,
  top_k sorted slice, mean_d np.mean, label_scores.append),
  top_3 building (top_3 list init, for lbl/d in label_scores[:3], alt_conf exp,
  top_3.append), confidence level branches (HIGH/MED/LOW assignments, auto_speak),
  return Tier1Prediction (label=best_label, mean_distance_to_nearest=best_dist),
  _invalidate_cache body (self._feature_matrix = None),
  _rebuild_cache_if_needed body (not-None guard, zeros empty branch, norm_mean/std None,
  np.stack rebuild, labels list comprehension, norm_mean/std computation, np.maximum clamp),
  template_count return (return len(self._templates)).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 7,978 | +30 |
| NeuroVoice | 9,681 | +30 |
| Combined | 17,659 | +60 |
| metaMirage | post-deadline | — |

---

## [20:10 UTC] Session — R188 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R188
- `tests/test_metrics_source_11_noarch.py` (+30, suite 7,978 → 8,008).
- 11th source-expression pass for `src/sps/metrics.py`:
  `n_directions=config.m_transforms,`, `use_synonym_directions=False,       # random orthogonal directions = T_arb`,
  `embedding_layer = _get_embedding_layer(model)`, `arb_family = EmbeddingPerturbationFamily(`,
  `embedding_layer=embedding_layer,`, `synonym_map=None,`, `config=arb_config,`,
  `estimator = build_sps_estimator(model, arb_family, config)`,
  `result = estimator.estimate(data_iterator)`, `return result if return_full else result["sps"]`,
  `def _fn(inputs_embeds: torch.Tensor) -> torch.Tensor:`,
  `if hasattr(out, "last_hidden_state"):`, `return out.last_hidden_state[:, 0, :]`,
  `return out[:, 0, :] if out.dim() == 3 else out`,
  `gap_result = spectral_gap(_fn, emb, directions)`, `report.spectral_gap_mean = gap_result.mean_gap`,
  `report.spectral_gap_result = gap_result`, `per_sample_gaps = gap_result.normalized_gap.tolist()`,
  `report.gap_ci = bootstrap_ci(per_sample_gaps)`, `if compute_relative:`,
  `arb_full = estimate_arbitrary_sps(model, iter(batches), config, return_full=True)`,
  `report.relative_sps = relative_sps(report.sps, arb_full["sps"])`,
  `arb_mean_s=arb_full["mean_sensitivity"],`, `arb_std_s=arb_full["std_sensitivity"],`,
  `arb_n=arb_full["n_samples"],`, `analyzer = LayerwiseSPSAnalyzer(model, transform_family, config)`,
  `report.layerwise_profile = analyzer.compute_profile_from_batches(batches)`, `return report`,
  `gap_ci: Optional[tuple[float, float, float]] = None   # (point, lo, hi) bootstrap CI for spectral gap`,
  `rsps_ci: Optional[tuple[float, float, float]] = None  # (point, lo, hi) delta-method CI for rSPS`.

### NeuroVoice R188
- `tests/test_tier1_template_source_4_noarch.py` (+30, suite 9,681 → 9,711).
- 4th source-expression pass for `src/emg/tier1_template.py`:
  `path = Path(path)`, `path.parent.mkdir(parents=True, exist_ok=True)`,
  `self._rebuild_cache_if_needed()`, `if self._templates:`, `raw_matrix = np.stack(`,
  `[t.features for t in self._templates], axis=0`, `).astype(np.float32)`,
  `raw_matrix = np.zeros((0, self._feature_dim), dtype=np.float32)`, `metadata = {`,
  `"k": self._k,`, `"feature_dim": self._feature_dim,`, `"normalize": self._normalize,`,
  `"n_templates": len(self._templates),`, `"vocabulary": sorted({t.label for t in self._templates}),`,
  `"session_ids": sorted({t.calibration_session_id for t in self._templates}),`,
  `np.savez(`, `feature_matrix=raw_matrix,`, `labels=np.array(`,
  `metadata=np.array([json.dumps(metadata)], dtype=object),`,
  `"Saved Tier1 decoder to %s (%d templates, %d labels)",`,
  `with np.load(path, allow_pickle=True) as data:`, `metadata = json.loads(str(data["metadata"][0]))`,
  `feature_matrix = data["feature_matrix"]`, `labels = [str(x) for x in data["labels"]]`,
  `session_ids = [str(x) for x in data["session_ids"]]`, `repetition_indices = data["repetition_indices"]`,
  `for i, label in enumerate(labels):`, `features=feature_matrix[i].astype(np.float32),`,
  `if float(np.sum(np.abs(norm_mean))) > 1e-8 or float(np.sum(np.abs(norm_std - 1.0))) > 1e-8:`,
  `return decoder`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,008 | +30 |
| NeuroVoice | 9,711 | +30 |
| Combined | 17,719 | +60 |
| metaMirage | post-deadline | — |

---

## [21:05 UTC] Session — R189 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R189
- `tests/test_sps_core_source_14_noarch.py` (+30, suite 8,008 → 8,038).
- 14th source-expression pass for `src/sps/core.py`: StructuredSensitivityEstimator class
  docstring (intro / Approximates heading / full Sens formula / by-sampling sentence /
  Args: model callable-mapping / inputs_embeds note / transform_family / divergence_fn /
  epsilon / device); AdversarialSPSEstimator class docstring narrative (Runs-two / adversarial-
  directions / AdversarialSPSReport-including / both-per-sample / gap-ratio-answers / Args:
  emb_estimator / adv_estimator / config defaults) and estimate() docstring (embeddings-consumed /
  Returns-all-metrics); RelativeSPSEstimator.estimate() docstring residuals (embeddings-list /
  internally / RelativeSPSReport-Returns); SPSConfig Attributes residuals (divergence /
  device / seed / batch_size); RelativeSPSEstimator class Args (t_estimator / arb_estimator /
  config-defaults-t_estimator).

### NeuroVoice R189
- `tests/test_tier1_template_source_5_noarch.py` (+30, suite 9,711 → 9,741).
- 5th source-expression pass for `src/emg/tier1_template.py`: load() residuals
  (norm_mean/norm_std data extraction, decoder=cls() with k/feature_dim/normalize_features,
  template-loop calibration_session_id/repetition_index ternary kwargs, _norm_mean/std astype,
  _invalidate_cache call, logger.info Loaded line); Tier1CalibrationSession.__post_init__
  (vocab-empty guard + msg, reps_required guard + msg); groups() list comprehension
  (slice / range); record_repetition() body (command-not-in-vocabulary guard + msg,
  decoder.add_template call, label=command, session_id); progress() body (counts +
  dict comprehension); is_complete() body (progress() call, return all()); next_command()
  body (incomplete dict, for cmd count in items, if not incomplete).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,038 | +30 |
| NeuroVoice | 9,741 | +30 |
| Combined | 17,779 | +60 |
| metaMirage | post-deadline | — |

---

## [22:14 UTC] Session — R190 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R190
- `tests/test_sps_core_source_15_noarch.py` (+30, suite 8,038 → 8,068).
- 15th source-expression pass for `src/sps/core.py`:
  SPSEstimator class docstring `Computes:` heading, `sensitivity_estimator` / `config` Args;
  SPSEstimator.estimate() docstring opener, `Each batch from data_iterator must contain:`,
  input_ids/attention_mask/embeddings batch-key lines, `data_iterator: Iterable of batches.`;
  estimate() Returns block: `Dict with keys:`, sps/mean_sensitivity/std_sensitivity/n_samples
  return key descriptions; SPSEstimator._get_embeddings() docstring (all three lines);
  SPSConfig class docstring opener `Configuration for SPS estimation.`;
  module docstring opener `Core SPS computation: Definitions 1 & 2`;
  SSE.estimate() docstring opener + Args block (embeddings/input_ids/attention_mask/m) +
  Returns sentence; SSE._forward() docstring (both lines); module Implements list
  SSE / SPSEstimator entries.

### NeuroVoice R190
- `tests/test_tier1_template_source_6_noarch.py` (+30, suite 9,741 → 9,771).
- 6th source-expression pass for `src/emg/tier1_template.py`:
  module docstring Why-bullets 1–3 (Zero training time — 4 lines; Graceful degradation
  — 3 lines; Interpretable — 3 lines); Why-bullets 4–5 (Proven — 3 lines; Pure numpy
  — 2 lines); Tier1Template class docstring opener (A-user-provides sentence);
  Tier1Template.__post_init__() msg f-string; Tier1Decoder class docstring opener +
  pure-numpy sentence; _safe_zscore() docstring; _euclidean_distances() docstring
  (opener / in-database / query-arg / database-arg / Returns);
  _distance_to_confidence() body residuals (sorted_d / next_k_mean ternary / gap_bonus
  formula / gap_bonus=0.0).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,068 | +30 |
| NeuroVoice | 9,771 | +30 |
| Combined | 17,839 | +60 |
| metaMirage | post-deadline | — |
