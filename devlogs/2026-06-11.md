# metaMirage Devlog — 2026-06-11

## [00:15 UTC] Session — R125 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R125
- `tests/test_cross_arch_adversarial_11_noarch.py` (+30, suite 6,088 → 6,118).
- 11th source-expression pass for `experiments/cross_arch_adversarial.py`:
  remaining sps import block names (`build_adversarial_sps_estimator`, `loo_batch_spectral_gap`,
  `permutation_test_gap_diff`, `set_seed`, `spectral_gap`, `spectral_gap_report`),
  `logging.basicConfig` datefmt, `sys.exit(1)`; `collect_spectral_gaps` body — SDPBackend
  import, `with _sdpa_kernel(SDPBackend.MATH):`, `except (ImportError, AttributeError):`,
  `dirs = t_emb.semantic_directions(emb, ids)`, `gaps.extend(result.normalized_gap.tolist())`;
  `run_architecture` residuals — `display_name = arch_name.split("/")[-1]`, SPSConfig call,
  `use_synonyms = bool(arch_synonym_map)`, return ArchResult.

### NeuroVoice R125
- `tests/test_pretrain_source_4_noarch.py` (+30, suite 7,791 → 7,821).
- 4th source-expression pass for `src/training/pretrain.py`:
  module docstring (description + MAE formula), `set_seed` body (random/np/torch/cuda/cudnn),
  data-split formulas (`n_val = max(1, int(...))`, `generator=torch.Generator().manual_seed`),
  DataLoader kwargs (`num_workers=4, pin_memory=True, drop_last=True`), model instantiations,
  `params = list(encoder.parameters()) + list(mae_decoder.parameters())`, `betas=(0.9, 0.95)`,
  `steps_per_epoch`, `pct_start=warmup_steps / max_steps`, `anneal_strategy="cos"`,
  `target = neural.permute(0, 2, 1)`, checkpoint names, `diff = (recon - target) ** 2`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,118 | +30 |
| NeuroVoice | 7,821 | +30 |
| Combined | 13,939 | +60 |
| metaMirage | post-deadline | — |

---

## [01:45 UTC] Session — R126 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R126
- `tests/test_cross_arch_adversarial_12_noarch.py` (+30, suite 6,118 → 6,148).
- 12th source-expression pass for `experiments/cross_arch_adversarial.py`:
  `prepare_batches` tokenizer kwargs (`padding=True`, `truncation=True`, `max_length=64`,
  `return_tensors="pt"`), `batches: list[dict] = []`, range loop stride, chunk slice,
  `enc["input_ids"]/enc["attention_mask"]` reads, `batches.append(...)` dict literal;
  `_make_fn`/`_make_mask_fn` lambda bodies (`def _fn(emb: torch.Tensor)`,
  `return model(inputs_embeds=emb, ...).last_hidden_state[:, 0, :]`, `return _fn`,
  count assertions ≥4 defs, `with _sdpa_kernel_adv(SDPBackend.MATH):`,
  `_make_mask_fn(batches[0]["attention_mask"].to(args.device))`);
  `gap_rep["mean/ci_lo/ci_hi/loo_stable"]` key accesses, `spectral_gap_report` call,
  `"%s: gap_mean=%.4f  CI=[%.4f, %.4f]  LOO_stable=%s"` format string,
  `gp['mean/ci_lo/loo_range/loo_stable']` in `print_adversarial_table`.

### NeuroVoice R126
- `tests/test_training_finetune_source_4_noarch.py` (+30, suite 7,821 → 7,851).
- 4th source-expression pass for `src/training/finetune.py`:
  module docstring (objective, MSE formula, Howard & Ruder 2018 citation, LR policy text);
  `_F0_IDX = 6`, `_VOICING_IDX = 8`, `__all__ = ["run_finetune"]`; all 10 `run_finetune`
  signature defaults (lr_encoder 1e-5, lr_decoder 1e-4, early_stop_patience 10,
  max_epochs 100, voicing_loss_weight 0.1, warmup_steps 500, batch_size 16, etc.);
  training loop inline comments (align, MSE, voicing, warmup), section header markers
  (`# ── Model`, `# ── Optimiser with separate LRs`, `# ── Training loop`),
  `val_loss = _validate(...)` full call, `batch["articulatory"].to(dev)`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,148 | +30 |
| NeuroVoice | 7,851 | +30 |
| Combined | 13,999 | +60 |
| metaMirage | post-deadline | — |

---

## [10:30 UTC] Session — R127 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R127
- `tests/test_cross_arch_adversarial_13_noarch.py` (+30, suite 6,148 → 6,178).
- 13th source-expression pass for `experiments/cross_arch_adversarial.py`:
  `validate_hypotheses` H1 branch residuals (`print("  Hypothesis Validation ...")`, `if rb:`,
  observed f-string, `if not h1_pass:`, `if ratio < 0.82:`, below/above-range messages,
  `if rb_name in by_name and deb_name in by_name:`, H2 DeBERTa/roberta prints),
  H2/H3 branches (`✓ CONFIRMED/✗ REFUTED` f-string, DeBERTa not-tighter messages, H3 header,
  H3 row f-string), `print_adversarial_table` row format strings (`rp = r.report`,
  `rp.sps_emb:>8.4f/rp.sps_adv:>8.4f/rp.adv_gap_ratio:>7.4f`, spectral gap row),
  main() JSON block (`import json as _json`, `from pathlib import Path as _Path`,
  mkdir, `_out = serialise_results(results)`, `with open`, `_json.dump`, `Results saved →`),
  pairwise loops (`for i in range(len(ranking)):`, `for j in range(i+1, ...)`).

### NeuroVoice R127
- `tests/test_ctc_head_source_2_noarch.py` (+30, suite 7,851 → 7,881).
- 2nd source-expression pass for `src/models/ctc_head.py`:
  `CharTokenizer.__init__` body (`BLANK_INDEX: int = 0`, base string, `chars = list(base) +
  list(extra_chars)`, `seen: set[str]`, `ordered: list[str]`, dedup for-loop,
  `self._idx_to_char = ["<blank>"] + ordered`, `c: i for i, c in enumerate(...)`),
  `encode`/`decode` bodies (`text.lower().strip()`, `out: list[int] = []`, `if c in
  self._char_to_idx:`, append, debug log, `chars: list[str] = []`, blank guard,
  bounds check, `chars.append(self._idx_to_char[idx])`, `return "".join(chars)`),
  `CTCHeadConfig` declarations (`@dataclasses.dataclass(slots=True)`, `encoder_dim: int = 256`,
  `vocab_size: int = 29`, `dropout: float = 0.1`), `logits: torch.Tensor = self.proj(x)`,
  ctc_greedy_decode inner loop (`for token in seq:`, collapse condition, `collapsed.append`,
  `prev = token`), `input_lengths.to(log_probs.device),`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,178 | +30 |
| NeuroVoice | 7,881 | +30 |
| Combined | 14,059 | +60 |
| metaMirage | post-deadline | — |

---

## [11:30 UTC] Session — R128 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R128
- `tests/test_adversarial_results_analysis_source_noarch.py` (+30, suite 6,178 → 6,208).
- First source-expression pass for `experiments/adversarial_sps_results_analysis.py`:
  module constants (_BASE/roberta-base, _LARGE/roberta-large, _DEBERTA/deberta-v3-base,
  H1_LO_DEFAULT=0.85, H1_HI_DEFAULT=0.92), `import json`, `from pathlib import Path`,
  `with open(path) as f:`, `return json.load(f)`; `format_results_table` body
  (header string, `{'Model':<35}` column format, `"-" * 82` separator, models loop,
  bootstrap_ci get, CI format string, loo_sym conditional, spectral_gap row get, join return);
  `verify_hypotheses` body (gap_base/large/deb model.get chains, h1/h2/h3 bool expressions,
  n_pass sum, gap_ratio = gap_base / max(gap_large, 1e-12), gap_ratio_roberta_base_large key).

### NeuroVoice R128
- `tests/test_encoder_body_source_noarch.py` (+30, suite 7,881 → 7,911).
- 1st body source-expression pass for `src/models/encoder.py`:
  `_TCNBlock.__init__` body (causal padding formula, Conv1d/LayerNorm/Dropout/GELU init),
  `_TCNBlock.forward` body (conv/causal-trim/rearrange/residual `return out + x`),
  `NeuralEncoder.__init__` (input_proj Conv1d kernel_size=1, tcn ModuleList, dilation=2**i,
  embed_dim/num_heads/batch_first, attn_norm/output_proj/output_norm),
  `NeuralEncoder.forward` (z=input_proj(x), rearrange, attn_out unpack, attn_norm residual,
  output_norm/proj), `MaskedAutoencoderDecoder` init (mask_token Parameter zeros,
  proj Linear, enable_nested_tensor=False, nn.init.normal_ std=0.02) and
  forward (mask_tokens.expand(B, T, D)).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,208 | +30 |
| NeuroVoice | 7,911 | +30 |
| Combined | 14,119 | +60 |
| metaMirage | post-deadline | — |

---

## [05:18 UTC] Session — R129 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R129
- `tests/test_adversarial_results_analysis_source_2_noarch.py` (+30, suite 6,208 → 6,238).
- 2nd source-expression pass for `experiments/adversarial_sps_results_analysis.py`:
  `verify_hypotheses` H1/H2/H3 format strings (`H1  gap_ratio={gap_ratio:.4f}`,
  `(expected [{h1_lo}, {h1_hi}])`, `{'PASS ✓' if h1/h2 else 'FAIL ✗'}`,
  `H3  LOO-stable all models`, `{n_pass}/3 hypotheses confirmed.`, `if n_pass == 3:`,
  `All 3 Kang (2026) §5.2 hypotheses verified on GPU. ✓`, `"formatted_report": "\n".join`),
  return dict spectral_gaps sub-keys (`"base"`, `"large"`, `"deberta"`); `build_summary`
  body (verify_hypotheses call, `"H1": hyps["h1"],`, `float(hyps["gap_ratio"])`,
  roberta_base/large/deberta_v3_base spectral gap floats, run_config conditional);
  `format_run_report` body (table/hyp_report calls, `return table + "\n\n" + hyp_report`);
  `main()` body (argv check, Usage print, `load_results(sys.argv[1])`).

### NeuroVoice R129
- `tests/test_session_adapt_source_noarch.py` (+30, suite 7,911 → 7,941).
- 1st source-expression pass for `src/personalization/session_adapt.py`:
  module imports (`copy`, `dataclasses`, `time`, `Callable/Literal/NamedTuple`,
  `from torch import Tensor`); `SessionAdaptConfig` field annotations (`strategy` Literal
  default, `lr`, `max_steps`, `patience`, `min_delta`, `top_k_tcn_blocks`, `weight_decay`,
  `clip_grad_norm`); `AdaptResult` NamedTuple fields (`loss_curve`, `wall_seconds`,
  `n_params`); `SessionAdapter.__init__` body (`encoder`/`cfg` assignments,
  `_saved_state: dict | None = None`); `adapt()` body (AdamW init, `best_loss = float("inf")`,
  `t0 = time.monotonic()`, total normalisation, patience check, `clip_grad_norm_` call,
  `wall = time.monotonic() - t0`, `save_state` deepcopy); `recommend_strategy` guard;
  `_unfreeze_target` `_TCNBlock` name check and `top_k` slice loop; `__all__`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,238 | +30 |
| NeuroVoice | 7,941 | +30 |
| Combined | 14,179 | +60 |
| metaMirage | post-deadline | — |

---

## [12:00 UTC] Session — R130 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R130
- `tests/test_bootstrap_sign_flip_source_3_noarch.py` (+30, suite 6,238 → 6,268).
- 3rd source-expression pass for `experiments/bootstrap_sign_flip.py`: `analyse()` signature
  and return dict residuals ("label", "r", "bootstrap_ci95", "permutation_p_two_sided", "loo"),
  `permutation_p` signature, INLINE_DATA key residuals ("aq_clean", "tdr_forced_abstention",
  "tdr_expertise_trap"), main() data list-comps, print loop, 4 analyse() calls in rows.

### NeuroVoice R130
- `tests/test_session_adapt_source_2_noarch.py` (+30, suite 7,941 → 7,971).
- 2nd source-expression pass for `src/personalization/session_adapt.py`: `_freeze_all` body,
  `_unfreeze_target` LayerNorm scan, `rollback` body, adapt() early-return path, AdaptResult
  return kwargs, adapt() loop init/accumulation residuals.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,268 | +30 |
| NeuroVoice | 7,971 | +30 |
| Combined | 14,239 | +60 |
| metaMirage | post-deadline | — |

---

## [07:15 UTC] Session — R131 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R131
- `tests/test_spectral_gap_loo_analysis_source_2_noarch.py` (+30, suite 6,268 → 6,298).
- 2nd source-expression pass for `experiments/spectral_gap_loo_analysis.py`:
  `generate_per_sample_gaps` residuals (`per_sample: list[float] = []`,
  `for batch_mean in batch_means:`, `per_sample.append(max(0.0, min(1.0, val)))`,
  `return per_sample`); module setup (`logger = logging.getLogger("spectral_gap_loo")`,
  `compare_architecture_cis/loo_spectral_gap/permutation_test_gap_diff/spectral_gap_report`
  stats bindings, `logging.basicConfig(` call); `print_loo_table` body
  (`"Table 4: Spectral Gap LOO Stability — Kang (2026)"`, `n_bat/gm/lo/hi/rng/stab/n_samp/s_rng`
  get calls, `mark = "YES" if stab else "NO"`); `main()` architecture iteration
  (`for arch_idx, (arch, cfg) in enumerate(ARCH_CONFIGS.items()):`,
  `arch_seed = args.seed + arch_idx * 100`), epsilon-scale loop
  (`for scale in [0.5, 1.0, 1.5, 2.0]:`, `mean, ci_lo, ci_hi = bootstrap_ci(...)`),
  H3 final summary (`n_stable = sum(...)`, `n_total = len(arch_results)`,
  "Final H3 Confirmation / Refutation Summary" header, loo-threshold f-string,
  `worst = b_loo.get("worst_batch", None)`, pairwise i-loop).

### NeuroVoice R131
- `tests/test_correction_store_source_noarch.py` (+30, suite 7,971 → 8,001).
- 1st source-expression pass for `src/personalization/correction_store.py`:
  `@dataclasses.dataclass(slots=True)` decorator, `CorrectionRecord` field annotations
  (`audio_hash: str`, `corrected_text: str`, `predicted_vector_hash: str`,
  `timestamp: str  # ISO format`), key imports (`hashlib`, `json`, `datetime/timezone`),
  `CorrectionStore.__init__` body (`self._store_path`, `_encryption_key`, `_records`, `_load()`),
  `_load` body (`exists guard`, `open`, `json.load`, `[CorrectionRecord(**r) for r in data]`),
  `_save` body (`parent.mkdir`, `open("w")`, `json.dump([dataclasses.asdict(r) for r in ...])`),
  `add_correction` body (`hashlib.sha256(audio.tobytes()).hexdigest()`, `corrected_text`,
  `predicted_vector.tobytes()`, `datetime.now(timezone.utc).isoformat()`,
  `records.append`, `_save()`, logger.info), `count` return, `corrections_needed` return.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,298 | +30 |
| NeuroVoice | 8,001 | +30 |
| Combined | 14,299 | +60 |
| metaMirage | post-deadline | — |

---

## [08:11 UTC] Session — R132 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R132
- `tests/test_spectral_gap_loo_analysis_source_3_noarch.py` (+30, suite 6,298 → 6,328).
- 3rd source-expression pass for `experiments/spectral_gap_loo_analysis.py`:
  `validate_h3` residuals (`s_range = s_loo.get("loo_range", float("nan"))`,
  `s_stable = s_loo.get("stable", False)`, `b_mark = "stable" if b_stable else "UNSTABLE"`,
  `s_mark = "stable" if s_stable else "UNSTABLE"`, print f-string with both
  batch_loo_range and sample_loo_range marks, H3 → REFUTED branch, for-arch loop,
  `b_loo = data["batch_loo"]`); `main()` arch loop residuals
  (`batch_means = generate_batch_means(`, `arch_mu=cfg["mu"],`, `arch_sigma=scaled_sigma,`,
  `per_sample = generate_per_sample_gaps(`, `samples_per_batch=args.samples_per_batch,`,
  `arch_results[arch] = {`, `arch_per_sample[arch] = per_sample`,
  `rb_gaps = arch_per_sample["roberta-base"]`, `n_boot_values = [100, 500, 1000, 5000]`,
  `print_bootstrap_sensitivity(rb_gaps, n_boot_values, seed=args.seed)`);
  cross-arch/pairwise sections (`rep = comp[arch]`,
  `gp = arch_results[arch]["gap_report"]`, `excludes_zero = "YES" if rep["ci_lo"] > 0.0`,
  `stable_mark = "YES" if gp["loo_stable"] else "NO"`, `arch_a, arch_b = ranking[i], ranking[j]`,
  `perm = permutation_test_gap_diff(`, `n_perm=10_000,`, sig_label/pair_label, "Analysis complete.").

### NeuroVoice R132
- `tests/test_on_device_source_noarch.py` (+30, suite 8,001 → 8,031).
- 1st source-expression pass for `src/personalization/on_device.py`:
  module docstring (ExecuTorch context, IMPORTANT autograd warning, cloud-sync fallback),
  `if TYPE_CHECKING:` guard with `import numpy as np` / `from numpy.typing import NDArray`,
  `logger = logging.getLogger(__name__)`, `@dataclasses.dataclass(slots=True)`,
  all 5 `AdapterWeights` field annotations (`user_id: str`, `weights_path: Path`,
  `size_kb: float`, `n_corrections: int  # number of corrections used to train`, `version: int`),
  `OnDeviceAdapter.__init__` body (4 self._ assignments, `mkdir(parents=True, exist_ok=True)`),
  `get_adapter` body (f-string path, `if not weights_path.exists():`,
  `size_kb=weights_path.stat().st_size / 1024,`, `n_corrections=0,  # read from metadata`,
  `version=1,`), `update()` body (TODO/hardest-engineering comments, logger.info call),
  `has_minimum_corrections` body (adapter assignment, `return adapter.n_corrections >= minimum`).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,328 | +30 |
| NeuroVoice | 8,031 | +30 |
| Combined | 14,359 | +60 |
| metaMirage | post-deadline | — |

---

## [HH:MM UTC] Session — R133 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R133
- `tests/test_estimate_sps_source_2_noarch.py` (+30, suite 6,328 → 6,358).
- 2nd source-expression pass for `experiments/estimate_sps.py`:
  `delta_method_rsps_ci` all 7 kwargs, `AdversarialEmbeddingFamily`/`AdversarialEmbeddingConfig` init,
  `model.embeddings.word_embeddings`, `getattr(t_emb, "synonym_map", {})`, `build_sps_estimator`,
  `adv_core["sps"]`, `relative_sps(adv_sps, arb_sps)`, gap_result/gap_mean/all_gaps/gap_ci/gap_loo
  assignments, `LayerwiseSPSAnalyzer` import+init+`compute_profile_from_batches`, T_arb row,
  stable_str ternary, bootstrap CI f-string.

### NeuroVoice R133
- `tests/test_progress_federated_source_noarch.py` (+30, suite 8,031 → 8,061).
- 1st source-expression pass for `src/personalization/progress.py` + `src/personalization/federated.py`:
  constants, field annotations, property return expressions, status_message branches, DP_EPSILON/
  SYNC_INTERVAL_DAYS, logger, TYPE_CHECKING guard, init body, is_active, sync_gradients guard.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,358 | +30 |
| NeuroVoice | 8,061 | +30 |
| Combined | 14,419 | +60 |
| metaMirage | post-deadline | — |

---

## [10:13 UTC] Session — R134 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R134
- `tests/test_jacobian_source_2_noarch.py` (+30, suite 6,358 → 6,388).
- 2nd source-expression pass for `src/sps/jacobian.py`:
  module imports (logging, dataclasses, Callable/Optional, torch.nn as nn,
  TransformationFamily, normalize_directions), logger binding, __all__ items
  (SpectralGapResult, spectral_gap, verify_spectral_gap_bound), __repr__ f-string
  fragments (opening/mean_restricted_norm/mean_gap), restricted_operator_norm for-k
  loop body (v_k slice, jvp_norm call, norms_per_dir append), adversarial_worst_direction
  gather expression (directions[torch.arange, worst_k], return worst_dirs),
  full_spectral_norm loop residuals (shape unpack, device, for-_, normalize_directions),
  return max_norm, spectral_gap return SpectralGapResult kwargs (restricted_norm/
  full_spectral_norm/normalized_gap .detach()), verify_spectral_gap_bound residuals
  (import math, theorem3 = math.exp(-lipschitz_constant)).

### NeuroVoice R134
- `tests/test_inference_source_noarch.py` (+30, suite 8,061 → 8,091).
- 1st source-expression pass for `src/serving/inference.py` (StreamingDecoder):
  module imports (deque, Callable/Optional, NeuroVoicePipeline, HiFiGANVocoder,
  NeuralPreprocessor), __all__, docstring fragments (<200ms, thread-safety, ring-buffer
  comment), __init__ body (device/pipeline/vocoder/buffer/samples init), push() body
  (transform_stream, for-t loop, buffer append, samples increment/decrement),
  _infer() body (np.stack window, from_numpy unsqueeze, pipeline call, art_np/audio_np
  squeeze), reset() body (clear, reset_stream), decode_offline() body (transform, np.pad edge).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,388 | +30 |
| NeuroVoice | 8,091 | +30 |
| Combined | 14,479 | +60 |
| metaMirage | post-deadline | — |

---

## [11:12 UTC] Session — R135 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R135
- `tests/test_transformations_source_3_noarch.py` (+30, suite 6,388 → 6,418).
- 3rd source-expression pass for `src/sps/transformations.py`:
  `SynonymSubstitutionFamily.sample()` loop body residuals (`B, seq_len, _ = embeddings.shape`,
  `for b in range(B):`, `if n_substituted >= self.config.max_substitutions:`,
  `tid = input_ids[b, pos].item()`, `syn = candidates[torch.randint(0, len(candidates), (1,)).item()]`,
  `perturbed_ids[b, pos] = syn`, `n_substituted += 1`,
  `perturbed_embeddings = self.embedding_layer(perturbed_ids)`,
  `valid.view(B, 1, 1).expand_as(embeddings),`, `return perturbed_embeddings, magnitudes`);
  `SSF.semantic_directions()` body (`B, seq_len, hidden = embeddings.shape`, `K = 1`,
  `all_dirs: list[torch.Tensor] = []`, `for s in range(seq_len):`,
  `tid = input_ids[b, s].item()`, `syn_ids[b, s] = candidates[...]`,
  `syn_emb = self.embedding_layer(syn_ids)`, `delta = syn_emb - embeddings`,
  `delta = delta + self.config.substitution_prob * torch.randn_like(delta)`,
  `return torch.cat(all_dirs, dim=1)`);
  `build_wordnet_synonym_map()` inner-loop residuals (`word = tokenizer.decode([token_id]).strip()`,
  `if not word.isalpha() or len(word) < 2:`, `synsets = wn.synsets(word)`,
  `synonym_tokens: list[int] = []`, `seen: set[str] = {word}`,
  `syn_word = lemma.name().replace("_", " ")`,
  `enc = tokenizer.encode(syn_word, add_special_tokens=False)`, `if len(enc) == 1:`,
  `if synonym_tokens: synonym_map[token_id] = synonym_tokens`, logger.info format string).

### NeuroVoice R135
- `tests/test_serving_session_adapt_source_noarch.py` (+30, suite 8,091 → 8,121).
- 1st source-expression pass for `src/serving/session_adapt.py`:
  module imports (`from pathlib import Path`, `from loguru import logger`,
  `from torch.utils.data import DataLoader, TensorDataset`,
  `from ..models.pipeline import NeuroVoicePipeline`), `__all__`,
  signature defaults (`n_steps: int = 200`, `lr: float = 5e-5`,
  `update_norm_layers_only: bool = True`), docstring landmarks
  (calibration target 20 utterances, sEMG drift cause);
  `adapt_to_session()` Strategy-1 body (`dev = torch.device(device)`,
  `pipeline = pipeline.to(dev)`, `_update_norm_stats(pipeline, calibration_neural.to(dev))`,
  `update_norm_layers_only or calibration_articulatory is None` guard,
  norm-only logger msg, `return pipeline.cpu()`, `dataset = TensorDataset(`,
  `DataLoader(..., batch_size=min(4, len(dataset)), ...)`);
  Strategy-2 body (`AdamW(pipeline.parameters(), lr=lr, weight_decay=0.0)`,
  `nn.MSELoss()`, `while step < n_steps:`, `T = min(pred.shape[1], art_target.shape[1])`,
  `loss = criterion(pred[:, :T, :], art_target[:, :T, :])`,
  `nn.utils.clip_grad_norm_(pipeline.parameters(), 1.0)`,
  `"Session adaptation complete."`);
  `_update_norm_stats()` body (`bn_layers` list-comp, BatchNorm1d debug msg,
  `m.momentum = 0.1  # restore default momentum`).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,418 | +30 |
| NeuroVoice | 8,121 | +30 |
| Combined | 14,539 | +60 |
| metaMirage | post-deadline | — |

---

## [12:22 UTC] Session — R136 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R136
- `tests/test_spectral_gap_loo_analysis_source_4_noarch.py` (+30, suite 6,418 → 6,448).
- 4th source-expression pass for `experiments/spectral_gap_loo_analysis.py`:
  `parse_args()` body, `print_bootstrap_sensitivity()` header block,
  main() startup `logger.info` format string, row f-string column specs.

### NeuroVoice R136
- `tests/test_emg2pose_dataset_source_noarch.py` (+30, suite 8,121 → 8,151).
- 1st source-expression pass for `src/data/emg2pose_dataset.py` (460 lines, first source pass).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,448 | +30 |
| NeuroVoice | 8,151 | +30 |
| Combined | 14,599 | +60 |
| metaMirage | post-deadline | — |

## [02:30 UTC] Session — R137 (satellite)

### R137 Progress (SPS + NeuroVoice)
- SPS R137: `tests/test_cross_arch_adversarial_source_2_noarch.py` (+30, suite 6,448 → 6,478).
  2nd source-expression pass for `experiments/cross_arch_adversarial.py` —
  `prepare_batches()` body, `collect_spectral_gaps()` + `validate_hypotheses()` body,
  `serialise_results()` + `parse_args()` body. All 30 passed in 0.08s.
- NeuroVoice R137: `tests/test_gaddy_dataset_source_noarch.py` (+30, suite 8,151 → 8,181).
  1st source-expression pass for `src/data/gaddy_dataset.py` (455 lines) — constants,
  `GaddyUtterance` slots/repr, `GaddyDataset.__init__` mode/vocab guards/FileNotFoundError,
  `_load` vocab_dirs, `_load_mode_dir` info_path/emg_path/json/np.load, `gaddy_collate_fn`
  emg_lengths/T_max/torch.zeros. All 30 passed in 0.07s.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,478 | +30 |
| NeuroVoice | 8,181 | +30 |
| Combined | 14,659 | +60 |
| metaMirage | post-deadline | — |

---

## [13:16 UTC] Session — R138 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R138
- `tests/test_metrics_source_3_noarch.py` (+30, suite 6,478 → 6,508).
- 3rd source-expression pass for `src/sps/metrics.py`:
  `LayerwiseSPSAnalyzer.__init__` body (self._layers = self._find_layers(model),
  raise ValueError / "Could not enumerate" message, NotImplementedError stub for
  compute_profile pointing to compute_profile_from_batches);
  `compute_profile_from_batches` loop body (profile list, logger.info per layer,
  sps_l = _compute_sps_at_layer, append, return profile);
  `_compute_sps_at_layer` hook internals (captured dict, isinstance(output, tuple),
  output[0][:, 0, :].detach() / output[:, 0, :].detach() / output.detach() branches,
  _LayerProxy class, register_forward_hook, handle.remove, divergence_fn / proxy wiring);
  `full_sps_analysis` spectral gap block (emb = batch.get("embeddings"), raise ValueError,
  semantic_directions call, _fn hasattr check, normalized_gap.tolist(), bootstrap_ci);
  relative block (estimate_arbitrary_sps return_full, relative_sps, delta_method_rsps_ci);
  layerwise block (LayerwiseSPSAnalyzer init + compute_profile_from_batches call).

### NeuroVoice R138
- `tests/test_gaddy_dataset_source_2_noarch.py` (+30, suite 8,181 → 8,211).
- 2nd source-expression pass for `src/data/gaddy_dataset.py`:
  GADDY_CHANNEL_NAMES entries; `_reshape_emg` body (ndim guard, shape[1]/shape[0] branches,
  emg_raw.T / emg_raw assigns, neither-dim warning, return None / return emg);
  `_normalize_text` body (lower/strip, re.sub drop-non-alpha, re.sub collapse-spaces);
  `__getitem__` body (utt assignment, sample dict keys, tokenizer block);
  `vocabulary_stats()` return dict; `top_n_words()` sorted lambda; `utterances_in_vocab()`
  out list / enumerate / issubset / append; `gaddy_collate_fn` texts / session_ids /
  is_silent keys, target_lengths tensor.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,508 | +30 |
| NeuroVoice | 8,211 | +30 |
| Combined | 14,719 | +60 |
| metaMirage | post-deadline | — |

---

## [14:17 UTC] Session — R139 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R139
- `tests/test_sps_core_source_3_noarch.py` (+30, suite 6,508 → 6,538).
- 3rd source-expression pass for `src/sps/core.py`:
  `AdversarialSPSEstimator._collect_sensitivities` residuals not in R82 — `for batch in
  batches:`, `input_ids/attention_mask .to(config.device)`, `embeddings batch .to(config.device)`,
  `sens = estimator.sens_estimator.estimate(`, `all_sens.append(sens.cpu())`,
  `if not all_sens: return []`, `return torch.cat(all_sens).tolist()`, docstring fragment;
  `AdversarialSPSEstimator.estimate()` result keys not in R82 —
  `mean_sens_emb/std_sens_emb/n_samples from emb_result`,
  `mean_sens_adv/std_sens_adv from adv_result`, `adv_sens_ci = bootstrap_ci(adv_sens_list)`,
  `loo_adv = loo_spectral_gap(adv_sens_list)`, logger format string, `loo_emb=/loo_adv=` kwargs;
  `RelativeSPSEstimator.estimate()` result keys not in R79 —
  `mean_sens_t/std_sens_t/n_samples_t from t_result`,
  `mean_sens_arb/std_sens_arb/n_samples_arb from arb_result`, logger format fragment,
  `sens_ci_t = bootstrap_ci(...)`, `loo_t = loo_spectral_gap(...)`, `n_samples_arb=` kwarg.

### NeuroVoice R139
- `tests/test_emg2pose_dataset_source_2_noarch.py` (+30, suite 8,211 → 8,241).
- 2nd source-expression pass for `src/data/emg2pose_dataset.py`:
  `_reshape_and_resample` body — `@staticmethod` signature, param annotations,
  `-> np.ndarray | None:` return type, docstring, `shape[1] == EMG2POSE_NATIVE_CHANNELS`,
  `shape[0] == EMG2POSE_NATIVE_CHANNELS`, fallback `shape[1] in (8, 16, 32)`,
  fallback `shape[0] in (8, 16, 32)`, "neither dim is a known channel count" warning,
  `return np.asarray(emg, dtype=np.float32)`;
  `subject_ids()`/`n_subjects()`/`stats()` — `sorted({r.subject_id for r in ...})`,
  `return len(self.subject_ids())`, empty-recordings guard, `total_samples` sum,
  `total_seconds` formula, `avg_recording_seconds`, `n_channels`, `n_subjects()` call;
  `__getitem__` preprocessor block — `if self.preprocessor is not None:`, `try:`,
  `except RuntimeError:`, `preprocessor.fit`, `preprocessor.transform`, `sr = recording.sample_rate`,
  `"sample_rate": sr,`; `emg2pose_collate_fn` residuals — `"subject_ids"` / `"session_ids"` list-comps.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,538 | +30 |
| NeuroVoice | 8,241 | +30 |
| Combined | 14,779 | +60 |
| metaMirage | post-deadline | — |

---

## [15:16 UTC] Session — R140 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R140
- `tests/test_stats_source_2_noarch.py` (+30, suite 6,538 → 6,568).
- 2nd source-expression pass for `src/sps/stats.py`:
  `loo_batch_spectral_gap` body — `n = len(per_batch_means)`, `if n < 3:` guard,
  `return {"stable": False, "n_batches": n}`, `total = sum(per_batch_means)`,
  `loo_means = [(total - g) / (n - 1) for g in per_batch_means]`,
  `grand_mean = total / n`, `worst_idx = max(range(n), key=lambda i: abs(loo_means[i] - grand_mean))`,
  `"n_batches": n,`, `"grand_mean": round(grand_mean, 6),`, `"worst_batch": worst_idx,`;
  `spectral_gap_report` body — `bootstrap_ci/loo_spectral_gap` calls, `.get("stable", False)`,
  `.get("loo_range", 0.0)`, all 6 return dict keys; `compare_architecture_cis` —
  `reports: dict[str, dict] = {}`, for-loop, `spectral_gap_report` call, ranking lambda;
  `permutation_test_gap_diff` — `pooled = gaps_a + gaps_b`, `obs_diff`, `perm_diff`,
  two-sided abs guard, `p_value = (extreme + 1) / (n_perm + 1)`, `"n_a": na, "n_b": nb`.

### NeuroVoice R140
- `tests/test_gaddy_calibration_source_noarch.py` (+30, suite 8,241 → 8,271).
- 1st source-expression pass for `src/data/gaddy_calibration.py` (355 lines):
  `GADDY_SAMPLE_RATE: int = 1000`, all 4 `__all__` entries, `if TYPE_CHECKING:` guard,
  `from src.data.gaddy_dataset import GaddyDataset` type-hint import,
  `class GaddyCalibrationDataset:`, `self.window_samples = int(window_ms * GADDY_SAMPLE_RATE / 1000)`,
  `self.stride_samples = int(stride_ms * GADDY_SAMPLE_RATE / 1000)`,
  `self._windows: list[Tensor] = []`, `self._build()`,
  `if u.session_id.startswith(self.session_prefix)`, `emg = utt.emg`,
  `while start + self.window_samples <= T:`,
  `torch.from_numpy(win[np.newaxis, ...].astype(np.float32))`,
  max_windows guard, `"n_windows": len(self._windows),`,
  `ds = GaddyCalibrationDataset(`, `return ds.as_list()`,
  feature-alignment loss body (mean_loss/std_loss/return sum),
  temporal-consistency loss body (rep_norm, prev_rep guard, cos_sim, smoothness return).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,568 | +30 |
| NeuroVoice | 8,271 | +30 |
| Combined | 14,839 | +60 |
| metaMirage | post-deadline | — |

---

## [16:15 UTC] Session — R141 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R141
- `tests/test_sps_utils_source_2_noarch.py` (+30, suite 6,568 → 6,598).
- 2nd source-expression pass for `src/sps/utils.py`:
  module imports (`from __future__`, `import logging/random`, `from typing import Callable, Literal`,
  `import numpy as np`, `import torch.nn.functional as F`, `logger = logging.getLogger(__name__)`),
  module docstring lines; per-function docstring fragments (description/returns/raises for all 5
  exported functions); return-type annotations (`-> torch.Tensor:` × 4, `-> DivergenceFn:`);
  param annotations (`a: torch.Tensor`, `b: torch.Tensor`, `name: DivergenceType`);
  ValueError message (`"Unknown divergence"`, `"Valid options: 'cosine', 'l2'."`).

### NeuroVoice R141
- `tests/test_gaddy_calibration_source_2_noarch.py` (+30, suite 8,271 → 8,301).
- 2nd source-expression pass for `src/data/gaddy_calibration.py`:
  `_build` deeper body — `utterances = list(self.gaddy_dataset._utterances)`,
  `if self.session_prefix is not None:`, `for utt in utterances:`, `T = emg.shape[1]`,
  `start = 0`, `win = emg[:, start : start + self.window_samples]`,
  `start += self.stride_samples`, `self.preprocessor.fit/transform(emg)`, `except Exception:`;
  public API — `return len/iter/list(self._windows)`; `summary()` dict — all 5 remaining keys
  and duration formula; `make_temporal_consistency_loss` deeper body — `_prev_rep` init,
  dim-3 pooling, zeros first-call return, prev_norm assignment, shape-mismatch guard, detach update;
  `make_feature_alignment_loss` dim promotions — `if z.dim() == 1:`, `z.unsqueeze(0).unsqueeze(0)`,
  `elif z.dim() == 2:`.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,598 | +30 |
| NeuroVoice | 8,301 | +30 |
| Combined | 14,899 | +60 |
| metaMirage | post-deadline | — |

---

## [17:09 UTC] Session — R142 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R142
- `tests/test_sps_init_source_2_noarch.py` (+30, suite 6,598 → 6,628).
- 2nd source-expression pass for `src/sps/__init__.py`:
  module docstring (title, "Reference: Kang (2026)", framework subtitle),
  `from __future__ import annotations`; core residuals (`AdversarialSPSReport`,
  `RelativeSPSEstimator`, `SPSEstimator`, `StructuredSensitivityEstimator`,
  `build_relative_sps_estimator`, `build_sps_estimator`); jacobian residuals
  (`full_spectral_norm`, `restricted_operator_norm`, `spectral_gap`); metrics
  residuals (`SPSReport`, `full_sps_analysis`); transformations residuals
  (`AdversarialEmbeddingFamily`, `EmbeddingPerturbationConfig`,
  `EmbeddingPerturbationFamily`, `SynonymSubstitutionFamily`,
  `build_wordnet_synonym_map`); `__all__` section comments (`# jacobian`,
  `# metrics`, `# transformations`, `# utils`); additional `__all__` entries
  (`RelativeSPSEstimator`, `StructuredSensitivityEstimator`, `spectral_gap`,
  `full_sps_analysis`, `build_wordnet_synonym_map`, `set_seed`).

### NeuroVoice R142
- `tests/test_preprocessing_source_noarch.py` (+30, suite 8,301 → 8,331).
- 1st source-expression pass for `src/data/preprocessing.py` (274 lines):
  module docstring (pipeline steps 1–3, IMPORTANT causal/zero-phase note,
  sosfiltfilt non-causal warning); imports (`from __future__ import annotations`,
  `from scipy.signal import butter, sosfiltfilt, iirnotch, sosfilt_zi, sosfilt`,
  `from loguru import logger`); `__all__`; `@dataclass(slots=True)`;
  `PreprocessConfig` all 6 field defaults (`sample_rate=1000.0`, `highpass_hz=20.0`,
  `lowpass_hz=450.0`, `notch_hz=field(default_factory=lambda: [50.0, 60.0])`,
  `apply_car=False`, `filter_order=4`) + inline comment; `NeuralPreprocessor.__init__`
  body (`_bp_sos` / `_notch_sos` build calls, `_channel_mean`/`_channel_std` None
  annotations); `_build_bandpass` body (`nyq = cfg.sample_rate / 2.0`, `butter` call);
  `_build_notches` body (`iirnotch`, `from scipy.signal import tf2sos`);
  `fit()` channel mean/std assignment; `transform()` fit-guard + z-score formula;
  `fit_transform` one-liner.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,628 | +30 |
| NeuroVoice | 8,331 | +30 |
| Combined | 14,959 | +60 |
| metaMirage | post-deadline | — |

---

## [18:14 UTC] Session — R143 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R143
- `tests/test_estimate_sps_source_3_noarch.py` (+30, suite 6,628 → 6,658).
- 3rd source-expression pass for `experiments/estimate_sps.py`:
  module docstring residuals (title, "Computes SPS, relative SPS...", "Kang (2026) Definitions 2, 5, 6, 7, 8."),
  `sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))`,
  stdlib imports (`import argparse`, `import logging`, `import sys`, `from pathlib import Path`, `import torch`),
  `from sps import (` block opener; `logging.basicConfig` details (level, format, datefmt),
  `logger = logging.getLogger("estimate_sps")`, `argparse.ArgumentParser` description,
  `--m-transforms` / `--n-directions` / `--batch-size` / `--seed` arg defaults, `return p.parse_args()`;
  `set_seed(args.seed)`, `AutoTokenizer/AutoModel.from_pretrained`, `model.eval()`,
  `embedding_layer = model.get_input_embeddings()`, `synonym_map: dict[int, list[int]] = {}`,
  `nltk.data.find("corpora/wordnet")`, `vocab_size=10_000`, `max_synonyms_per_token=5`,
  `logger.warning` WordNet fallback.

### NeuroVoice R143
- `tests/test_preprocessing_source_2_noarch.py` (+30, suite 8,331 → 8,361).
- 2nd source-expression pass for `src/data/preprocessing.py`:
  `_bp_zi` / `_notch_zi` streaming state init, `cfg = self.config`, `low/high = cfg.*_hz / nyq`,
  `return sos`, `sosl: list[np.ndarray] = []`, `nyq = self.config.sample_rate / 2.0`,
  `sosl.append(tf2sos(b, a))`, `return sosl`; fit() — `filtered = self._filter(data)`,
  `if self.config.apply_car:`, `filtered = self._common_average_reference(filtered)`,
  `self._channel_std  = filtered.std(axis=1).clip(min=1e-8)`, `return self`;
  transform() — `x = self._filter(data)`, CAR call, `return x.astype(np.float32)`;
  transform_stream() RuntimeError, `C = chunk.shape[0]`, `sosfilt_zi` init (bp + notch),
  `np.repeat(..., axis=1).astype(np.float64)`, `sosfilt` calls (bp + notch);
  epoch() `C, T = data.shape`, `np.stack([data[:, s:s+W] for s in starts])`;
  `_filter` `sosfiltfilt` call; `_common_average_reference` return.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 6,658 | +30 |
| NeuroVoice | 8,361 | +30 |
| Combined | 15,019 | +60 |
| metaMirage | post-deadline | — |
