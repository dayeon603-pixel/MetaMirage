# metaMirage Devlog — 2026-06-15

## [00:00 UTC] Session — R218 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R218
- `tests/test_spectral_gap_loo_analysis_source_10_noarch.py` (+30, suite 8,878 → 8,908).
- 10th source pass for `experiments/spectral_gap_loo_analysis.py`.
  Prior devlog suggested jacobian.py 4th pass, but jacobian.py was already exhausted
  through R197 (7 passes). Pivoted to spectral_gap_loo_analysis.py which has remaining
  uncovered territory in Sections 0/1/5/6/8.
  New strings: `"epsilon_scale    : {args.epsilon_scale:.2f}  (sigma multiplier)"`,
  Section 1 generate_batch_means kwargs (arch_mu/arch_sigma=scaled_sigma/n_batches/seed=arch_seed),
  `"per_sample = generate_per_sample_gaps("` + kwargs, `"b_loo = loo_batch_spectral_gap(batch_means)"`,
  Section 1 results dict keys ("batch_means"/"batch_loo"/"sample_loo"/"gap_report"),
  `"arch_per_sample[arch] = per_sample"`, Section 5 ranking header + `for rank_i, arch in enumerate`,
  `gp = arch_results[arch]["gap_report"]`, Section 6 perm test block (arch_a/arch_b unpack /
  `perm = permutation_test_gap_diff(` / n_perm=10_000, / sig_label / pair_label),
  Section 8 (`"Final H3 Confirmation / Refutation Summary"` / `worst = b_loo.get("worst_batch", None)` /
  H3 CONFIRMED string / `"Analysis complete."`).

### NeuroVoice R218
- `tests/test_vocabulary_tiers_source_6_noarch.py` (+30, suite 10,581 → 10,611).
- 6th source pass for `src/emg/vocabulary_tiers.py` (338 lines).
  Pivot from tier1_template.py (near-exhausted, 14 passes) and stream.py (near-exhausted,
  3 passes on 141 lines). vocabulary_tiers.py had a full uncovered category: method def
  signature lines and enum value assignments never tested as raw strings.
  New strings: `TIER_1/2/3 = auto()` enum values, VocabularyTier property def signatures
  (vocab_size_cap/uses_hard_trie_constraint/uses_soft_trie_bias/next_tier_utterances_needed),
  `"# ── SONAR-based vocabulary expansion"` / `"# ── Internal helpers"` section comments,
  UVP method def lines (record_utterance/to_dict/from_dict/save/load), expand function def +
  top_k param + profile/Returns/Raises doc, `def _compute_tier(...)` def line,
  expand body residuals (expanded list init / word_idx loop / row assign / for idx / word from
  pool / if not in expanded / append / Cap comment), _compute_tier docstring arg + Returns.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,908 | +30 |
| NeuroVoice | 10,611 | +30 |
| Combined | 19,519 | +60 |
| metaMirage | post-deadline | — |

---

## [~01:00 UTC] Session — R219 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R219
- `tests/test_spectral_gap_loo_analysis_source_11_noarch.py` (+30, suite 8,908 → 8,938).
- 11th source pass for `experiments/spectral_gap_loo_analysis.py`.
  Per-arch logger.info format string (grand_mean/batch_loo_range/gap_CI/loo_stable tokens),
  Section 4 (rb_gaps/n_boot_values/print_bootstrap_sensitivity call/caption print),
  Section 8 stable-count loop (n_stable/for data in arch_results/if batch_loo.get stable),
  Section 5 ranking row f-strings (rank_i/mean-ci_lo-ci_hi/ci_width-excludes_zero-stable_mark),
  Section 5 H-hypothesis block (ci_lo_pos/CONFIRMED-REFUTED f-string/rep = comp[arch]),
  Section 7 bootstrap_ci call with ps + CI_width expression,
  Section 8 final-summary prints (H3 REFUTED/across batches/worst-batch outliers/
  passing H3 threshold/YES-NO b_stab).

### NeuroVoice R219
- `tests/test_calibration_source_1_noarch.py` (+30, suite 10,611 → 10,641).
- 1st source pass for `src/emg/calibration.py` (193 lines).
  Module docstring (LoRA/CNN-LSTM/Random Forest), constants (MIN_REPS_PER_WORD/RECOMMENDED_REPS),
  CalibrationSample fields (word_index/emg_data/timestamp_ms), CalibrationProgress docstring +
  fields (target_vocabulary/samples_per_word/min_reps/recommended_reps), property docstrings +
  bodies (is_minimum_met/is_recommended_met/total_samples/next_word), status_message docstring +
  return values (Calibration complete / Basic calibration done / more samples needed),
  EMGCalibrator.__init__ body (word_to_idx dict comp / data_dir / _samples / mkdir / _load_existing),
  _load_existing logger.info format.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,938 | +30 |
| NeuroVoice | 10,641 | +30 |
| Combined | 19,579 | +60 |
| metaMirage | post-deadline | — |

---

## [02:10 UTC] Session — R220 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R220
- `tests/test_estimate_sps_source_4_noarch.py` (+30, suite 8,938 → 8,968).
- 4th source pass for `experiments/estimate_sps.py` (558 lines, 3 prior passes).
  Pivot from spectral_gap_loo_analysis.py (substantially exhausted after 11 passes).
  New strings: prepare_batches docstring (tokenize sentence + batch-dict column headers),
  tokenizer call kwargs (padding/truncation/max_length/return_tensors), batch dict key
  construction ("input_ids"/"embeddings"), parse_args skip flags (--device / --skip-layerwise /
  --skip-wordnet / --skip-gap-loo / --skip-adversarial), main() data prep logger.info
  (Tokenizing %d / Building T_emb), prepare_batches call (TEST_SENTENCES/tokenizer/embedding_layer
  + batch_size/device kwargs), SPSConfig kwargs (epsilon/m_transforms/divergence="cosine"),
  EmbeddingPerturbationConfig init (n_directions/use_synonym_directions), T_emb Family init
  (embedding_layer=embedding_layer), estimator = build_sps_estimator(model, t_emb, config).

### NeuroVoice R220
- `tests/test_calibration_source_2_noarch.py` (+30, suite 10,641 → 10,671).
- 2nd source pass for `src/emg/calibration.py` (193 lines).
  New strings: add_sample docstring (Args/Raises), word-guard (if word not in self._word_to_idx /
  not in vocabulary msg), CalibrationSample init (sample = CalibrationSample / word_index kwarg /
  self._samples.append), np.save + self._save_meta, _save_meta docstring + meta dict keys
  (user_id / vocabulary_size / total_samples / samples_per_word) + json.dump,
  _count_per_word body (counts init / increment / return),
  get_progress body (return CalibrationProgress / target_vocabulary / samples_per_word),
  get_training_data docstring + body (data/labels list comps + return).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,968 | +30 |
| NeuroVoice | 10,671 | +30 |
| Combined | 19,639 | +60 |
| metaMirage | post-deadline | — |

---

## [03:10 UTC] Session — R221 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R221
- `tests/test_estimate_sps_source_5_noarch.py` (+30, suite 8,968 → 8,998).
- 5th source pass for `experiments/estimate_sps.py` (558 lines).
  New strings: core = estimator.estimate(iter(batches)), logger.info for T_emb estimation,
  arb_result = estimate_arbitrary_sps + arb_sps + rsps = relative_sps, logger.info for T_arb,
  rsps_ci: tuple | None = None, rsps_ci = delta_method_rsps_ci( + emb_mean_s/arb_mean_s kwargs,
  adv_sps/adv_rsps: float | None = None inits, logger.info "Computing spectral gap ...",
  gap_result = None, gap_mean: float | None = None, all_gaps: list[float] = [],
  gap_ci: tuple | None = None, gap_loo: dict | None = None, logger.info gap LOO all-batches,
  layerwise: list[float] | None = None, rows dict ("Family": "T_emb"/"T_arb"/"T_adv"),
  "rSPS": f"{rsps:.4f}" / "1.0000" / if adv_sps is not None: / ci_str f-string /
  CI excludes/includes 1 interp strings, logger.info "Computing layer-wise SPS profile ...".

### NeuroVoice R221
- `tests/test_calibration_source_3_noarch.py` (+30, suite 10,671 → 10,701).
- 3rd source pass for `src/emg/calibration.py` (193 lines).
  New strings: all module imports (from __future__ / dataclasses / json / logging /
  pathlib.Path / typing.TYPE_CHECKING / numpy / NDArray), logger = logging.getLogger(__name__),
  CalibrationSample `word: str` field, constructor kwargs (word=word / emg_data=emg_data /
  timestamp_ms=timestamp_ms), sample_path f-string assignment, raise ValueError(msg),
  # Save to disk comment, def add_sample( + timestamp_ms: int = 0, param, @property decorator,
  def _load_existing() + docstring + meta_path assignment + if exists + open + json.load +
  count.get("total_samples"), def get_training_data() signature + Returns doc,
  def next_word() signature.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,998 | +30 |
| NeuroVoice | 10,701 | +30 |
| Combined | 19,699 | +60 |
| metaMirage | post-deadline | — |

---

## [04:10 UTC] Session — R222 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R222
- `tests/test_estimate_sps_source_6_noarch.py` (+30, suite 8,998 → 9,028).
- 6th source pass for `experiments/estimate_sps.py` (558 lines, 5 prior passes).
  New strings: emb_std_s/emb_n/arb_std_s/arb_n delta-method kwargs, adv_ratio computation,
  adversarial analysis print block (header / ratio line / near-adversarial < 1.1 guard /
  near-adversarial print lines), moderate-gap (< 2.0) and large-gap print branches,
  spectral gap analysis header + mean_restricted_norm / mean_full_norm / mean_gap print lines,
  strong/weak gamma-bar separation print branches, gap_ci unpack + all-batches CI print +
  mean_g / lo_g / hi_g tokens, gap_loo stable/unstable string, layer-wise bar chart █ format.

### NeuroVoice R222
- `tests/test_lexicon_rescorer_source_1_noarch.py` (+30, suite 10,701 → 10,731).
- 1st source pass for `src/correction/lexicon_rescorer.py` (244 lines, fresh file).
  Pivot from `src/emg/calibration.py` (near-exhausted after 3 passes). New strings: module
  docstring (CTC non-words / C++ build / score formula / λ=0 / WER claims), all 4 imports,
  levenshtein() signature + max_d early-exit guard + two-row DP comment + prev/curr init +
  cost + deletion/insertion/substitution costs + return max_d+1, build_lexicon def + doc +
  counter init/increment/return, LexiconRescorer __slots__ + max_edit_distance guard +
  error message + sort comment + _words sorted assignment.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,028 | +30 |
| NeuroVoice | 10,731 | +30 |
| Combined | 19,759 | +60 |
| metaMirage | post-deadline | — |

---

## [05:14 UTC] Session — R223 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R223
- `tests/test_estimate_sps_source_7_noarch.py` (+30, suite 9,028 → 9,058).
- 7th (final) source pass for `experiments/estimate_sps.py`.
  Covered: print_results_table line-join body (print(sep)/line = "  ".join/str(r.get(k,"—"))/
  print(line)), results section framing (print("\n" + "=" * 65) / print("=" * 65) /
  print("\n" + "=" * 65 + "\n")), compute_all_spectral_gaps debug logger + return,
  rSPS tolerance block (_tol = 5e-4 / if rsps > 1.0 + _tol / < 1.0 - _tol / ≈ 1 print
  branches / if ci_str / if ci_interp), control-flow guards (if rsps_ci/gap_result/gap_ci/
  gap_loo/layerwise is not None), LOO stability range print substrings, layer enumeration
  (for i, val in enumerate(layerwise) / Layer {i:2d}:  {val:.4f}  {bar}).
  `estimate_sps.py` is now substantially exhausted after 7 passes (210 total assertions).

### NeuroVoice R223
- `tests/test_lexicon_rescorer_source_2_noarch.py` (+30, suite 10,731 → 10,761).
- 2nd source pass for `src/correction/lexicon_rescorer.py` (244 lines).
  Covered: levenshtein inner-loop residuals (curr[0] = i / row_min init + update guard /
  if max_d is not None and row_min > max_d / prev, curr = curr, prev / return prev[m]),
  LexiconRescorer.__init__ all 4 field assignments (self._word_log_probs / .max_edit_distance /
  .lm_weight / .keep_unmatched), from_counts classmethod (@classmethod / def / docstring /
  if not word_counts guard / total / log_probs = { / w: math.log / for w, c in word_counts /
  smoothing: float = 1.0), restrict_to dict comprehension, snap_word body (in-vocab fast path /
  best/best_score inits / levenshtein call + score formula / return keep_unmatched / return best).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,058 | +30 |
| NeuroVoice | 10,761 | +30 |
| Combined | 19,819 | +60 |
| metaMirage | post-deadline | — |
