# metaMirage Devlog — 2026-06-15

## [00:00 UTC] Session — R218 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R218
- `tests/test_spectral_gap_loo_analysis_source_10_noarch.py` (+30, suite 8,878 → 8,908).
- 10th source pass for `experiments/spectral_gap_loo_analysis.py`.
  New strings: epsilon_scale print, Section 1 generation loop kwargs, Section 1 LOO/results
  storage, Section 5 ranking table, Section 6 perm tests, Section 8 final H3 summary.

### NeuroVoice R218
- `tests/test_vocabulary_tiers_source_6_noarch.py` (+30, suite 10,581 → 10,611).
- 6th source pass for `src/emg/vocabulary_tiers.py` (338 lines).
  New strings: TIER_1/2/3 = auto() enum values, property def signatures, section comments,
  UVP method def lines, expand body residuals, _compute_tier def + docstring.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,908 | +30 |
| NeuroVoice | 10,611 | +30 |
| Combined | 19,519 | +60 |
| metaMirage | post-deadline | — |

---

## [~01:00 UTC] Session — R219 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R219
- `tests/test_spectral_gap_loo_analysis_source_11_noarch.py` (+30, suite 8,908 → 8,938).
- 11th source pass for `experiments/spectral_gap_loo_analysis.py`.
  Per-arch logger.info format string, Section 4 rb_gaps/n_boot_values/sensitivity call,
  Section 5 ranking row f-strings + H-hypothesis block, Section 7 bootstrap_ci/CI_width,
  Section 8 n_stable loop + summary prints.

### NeuroVoice R219
- `tests/test_calibration_source_1_noarch.py` (+30, suite 10,611 → 10,641).
- 1st source pass for `src/emg/calibration.py` (193 lines).
  Module docstring (LoRA/CNN-LSTM/Random Forest), constants (MIN_REPS_PER_WORD/RECOMMENDED_REPS),
  CalibrationSample fields, CalibrationProgress docstring + fields + property bodies,
  status_message docstring + return values, EMGCalibrator.__init__ body + _load_existing logger.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,938 | +30 |
| NeuroVoice | 10,641 | +30 |
| Combined | 19,579 | +60 |
| metaMirage | post-deadline | — |

---

## [02:10 UTC] Session — R220 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R220
- `tests/test_estimate_sps_source_4_noarch.py` (+30, suite 8,938 → 8,968).
- 4th source pass for `experiments/estimate_sps.py` (558 lines, 3 prior passes).
  New strings: prepare_batches docstring (tokenize sentence + batch-dict column headers),
  tokenizer call kwargs, batch dict key construction, parse_args skip flags,
  main() data prep logger.info calls, SPSConfig + EmbeddingPerturbationConfig +
  Family init, estimator = build_sps_estimator.

### NeuroVoice R220
- `tests/test_calibration_source_2_noarch.py` (+30, suite 10,641 → 10,671).
- 2nd source pass for `src/emg/calibration.py` (193 lines).
  New strings: add_sample docstring (Args/Raises), word-guard, CalibrationSample init,
  np.save + _save_meta, _save_meta meta dict keys + json.dump, _count_per_word body,
  get_progress body, get_training_data docstring + body.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,968 | +30 |
| NeuroVoice | 10,671 | +30 |
| Combined | 19,639 | +60 |
| metaMirage | post-deadline | — |

---

## [03:10 UTC] Session — R221 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R221
- `tests/test_estimate_sps_source_5_noarch.py` (+30, suite 8,968 → 8,998).
- 5th source pass for `experiments/estimate_sps.py` (558 lines).
  New strings: core/arb_result/rsps assignments + rsps_ci.

### NeuroVoice R221
- `tests/test_calibration_source_3_noarch.py` (+30, suite 10,671 → 10,701).
- 3rd source pass for `src/emg/calibration.py` (193 lines).
  New strings: module imports, CalibrationSample word: str field + constructor kwargs,
  sample_path assignment, raise ValueError, # Save to disk, add_sample signature + @property,
  _load_existing body (meta_path/exists/open/json.load/count.get),
  get_training_data signature + Returns doc, def next_word signature.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 8,998 | +30 |
| NeuroVoice | 10,701 | +30 |
| Combined | 19,699 | +60 |
| metaMirage | post-deadline | — |

---

## [04:10 UTC] Session — R222 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R222
- `tests/test_estimate_sps_source_6_noarch.py` (+30, suite 8,998 → 9,028).
- 6th source pass for `experiments/estimate_sps.py` (558 lines, 5 prior passes).
  New strings: emb_std_s/emb_n/arb_std_s/arb_n delta-method kwargs, adv_ratio computation,
  adversarial analysis print block, moderate/large gap print branches, spectral gap analysis
  header + mean_restricted_norm/mean_full_norm/mean_gap, strong/weak gamma-bar interpretation,
  gap_ci all-batches CI block, gap_loo stable/unstable string, layer-wise bar chart █ format.

### NeuroVoice R222
- `tests/test_lexicon_rescorer_source_1_noarch.py` (+30, suite 10,701 → 10,731).
- 1st source pass for `src/correction/lexicon_rescorer.py` (244 lines, fresh file).
  New strings: module docstring (CTC non-words / C++ build / score formula / λ=0 / WER claims),
  all 4 imports, levenshtein() signature + DP body, build_lexicon def + doc + counter,
  LexiconRescorer __slots__ + max_edit_distance guard + sort comment + _words assignment.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,028 | +30 |
| NeuroVoice | 10,731 | +30 |
| Combined | 19,759 | +60 |
| metaMirage | post-deadline | — |

---

## [05:14 UTC] Session — R223 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R223
- `tests/test_estimate_sps_source_7_noarch.py` (+30, suite 9,028 → 9,058).
- 7th (final) source pass for `experiments/estimate_sps.py`.
  Covered: print_results_table line-join body, results section framing,
  rSPS tolerance block (_tol = 5e-4 / > 1 / < 1 / ≈1 print branches),
  control-flow guards, LOO stability range print, layer enumeration loop.
  `estimate_sps.py` substantially exhausted after 7 passes (210 total assertions).

### NeuroVoice R223
- `tests/test_lexicon_rescorer_source_2_noarch.py` (+30, suite 10,731 → 10,761).
- 2nd source pass for `src/correction/lexicon_rescorer.py` (244 lines).
  Covered: levenshtein inner-loop residuals, LexiconRescorer.__init__ 4 field assignments,
  from_counts classmethod body, restrict_to dict comprehension, snap_word body.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,058 | +30 |
| NeuroVoice | 10,761 | +30 |
| Combined | 19,819 | +60 |
| metaMirage | post-deadline | — |

---

## [06:15 UTC] Session — R224 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R224
- `tests/test_adversarial_sps_results_analysis_source_1_noarch.py` (+30, suite 9,058 → 9,088).
- 1st source pass for `experiments/adversarial_sps_results_analysis.py` (182 lines,
  0 prior source passes). Pivot from estimate_sps.py (exhausted after 7 passes, R223).
  New strings: module docstring, constants (_BASE/_LARGE/_DEBERTA/H1_LO/H1_HI defaults),
  load_results (signature/docstring/with open/return json.load), format_results_table header
  and model loop, verify_hypotheses gap getters + gap_ratio + h1/h2/h3 bools + H1 f-string.

### NeuroVoice R224
- `tests/test_lexicon_rescorer_source_3_noarch.py` (+30, suite 10,761 → 10,791).
- 3rd source pass for `src/correction/lexicon_rescorer.py` (244 lines).
  New strings: vocab_size property, restrict_to() full docstring (4 lines) + return kwargs,
  snap_word() docstring + loop residuals, rescore_sentence full body + rescore_batch signature.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,088 | +30 |
| NeuroVoice | 10,791 | +30 |
| Combined | 19,879 | +60 |
| metaMirage | post-deadline | — |

---

## [07:17 UTC] Session — R225 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R225
- `tests/test_adversarial_sps_results_analysis_source_2_noarch.py` (+30, suite 9,088 → 9,118).
- 2nd source pass for `experiments/adversarial_sps_results_analysis.py` (182 lines).
  New strings: format_results_table() remaining body, verify_hypotheses() docstring +
  H2/H3 report lines, build_summary() + format_run_report() + main().

### NeuroVoice R225
- `tests/test_lexicon_rescorer_source_4_noarch.py` (+30, suite 10,791 → 10,821).
- 4th source pass for `src/correction/lexicon_rescorer.py` (244 lines).
  New strings: import logging + logger + __all__ entries, levenshtein early-return guards,
  build_lexicon for-loops, LexiconRescorer class docstring + __init__ raise, rescore_batch,
  misc residuals. File substantially exhausted after 4 passes.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,118 | +30 |
| NeuroVoice | 10,821 | +30 |
| Combined | 19,939 | +60 |
| metaMirage | post-deadline | — |

---

## [08:20 UTC] Session — R226 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R226
- `tests/test_adversarial_sps_results_analysis_source_3_noarch.py` (+30, suite 9,118 → 9,148).
- 3rd source pass for `experiments/adversarial_sps_results_analysis.py` (182 lines) — imports,
  section comments, verify_hypotheses() kwarg defaults + return dict residuals,
  build_summary() residuals, format_run_report() docstring+body,
  main() __name__ guard. File substantially exhausted after 3 passes (90 total assertions).

### NeuroVoice R226
- `tests/test_llm_corrector_source_2_noarch.py` (+30, suite 10,821 → 10,851).
- 2nd source pass for `src/correction/llm_corrector.py` (~130 lines). Pivot from
  lexicon_rescorer.py (exhausted after 4 passes). Module docstring, imports, logger,
  class docstrings, Args doc, init default, load/correct docstrings + return kwargs,
  _build_prompt + helpers. File substantially exhausted after 2 passes.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,148 | +30 |
| NeuroVoice | 10,851 | +30 |
| Combined | 19,999 | +60 |
| metaMirage | post-deadline | — |

---

## [09:30 UTC] Session — R227 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R227
- `tests/test_bootstrap_sign_flip_source_1_noarch.py` (+30, suite 9,148 → 9,178).
- 1st source pass for `experiments/bootstrap_sign_flip.py` (265 lines, 0 prior source passes).
  Module docstring (filename/re-verifies/sign-flip-finding lines + verified output block:
  global/conf_inv/forced_a/exp_trap), INLINE_DATA model names (7 entries) + tdr_global +
  aq_clean + 3 TDR arrays (confidence_inversion/forced_abstention/expertise_trap),
  pearson_r() def signature + docstring + body (mx/num/denom_x/denom_y/denom).

### NeuroVoice R227
- `tests/test_whisper_hypotheses_source_2_noarch.py` (+30, suite 10,851 → 10,881).
- 2nd source pass for `src/correction/whisper_hypotheses.py` (~120 lines).
  Module docstring (4 lines), all import lines, logger setup, @dataclasses.dataclass(slots=True)
  decorator, class declarations + docstrings + Args block, load() full body
  (transformers import + from_pretrained + model.eval + logger.info),
  generate() docstring Args/Returns/Raises block.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,178 | +30 |
| NeuroVoice | 10,881 | +30 |
| Combined | 20,059 | +60 |
| metaMirage | post-deadline | — |

---

## [10:30 UTC] Session — R228 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R228
- `tests/test_bootstrap_sign_flip_source_2_noarch.py` (+30, suite 9,178 → 9,208).
- 2nd source pass for `experiments/bootstrap_sign_flip.py` — module header / CI docstring /
  loo_correlation def+doc / main() argparse + inline INLINE_DATA assignments.

### NeuroVoice R228
- `tests/test_whisper_hypotheses_source_3_noarch.py` (+30, suite 10,881 → 10,911).
- 3rd (final) source pass for `src/correction/whisper_hypotheses.py` — TYPE_CHECKING block /
  __init__ full signature / generate() params + docstring / inference body exhausted.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,208 | +30 |
| NeuroVoice | 10,911 | +30 |
| Combined | 20,119 | +60 |
| metaMirage | post-deadline | — |

---

## [11:30 UTC] Session — R229 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R229
- `tests/test_stats_source_1_noarch.py` (+30, suite 9,208 → 9,238).
- 1st source pass for `src/sps/stats.py` (12,748 bytes, 0 prior source passes).
  Module docstring (title/subtitle/Implements/uncertainty-bounds/7 function table entries),
  6 of 7 __all__ entries (bootstrap_ci through compare_architecture_cis),
  module imports (from __future__/math/random), bootstrap_ci() function signature
  (def + 4 typed params + return annotation), bootstrap_ci() docstring (opening line +
  two percentile-bootstrap lines + "Sample of scalar observations." param entry).

### NeuroVoice R229
- `tests/test_beam_decoder_source_1_noarch.py` (+30, suite 10,911 → 10,941).
- 1st source pass for `src/emg/beam_decoder.py` (16,266 bytes, 0 prior source passes).
  Module docstring (10 content lines: title/ContinuousEMGEncoder/produces-hypotheses/
  3 scoring bullets/combined_score formula/temporal-axis note/marisa_trie/dict-fallback),
  module imports (logging/math/dataclass+field/Callable+Protocol+runtime_checkable),
  4 constants (CTC_BLANK_ID/DEFAULT_BEAM_WIDTH/DEFAULT_LM_WEIGHT/NEG_INF),
  LanguageModel Protocol (@runtime_checkable/class/docstring/score_prefix/language_code),
  VocabularyTrie class head (class declaration/docstring title/Args docs).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,238 | +30 |
| NeuroVoice | 10,941 | +30 |
| Combined | 20,179 | +60 |
| metaMirage | post-deadline | — |

---

## [12:00 UTC] Session — R230 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R230
- `tests/test_stats_source_2_noarch.py` (+30, suite 9,238 → 9,268).
- 2nd source pass for `src/sps/stats.py` — 7th __all__ entry (permutation_test_gap_diff) /
  bootstrap_ci() param docs (values/n_boot/alpha headers + desc + Returns type + rounding note) /
  Notes section / full bootstrap_ci() body (n/empty guard/rng/sorted comp/lo_idx/hi_idx/
  return triple) / loo_spectral_gap() signature + docstring + n<3 guard + total + loo_means.

### NeuroVoice R230
- `tests/test_beam_decoder_source_2_noarch.py` (+30, suite 10,941 → 10,971).
- 2nd source pass for `src/emg/beam_decoder.py` — VocabularyTrie.__init__ body (set comp /
  marisa=None / dict_trie init / use_marisa guard / try-import marisa_trie / Trie init /
  logger.debug marisa + fallback / except ImportError) / _build_dict_trie (def + docstring +
  root init + word loop + char loop + setdefault + terminal marker) / is_valid_prefix
  (def + docstring + arg/returns doc + prefix.lower() + marisa branch + dict-trie comment) /
  is_complete_word (def + return).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,268 | +30 |
| NeuroVoice | 10,971 | +30 |
| Combined | 20,239 | +60 |
| metaMirage | post-deadline | — |

---

## [13:00 UTC] Session — R231 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R231
- `tests/test_stats_source_3_noarch.py` (+30, suite 9,268 → 9,298).
- 3rd source pass for `src/sps/stats.py` — loo_spectral_gap() return dict (all 6 key-value
  pairs: n/mean/loo_min/loo_max/loo_range/stable) / Returns docstring (dict-with-keys header +
  6 EM-dash descriptions + n<3 sentence) / delta_method_rsps_ci() function signature
  (6 typed params + alpha default) / Derivation docstring (SPS-definition / 3-step log-rSPS
  derivation / Var(log rSPS) formula / CI formula with ±z×SE).

### NeuroVoice R231
- `tests/test_beam_decoder_source_3_noarch.py` (+30, suite 10,971 → 11,001).
- 3rd source pass for `src/emg/beam_decoder.py` — BeamHypothesis @dataclass(slots=True) +
  class declaration + Attributes docstring (all 7 field descriptions with ≤ / ×) + all 7 typed
  field declarations / _BeamState class declaration + one-line docstring + prefix field +
  p_blank/p_nonblank/lm_score fields / total_log_prob @property def + return body /
  _log_add() def + docstring + 3 body branches (a==NEG_INF / b==NEG_INF / final return).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,298 | +30 |
| NeuroVoice | 11,001 | +30 |
| Combined | 20,299 | +60 |
| metaMirage | post-deadline | — |

---

## [13:30 UTC] Session — R232 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R232
- `tests/test_stats_source_4_noarch.py` (+30, suite 9,298 → 9,328).
- 4th source pass for `src/sps/stats.py` — delta_method_rsps_ci() Parameters section
  (emb_mean_s/emb_std_s/emb_n/arb_mean_s/arb_std_s/arb_n/alpha docs), Wald-interval note,
  Returns line, z-score body (1.96/2.576/1.645 branches), log_rsps/var_log/se_log/rsps_point
  assignments, rsps_lo/rsps_hi/return triple; loo_batch_spectral_gap() signature + opening doc.

### NeuroVoice R232
- `tests/test_beam_decoder_source_4_noarch.py` (+30, suite 11,001 → 11,031).
- 4th source pass for `src/emg/beam_decoder.py` — is_valid_prefix dict-trie traversal body
  (node=self._dict_trie / for ch / isinstance check / return False / node[ch] / return True),
  VocabularyTrie.size property, MultilingualBeamDecoder class + docstring + Usage block +
  Args section + __init__ signature + blank guard + self._language_models attr.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,328 | +30 |
| NeuroVoice | 11,031 | +30 |
| Combined | 20,359 | +60 |
| metaMirage | post-deadline | — |

---

## [14:30 UTC] Session — R233 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R233
- `tests/test_stats_source_11_noarch.py` (+30, suite 9,328 → 9,358).
- 11th source pass for `src/sps/stats.py` (5th in the new indented series).
  New strings: loo_batch_spectral_gap() param desc (Per-batch mean / gamma-bar lines),
  Returns section (8-space indent, all 7 key descriptions), n_batches<3 note,
  full body with 4-space indent (n=len / n<3 guard / return / total / loo_means /
  grand_mean / worst_idx / return dict n_batches/grand_mean/worst_batch);
  spectral_gap_report() def + per_sample_gaps + n_boot params + docstring
  (Consolidated / Combines / Table 3) + body (bootstrap_ci call / loo_spectral_gap /
  loo_stable / loo_range get / "ci_width" key).

### NeuroVoice R233
- `tests/test_beam_decoder_source_6_noarch.py` (+30, suite 11,031 → 11,061).
- 6th source pass for `src/emg/beam_decoder.py` (16,266 bytes).
  New strings: __init__ body residuals (ValueError f-string / self._id_to_char /
  self._beam_width / self._lm_weight / self._trie), decode() signature (def / log_probs /
  language / top_k / return annotation), decode() docstring (triple-quote opening /
  log_probs arg / log-softmax note / language arg / top_k arg / Returns / Length / Raises),
  decode() body (ndim guard / isfinite guard / raise ValueError NaN/Inf / T, n_tokens /
  lm / # Initialise beams / # State dict / beams dict / () _BeamState / for t / frame_log_probs
  / new_beams).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,358 | +30 |
| NeuroVoice | 11,061 | +30 |
| Combined | 20,419 | +60 |
| metaMirage | post-deadline | — |

---

## [15:25 UTC] Session — R234 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R234
- `tests/test_stats_source_12_noarch.py` (+30, suite 9,358 → 9,388).
- 12th source pass for `src/sps/stats.py` — compare_architecture_cis + permutation_test_gap_diff
  doc/body residuals + spectral_gap_report/permtest Returns dict key descriptions (8-space indent).

### NeuroVoice R234
- `tests/test_beam_decoder_source_7_noarch.py` (+30, suite 11,061 → 11,091).
- 7th source pass for `src/emg/beam_decoder.py` — decode() inner loop: sorted_beams phase,
  non-blank extension with trie constraint, CTC rule, and LM rescoring at word boundaries.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,388 | +30 |
| NeuroVoice | 11,091 | +30 |
| Combined | 20,479 | +60 |
| metaMirage | post-deadline | — |

---

## [16:19 UTC] Session — R235 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R235
- `tests/test_stats_source_13_noarch.py` (+30, suite 9,388 → 9,418).
- 13th source pass for `src/sps/stats.py` (394 lines).
  New territory: exact-indented strings across spectral_gap_report param docs,
  compare_architecture_cis body + docstring, permutation_test_gap_diff signature + body.

### NeuroVoice R235
- `tests/test_beam_decoder_source_8_noarch.py` (+30, suite 11,091 → 11,121).
- 8th source pass for `src/emg/beam_decoder.py` (448 lines).
  New strings: module docstring title / Scoring: / Design notes: / Pure-Python beam search /
  LM scoring deferred / can be swapped / 4 section separator comments / LanguageModel
  interface docstrings / VocabularyTrie class docstring + During beam search + in the
  vocabulary are kept / words: list[str], + use_marisa: bool = True, / is_valid_prefix
  docstring + is_complete_word docstring + word: Candidate arg / decode body: if prefix
  not in new_beams + p_blank log_add arg + shape error msg / _prefix_to_text def + docstring
  + prefix: Tuple of token ids. / _is_word_boundary def + docstring + prefix: Current token id.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,418 | +30 |
| NeuroVoice | 11,121 | +30 |
| Combined | 20,539 | +60 |
| metaMirage | post-deadline | — |

---

## [17:00 UTC] Session — R236 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R236
- `tests/test_stats_source_14_noarch.py` (+30, suite 9,418 → 9,448).
- 14th source pass for `src/sps/stats.py` (394 lines).
  New strings: permutation_test_gap_diff() body init (rng = random.Random(seed) /
  if na == 0 or nb == 0: / early-return dict 8+16-space / n_total = na + nb /
  mean_a / mean_b / obs_diff = mean_a - mean_b / for _ in range(n_perm): /
  rng.shuffle(pooled)) + loop body (perm_a = pooled[:na] / perm_b = pooled[na:] /
  perm_diff = sum(perm_a)/na - sum(perm_b)/nb / if abs(perm_diff) >= abs(obs_diff): /
  elif alternative == "greater": / if perm_diff >= obs_diff: /
  else: # "less" / if perm_diff <= obs_diff:) + sig residuals (gaps_b: list[float], /
  seed: int = 42,) + return dict 8-space keys (p_value / n_perm / n_a / n_b / alternative) +
  spectral_gap_report actual return dict 8-space keys (mean / ci_lo / ci_hi /
  loo_stable / loo_range).

### NeuroVoice R236
- `tests/test_beam_decoder_source_9_noarch.py` (+30, suite 11,121 → 11,151).
- 9th source pass for `src/emg/beam_decoder.py` (448 lines).
  New strings: _BeamState field comments (# Prefix as a tuple of token ids / # Log-prob
  of paths ending in blank / # Log-prob of paths ending in non-blank / # Cumulative LM
  log-prob) + _log_add if a > b: branch + return a + math.log1p(math.exp(b - a)) +
  VocabularyTrie docstring body (Supports two backends: / marisa_trie desc / dict-of-dicts
  desc / whitespace-tokenised note) + _prefix_to_text body (Consecutive identical ids /
  Token 0 (blank) ignored / Decoded text string. / chars: list[str] = [] / prev = -1 /
  for tok_id in prefix / blank guard / prev = -1 reset / repeat guard / chars.append /
  prev = tok_id / return join+strip) + _is_word_boundary body (if not prefix /
  return False / last_char assign / return space check) + decode non-blank beam update
  (if new_prefix not in new_beams / new_beams[new_prefix] = _BeamState / p_nonblank
  _log_add / Keep higher LM score comment).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,448 | +30 |
| NeuroVoice | 11,151 | +30 |
| Combined | 20,599 | +60 |
| metaMirage | post-deadline | — |

---

## [18:19 UTC] Session — R237 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R237
- `tests/test_stats_source_15_noarch.py` (+30, suite 9,448 → 9,478).
- 15th source pass for `src/sps/stats.py` (394 lines).
  New strings: "Functions\n---------" module doc section (multiline); all 7 __all__ entries
  with exact 4-space indent; seed:/RNG seed param docs (multi-function); n=len(per_sample_gaps)
  + if n<3: (loo body); alternative: str="two-sided", + gaps_a,b: + 3 permtest description
  lines + alt param doc line 2; ----------/------- structural separators;
  10 multiline compound assertions (n<3 return pairs, boot_means sorted, Parameters+Returns
  section headers, permtest signature consecutive pairs).

### NeuroVoice R237
- `tests/test_beam_decoder_source_10_noarch.py` (+30, suite 11,151 → 11,181).
- 10th source pass for `src/emg/beam_decoder.py` (448 lines).
  New strings: module doc residuals (Token index 0/windows C extension note); from __future__
  annotations + import numpy as np + logger=logging.getLogger(__name__); Constants section
  comment (Unicode box); LanguageModel Args/tokens/Returns/log-prob/... (8/12-space);
  VocabularyTrie __init__ sig (def/self/)->None:); 3 logger.debug calls (marisa-16/fallback-16/
  dict-backend-8-space); self._dict_trie=root; blank-extend block (new_p_blank _log_add +
  new_beams[prefix]=_BeamState( + prefix/lm_score kwarg + p_blank _log_add update, 16/20/24-space);
  prune block (beams=dict/sorted/new_beams.items/key lambda/beam_width slice, 12/16/20-space);
  raw_hypotheses list init + total_log_prob+lm_weight + for state in beams.values +
  raw_hypotheses.sort + for rank enumerate (8/12/16-space).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,478 | +30 |
| NeuroVoice | 11,181 | +30 |
| Combined | 20,659 | +60 |
| metaMirage | post-deadline | — |

---

## [19:14 UTC] Session — R238 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R238
- `tests/test_stats_source_16_noarch.py` (+30, suite 9,478 → 9,508).
- 16th source pass for `src/sps/stats.py` (394 lines).
  New strings: module doc header lines, from __future__/import math/import random,
  def bootstrap_ci( / values/alpha params / return annotation / n=len(values),
  if n==0 / return 0.0,0.0,0.0 / lo_idx / hi_idx / return( / round triple,
  def loo_spectral_gap / early-return n / total=sum / loo_means=listcomp /
  return dict: "n":n / "mean" / "loo_min" / "loo_max" / "loo_range" / "stable",
  def delta_method_rsps_ci( / emb_mean_s: float,.

### NeuroVoice R238
- `tests/test_beam_decoder_source_11_noarch.py` (+30, suite 11,181 → 11,211).
- 11th source pass for `src/emg/beam_decoder.py` (448 lines).
  New strings: module docstring opening (→ text decoding / CTCHead / ranked hypotheses via /
  3 bullet items), import logging/math/dataclasses/typing, 4 constants (BLANK_ID/BEAM_WIDTH/
  LM_WEIGHT/NEG_INF), @runtime_checkable / class LanguageModel(Protocol) / docstring /
  score_prefix sig / @property / language_code sig, class VocabularyTrie: / ) -> None: /
  self._words set comp / self._marisa=None / self._dict_trie init / if use_marisa: /
  try: / import marisa_trie / marisa_trie.Trie init / return.

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,508 | +30 |
| NeuroVoice | 11,211 | +30 |
| Combined | 20,719 | +60 |
| metaMirage | post-deadline | — |

---

## [20:15 UTC] Session — R239 (Satellite Tracking)

metaMirage is post-deadline steady state. No new code or tests this session.

### SPS R239
- `tests/test_stats_source_17_noarch.py` (+30, suite 9,508 → 9,538).
- 17th source pass for `src/sps/stats.py` — delta_method_rsps_ci() param lines
  (emb_std_s/emb_n/arb_mean_s/arb_std_s/arb_n), Parameters doc E_x/Std_x lines,
  Returns triple, z-score guards, loo_batch middle docstring, loo_spectral_gap docstring
  body, per_batch_means param header, Derivation section residuals.

### NeuroVoice R239
- `tests/test_beam_decoder_source_12_noarch.py` (+30, suite 11,211 → 11,241).
- 12th source pass for `src/emg/beam_decoder.py` — decode() final hypothesis loop
  (hypotheses init / best_combined / for rank enumerate / BeamHypothesis kwargs) +
  logger.debug call + return hypotheses + CTC residuals (new_p_nonblank / p_from_nonblank /
  new_beams lm_score max).

| Repo | Suite | Delta |
|------|-------|-------|
| SPS | 9,538 | +30 |
| NeuroVoice | 11,241 | +30 |
| Combined | 20,779 | +60 |
| metaMirage | post-deadline | — |